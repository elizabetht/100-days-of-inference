#!/usr/bin/env python3
"""Generate days 28-50: Phase 4 Deep Implementation."""
import json, os, sys
sys.path.insert(0, '/home/nvidia/src/github.com/elizabetht/100-days-of-inference')
from gen_notebooks import cell_md, cell_code, write_nb, write_readme, validate

PHASE = "Implementation"

def make_day(day, slug, topic, summary, insight, key_demo, cells, refs=""):
    p = write_nb(day, slug, cells)
    write_readme(day, topic, PHASE, summary, insight, slug, key_demo, refs)
    return validate(p)

def day28():
    cells = [
        cell_md(["# Day 28: BPE Tokenizer from Scratch\n","\n**Layer:** Implementation | **Prerequisite:** Day 01 (Tokenization)\n"],"c01"),
        cell_md(["## Concept Overview\n","\n","Byte Pair Encoding (BPE) learns a vocabulary of subword tokens by iteratively merging the most frequent pair of symbols. Starting from characters, each merge step creates a new token. The final vocabulary is used to tokenize new text by applying the merge rules in order.\n"],"c02"),
        cell_code(["import collections, re, json\nimport numpy as np\nimport matplotlib.pyplot as plt\n\nprint('BPE tokenizer implementation')\n"],"c03"),
        cell_md(["## 1. BPE Training\n"],"c04"),
        cell_code(["def get_vocab(text):\n    \"\"\"Initialize vocab from character-level tokenization.\"\"\"\n    vocab = collections.defaultdict(int)\n    for word in text.split():\n        chars = ' '.join(list(word)) + ' </w>'\n        vocab[chars] += 1\n    return vocab\n\ndef get_stats(vocab):\n    \"\"\"Count frequency of each symbol pair.\"\"\"\n    pairs = collections.defaultdict(int)\n    for word, freq in vocab.items():\n        symbols = word.split()\n        for i in range(len(symbols)-1):\n            pairs[(symbols[i], symbols[i+1])] += freq\n    return pairs\n\ndef merge_vocab(pair, vocab):\n    \"\"\"Merge the most frequent pair in all vocab entries.\"\"\"\n    bigram = ' '.join(pair)\n    replacement = ''.join(pair)\n    new_vocab = {}\n    for word in vocab:\n        new_word = word.replace(bigram, replacement)\n        new_vocab[new_word] = vocab[word]\n    return new_vocab\n\ndef train_bpe(text, num_merges=50):\n    vocab = get_vocab(text)\n    merges = []\n    for i in range(num_merges):\n        pairs = get_stats(vocab)\n        if not pairs:\n            break\n        best = max(pairs, key=pairs.get)\n        vocab = merge_vocab(best, vocab)\n        merges.append(best)\n    return vocab, merges\n\ntext = 'low lower newest widest low lower'\nvocab, merges = train_bpe(text, num_merges=10)\nprint('Training text:', text)\nprint('\\nFirst 10 BPE merges:')\nfor i, merge in enumerate(merges):\n    print(f'  {i+1}: {merge[0]} + {merge[1]} -> {merge[0]+merge[1]}')\nprint('\\nFinal vocabulary:')\nfor word, freq in sorted(vocab.items(), key=lambda x: -x[1]):\n    print(f'  {word!r}: {freq}')\n"],"c05"),
        cell_md(["## 2. BPE Tokenization (Encoding)\n"],"c06"),
        cell_code(["def tokenize(word, merges):\n    \"\"\"Apply BPE merges to tokenize a word.\"\"\"\n    chars = list(word) + ['</w>']\n    tokens = chars\n    merge_set = {pair: i for i, pair in enumerate(merges)}\n    \n    while True:\n        pairs = [(tokens[i], tokens[i+1]) for i in range(len(tokens)-1)]\n        pair_priorities = {p: merge_set[p] for p in pairs if p in merge_set}\n        if not pair_priorities:\n            break\n        best = min(pair_priorities, key=pair_priorities.get)\n        i = 0\n        new_tokens = []\n        while i < len(tokens):\n            if i < len(tokens)-1 and (tokens[i], tokens[i+1]) == best:\n                new_tokens.append(tokens[i] + tokens[i+1])\n                i += 2\n            else:\n                new_tokens.append(tokens[i])\n                i += 1\n        tokens = new_tokens\n    return tokens\n\n# Test\nfor word in ['lower', 'lowest', 'newer', 'widest']:\n    tokens = tokenize(word, merges)\n    print(f'{word!r:12} -> {tokens}')\n"],"c07"),
        cell_md(["## 3. Compression Analysis\n"],"c08"),
        cell_code(["corpus = 'inference engineering is the discipline of serving generative AI models in production faster cheaper and more reliably'\nvocab, merges = train_bpe(corpus, num_merges=30)\n\n# Compare character-level vs BPE token count\nchar_tokens = len(corpus.replace(' ', ''))\nbpe_tokens = sum(len(tokenize(w, merges)) for w in corpus.split())\nprint(f'Corpus: {len(corpus.split())} words')\nprint(f'Character tokens: {char_tokens}')\nprint(f'BPE tokens (30 merges): {bpe_tokens}')\nprint(f'Compression: {char_tokens/bpe_tokens:.2f}x')\n\n# Plot compression vs merge count\ncounts = []\nfor n in range(0, 35, 5):\n    v, m = train_bpe(corpus, num_merges=n)\n    total = sum(len(tokenize(w, m)) for w in corpus.split())\n    counts.append(total)\n\nplt.plot(range(0, 35, 5), counts, 'b-o')\nplt.xlabel('Number of BPE merges'); plt.ylabel('Token count')\nplt.title('BPE Compression vs Number of Merges')\nplt.grid(True)\nplt.savefig('bpe_compression.png', dpi=100, bbox_inches='tight')\nplt.show()\n"],"c09"),
        cell_md(["## Experiments: Try These\n\n1. Scale to a larger corpus. How many merges does it take to reach vocabulary size 50K?\n2. Implement a regex pre-tokenizer (like GPT-2's) that splits on whitespace/punctuation before BPE.\n3. Compare BPE vocabulary to a character-level vocabulary on out-of-distribution text.\n"],"c10"),
        cell_md(["## Key Takeaways\n\n- BPE greedily merges the most frequent symbol pair in each training step — this is an O(V² T) algorithm for vocabulary size V and training size T.\n- The merge rules form a priority queue — tokenizing new text applies rules in training order, greedily merging the highest-priority pair first.\n- BPE balances vocabulary size against tokenization granularity — 32K-100K merges is typical for LLMs (GPT-2: 50K, Llama-3: 128K).\n- The `</w>` end-of-word marker distinguishes subwords that occur at word boundaries vs in the middle.\n\n## References\n- Sennrich et al. (2016), \"Neural Machine Translation of Rare Words with Subword Units\"\n- OpenAI tiktoken repository\n"],"c11"),
    ]
    return make_day(28, "bpe-tokenizer", "BPE Tokenizer from Scratch",
        "Implemented a full BPE tokenizer from scratch: training the merge table from a corpus, encoding text by applying merges in priority order, and measuring compression ratio vs number of merges.",
        "BPE is a greedy frequency-based algorithm — each merge is locally optimal, but the resulting vocabulary is globally near-optimal for the training corpus.",
        "BPE merge table training + token count compression curve", cells)

def day29():
    cells = [
        cell_md(["# Day 29: Autoregressive Decoder Loop\n","\n**Layer:** Implementation | **Prerequisite:** Day 01 (Decoding), Day 28 (Tokenizer)\n"],"c01"),
        cell_md(["## Concept Overview\n","\n","The autoregressive decoder loop is the core of LLM inference: repeatedly sample one token from the model's output distribution, append it to the input, and repeat until the stop condition. The KV cache avoids recomputing attention for previous tokens.\n"],"c02"),
        cell_code(["import torch\nimport torch.nn as nn\nimport torch.nn.functional as F\nimport numpy as np\nimport matplotlib.pyplot as plt\nimport time\n\nprint(f'CUDA: {torch.cuda.is_available()}')\nif torch.cuda.is_available(): print(f'GPU: {torch.cuda.get_device_name(0)}')\n"],"c03"),
        cell_md(["## 1. Minimal Transformer for Decoding\n"],"c04"),
        cell_code(["class MinimalAttention(nn.Module):\n    def __init__(self, d_model, n_heads):\n        super().__init__()\n        self.n_heads = n_heads\n        self.d_head = d_model // n_heads\n        self.qkv = nn.Linear(d_model, 3 * d_model, bias=False)\n        self.out = nn.Linear(d_model, d_model, bias=False)\n\n    def forward(self, x, kv_cache=None):\n        B, T, D = x.shape\n        qkv = self.qkv(x).chunk(3, dim=-1)\n        q, k, v = [t.view(B, T, self.n_heads, self.d_head).transpose(1,2) for t in qkv]\n        if kv_cache is not None:\n            k = torch.cat([kv_cache['k'], k], dim=2)\n            v = torch.cat([kv_cache['v'], v], dim=2)\n        new_cache = {'k': k, 'v': v}\n        scale = self.d_head ** -0.5\n        attn = F.softmax(q @ k.transpose(-2,-1) * scale, dim=-1)\n        out = (attn @ v).transpose(1,2).reshape(B, T, D)\n        return self.out(out), new_cache\n\nclass MinimalDecoder(nn.Module):\n    def __init__(self, vocab_size=1000, d_model=128, n_heads=4, n_layers=2):\n        super().__init__()\n        self.embed = nn.Embedding(vocab_size, d_model)\n        self.layers = nn.ModuleList([MinimalAttention(d_model, n_heads) for _ in range(n_layers)])\n        self.norm = nn.LayerNorm(d_model)\n        self.lm_head = nn.Linear(d_model, vocab_size, bias=False)\n        self.vocab_size = vocab_size\n\n    def forward(self, tokens, kv_caches=None):\n        x = self.embed(tokens)\n        new_caches = []\n        for i, layer in enumerate(self.layers):\n            cache = kv_caches[i] if kv_caches else None\n            x, new_cache = layer(x, cache)\n            new_caches.append(new_cache)\n        return self.lm_head(self.norm(x)), new_caches\n\nmodel = MinimalDecoder()\nprint(f'Model params: {sum(p.numel() for p in model.parameters()):,}')\n"],"c05"),
        cell_md(["## 2. Autoregressive Generation Loop\n"],"c06"),
        cell_code(["def generate(model, prompt_tokens, max_new_tokens=20, temperature=1.0,\n             top_k=50, device='cpu'):\n    model.eval()\n    tokens = torch.tensor([prompt_tokens], device=device)\n    generated = []\n    kv_caches = None\n    ttft_measured = False\n    times = []\n    t_start = time.perf_counter()\n\n    with torch.no_grad():\n        # Prefill: process all prompt tokens at once\n        logits, kv_caches = model(tokens)\n        if not ttft_measured:\n            ttft = (time.perf_counter() - t_start) * 1000\n            ttft_measured = True\n        next_token = sample_token(logits[:, -1, :], temperature, top_k)\n        generated.append(next_token)\n\n        # Decode: one token at a time, using KV cache\n        for _ in range(max_new_tokens - 1):\n            t0 = time.perf_counter()\n            inp = torch.tensor([[next_token]], device=device)\n            logits, kv_caches = model(inp, kv_caches)\n            next_token = sample_token(logits[:, -1, :], temperature, top_k)\n            times.append((time.perf_counter() - t0) * 1000)\n            generated.append(next_token)\n            if next_token == 2:  # <eos> = 2\n                break\n\n    return generated, ttft, np.mean(times) if times else 0\n\ndef sample_token(logits, temperature, top_k):\n    logits = logits / temperature\n    top_logits, top_indices = torch.topk(logits, top_k)\n    probs = F.softmax(top_logits, dim=-1)\n    idx = torch.multinomial(probs, 1)\n    return top_indices[0, idx.item()].item()\n\ntorch.manual_seed(42)\nprompt = [10, 20, 30, 40, 50]\ngenerated, ttft, tpot = generate(model, prompt, max_new_tokens=20)\nprint(f'Prompt: {prompt}')\nprint(f'Generated: {generated}')\nprint(f'TTFT: {ttft:.2f} ms (prefill {len(prompt)} tokens)')\nprint(f'TPOT: {tpot:.2f} ms (mean decode step)')\n"],"c07"),
        cell_md(["## 3. KV Cache Memory Growth\n"],"c08"),
        cell_code(["# Measure KV cache memory vs sequence length\ndef measure_kv_growth(model, max_len=100):\n    tokens = [1] * 10  # start with 10 tokens\n    _, kv = model(torch.tensor([tokens]))\n    sizes = []\n    for i in range(max_len):\n        total_bytes = sum(\n            kv[l]['k'].nelement() * kv[l]['k'].element_size() +\n            kv[l]['v'].nelement() * kv[l]['v'].element_size()\n            for l in range(len(kv))\n        )\n        sizes.append(total_bytes / 1024)  # KB\n        next_tok = torch.tensor([[42]])\n        _, kv = model(next_tok, kv)\n    return sizes\n\nsizes = measure_kv_growth(model)\nplt.plot(range(10, 10+len(sizes)), sizes)\nplt.xlabel('Sequence length'); plt.ylabel('KV cache size (KB)')\nplt.title('KV Cache Memory Growth (Linear in Sequence Length)')\nplt.grid(True)\nplt.savefig('kv_growth.png', dpi=100, bbox_inches='tight')\nplt.show()\nprint(f'KV cache at seq=10: {sizes[0]:.2f} KB')\nprint(f'KV cache at seq=110: {sizes[-1]:.2f} KB')\nprint(f'Linear growth ratio: {sizes[-1]/sizes[0]:.1f}x (10x length = {sizes[-1]/sizes[0]:.1f}x memory)')\n"],"c09"),
        cell_md(["## Experiments: Try These\n\n1. Add streaming output: yield tokens as they're generated and measure user-perceived time-to-first-token.\n2. Implement beam search on top of this loop. How does it change latency vs greedy?\n3. Remove the KV cache and measure the slowdown per token as sequence length grows.\n"],"c10"),
        cell_md(["## Key Takeaways\n\n- The autoregressive loop is: prefill once (full prompt in parallel) → decode one token at a time, appending to context.\n- KV cache grows linearly with sequence length — this is why context length × batch size × model size determines GPU memory budget.\n- Sampling temperature controls output diversity; top-k/top-p pruning prevents low-probability nonsense tokens.\n- TTFT is dominated by prefill (quadratic in prompt length); TPOT is dominated by weight loading (linear, memory-bound).\n\n## References\n- Brown et al. (2020), \"Language Models are Few-Shot Learners\"\n- HuggingFace generate() documentation\n"],"c11"),
    ]
    return make_day(29, "autoregressive-decoder", "Autoregressive Decoder Loop",
        "Built a complete autoregressive decoder loop with KV cache from scratch. Measured TTFT vs TPOT, tracked KV cache memory growth with sequence length, and implemented top-k sampling.",
        "The KV cache converts attention from O(n²) recomputation to O(n) memory — without it, each decode step requires full attention over all prior tokens.",
        "KV cache memory growth visualization + TTFT/TPOT measurement", cells)

def day30():
    cells = [
        cell_md(["# Day 30: Scaled Dot-Product Attention with Masking\n","\n**Layer:** Implementation | **Prerequisite:** Day 02 (Transformer), Day 29 (Decoder)\n"],"c01"),
        cell_md(["## Concept Overview\n","\n","Scaled dot-product attention (SDPA) is the core computation of transformers. For queries Q, keys K, and values V: Attention(Q,K,V) = softmax(QKᵀ/√d_head) · V. The causal mask ensures tokens only attend to earlier positions. Understanding SDPA in detail reveals why FlashAttention and GQA are so important.\n"],"c02"),
        cell_code(["import torch\nimport torch.nn.functional as F\nimport numpy as np\nimport matplotlib.pyplot as plt\nimport time\n\nprint(f'CUDA: {torch.cuda.is_available()}')\nif torch.cuda.is_available(): print(f'GPU: {torch.cuda.get_device_name(0)}')\n"],"c03"),
        cell_md(["## 1. SDPA Implementation\n"],"c04"),
        cell_code(["def sdpa(Q, K, V, mask=None, dropout_p=0.0):\n    \"\"\"\n    Scaled Dot-Product Attention.\n    Q,K,V: [batch, heads, seq, d_head]\n    Returns: [batch, heads, seq, d_head]\n    \"\"\"\n    d = Q.shape[-1]\n    scores = Q @ K.transpose(-2, -1) / (d ** 0.5)  # [B, H, T, T]\n    if mask is not None:\n        scores = scores.masked_fill(mask == 0, float('-inf'))\n    weights = F.softmax(scores, dim=-1)\n    weights = F.dropout(weights, p=dropout_p)\n    return weights @ V, weights\n\ndef causal_mask(seq_len, device='cpu'):\n    \"\"\"Lower triangular causal mask (attend to self + past).\"\"\"\n    return torch.tril(torch.ones(seq_len, seq_len, device=device))\n\n# Test\ntorch.manual_seed(42)\nB, H, T, d = 2, 4, 16, 32\nQ = torch.randn(B, H, T, d)\nK = torch.randn(B, H, T, d)\nV = torch.randn(B, H, T, d)\nmask = causal_mask(T)\n\nout, weights = sdpa(Q, K, V, mask)\nprint(f'Input shape:  Q={Q.shape}')\nprint(f'Output shape: {out.shape}')\nprint(f'Attention weights shape: {weights.shape}')\nprint(f'Weights sum to 1 per query: {weights[0,0].sum(dim=-1).allclose(torch.ones(T))} (unmasked)')\n\n# Verify causal: weight[i,j]=0 for j>i\ncausal_ok = (weights[0,0].triu(diagonal=1).abs().max() < 1e-6).item()\nprint(f'Causal masking correct: {causal_ok}')\n"],"c05"),
        cell_md(["## 2. Visualizing Attention Patterns\n"],"c06"),
        cell_code(["# Visualize attention weights\nfig, axes = plt.subplots(1, 2, figsize=(12, 5))\n\n# Causal attention\nax = axes[0]\nim = ax.imshow(weights[0, 0].detach().numpy(), cmap='Blues')\nplt.colorbar(im, ax=ax)\nax.set_title('Causal Attention Weights\\n(Lower triangular)')\nax.set_xlabel('Key position'); ax.set_ylabel('Query position')\n\n# Full (bidirectional) attention\n_, weights_full = sdpa(Q, K, V)  # no mask\nax = axes[1]\nim = ax.imshow(weights_full[0, 0].detach().numpy(), cmap='Blues')\nplt.colorbar(im, ax=ax)\nax.set_title('Bidirectional Attention Weights\\n(Full)')\nax.set_xlabel('Key position'); ax.set_ylabel('Query position')\n\nplt.tight_layout()\nplt.savefig('attention_patterns.png', dpi=100, bbox_inches='tight')\nplt.show()\n"],"c07"),
        cell_md(["## 3. Memory and Compute Scaling\n"],"c08"),
        cell_code(["device = 'cuda' if torch.cuda.is_available() else 'cpu'\n\ndef benchmark_sdpa(seq_len, d=64, heads=8, batch=1, iters=20):\n    Q = torch.randn(batch, heads, seq_len, d, device=device, dtype=torch.float16)\n    K = torch.randn_like(Q); V = torch.randn_like(Q)\n    mask = causal_mask(seq_len, device=device)\n    # Warmup\n    for _ in range(3): sdpa(Q, K, V, mask)\n    if device=='cuda': torch.cuda.synchronize()\n    t0 = time.perf_counter()\n    for _ in range(iters): sdpa(Q, K, V, mask)\n    if device=='cuda': torch.cuda.synchronize()\n    return (time.perf_counter()-t0)/iters*1000\n\ndef attn_memory_mb(seq_len, heads=8, batch=1):\n    \"\"\"Memory for attention score matrix (FP16).\"\"\"\n    return batch * heads * seq_len * seq_len * 2 / 1e6\n\nprint(f'{\"Seq Len\":>10} {\"Time (ms)\":>12} {\"Attn Matrix (MB)\":>20}')\nfor seq in [64, 128, 256, 512, 1024, 2048]:\n    t = benchmark_sdpa(seq)\n    mem = attn_memory_mb(seq)\n    print(f'{seq:>10} {t:>12.3f} {mem:>20.1f}')\nprint('Note: attention matrix grows O(seq²) — this is what FlashAttention eliminates from HBM')\n"],"c09"),
        cell_md(["## 4. Flash Attention vs Standard SDPA\n"],"c10"),
        cell_code(["# Compare PyTorch's SDPA backends\nif torch.cuda.is_available():\n    for backend, use_flash in [('Standard', False), ('Flash/Mem-Eff', True)]:\n        Q2 = torch.randn(2, 8, 512, 64, device='cuda', dtype=torch.float16)\n        K2, V2 = torch.randn_like(Q2), torch.randn_like(Q2)\n        # Warmup\n        for _ in range(5):\n            if use_flash:\n                F.scaled_dot_product_attention(Q2, K2, V2, is_causal=True)\n            else:\n                sdpa(Q2, K2, V2, causal_mask(512, 'cuda'))\n        torch.cuda.synchronize()\n        t0 = time.perf_counter()\n        for _ in range(50):\n            if use_flash:\n                out = F.scaled_dot_product_attention(Q2, K2, V2, is_causal=True)\n            else:\n                out, _ = sdpa(Q2, K2, V2, causal_mask(512, 'cuda'))\n        torch.cuda.synchronize()\n        t_ms = (time.perf_counter()-t0)/50*1000\n        print(f'{backend}: {t_ms:.3f} ms (seq=512, batch=2, h=8)')\nelse:\n    print('Flash attention comparison requires CUDA GPU')\n    print('F.scaled_dot_product_attention() automatically uses FlashAttention when available')\n"],"c11"),
        cell_md(["## Experiments\n\n1. Measure the inflection point where standard SDPA runs OOM vs FlashAttention.\n2. Implement multi-query attention (MQA) where K,V have 1 head while Q has N heads.\n3. Implement ALiBi position bias instead of causal mask — how does it affect the attention pattern?\n"],"c12"),
        cell_md(["## Key Takeaways\n\n- SDPA has O(n²) time and space complexity in sequence length — the attention score matrix is seq×seq.\n- Causal masking is implemented by filling future positions with -inf before softmax, producing zero attention weight.\n- F.scaled_dot_product_attention() automatically dispatches to FlashAttention when available — use it, never write a manual SDPA in production.\n- The attention score matrix is the primary memory bottleneck for long-context inference — 1K seq, 32 heads, FP16 = 64MB just for scores.\n\n## References\n- Vaswani et al. (2017), \"Attention Is All You Need\"\n- Dao et al. (2022), \"FlashAttention\"\n"],"c13"),
    ]
    return make_day(30, "scaled-dot-product-attention", "Scaled Dot-Product Attention with Masking",
        "Implemented SDPA from scratch with causal masking. Visualized attention patterns, measured O(n²) memory and compute scaling, and compared against PyTorch's F.scaled_dot_product_attention() which automatically dispatches to FlashAttention.",
        "The attention score matrix is O(n²) — 4096² × 32 heads × FP16 = 1GB just for scores. This is the exact bottleneck FlashAttention eliminates by never materializing the full matrix in HBM.",
        "Attention pattern visualization + seq² memory scaling", cells)

def day31():
    cells = [
        cell_md(["# Day 31: Flash Attention — Tiling in Python\n","\n**Layer:** Implementation | **Prerequisite:** Day 30 (SDPA)\n"],"c01"),
        cell_md(["## Concept Overview\n","\n","FlashAttention avoids materializing the full O(n²) attention score matrix in HBM by computing attention in tiles that fit in SRAM. Each tile computes a partial softmax using the online softmax trick (tracking running max and sum), then updates the output. The key result: O(n) HBM reads/writes instead of O(n²).\n"],"c02"),
        cell_code(["import torch\nimport torch.nn.functional as F\nimport numpy as np\nimport matplotlib.pyplot as plt\nimport time\n\nprint(f'CUDA: {torch.cuda.is_available()}')\nif torch.cuda.is_available(): print(f'GPU: {torch.cuda.get_device_name(0)}')\n"],"c03"),
        cell_md(["## 1. Online Softmax\n","\n","The key primitive: compute softmax incrementally without seeing all values first.\n$$m_i = \\max(m_{i-1}, x_i), \\quad d_i = d_{i-1} e^{m_{i-1}-m_i} + e^{x_i - m_i}$$\n"],"c04"),
        cell_code(["def online_softmax(x):\n    \"\"\"Numerically stable online softmax.\"\"\"\n    m = float('-inf')  # running max\n    d = 0.0            # running sum\n    for xi in x:\n        m_new = max(m, xi.item())\n        d = d * (m - m_new) ** 2 + (xi - m_new).exp().item()  # update with rescaling\n        d = d * np.exp(m - m_new) + np.exp(xi.item() - m_new)\n        m = m_new\n    return torch.tensor([np.exp(xi.item() - m) / d for xi in x])\n\n# Verify correctness\ntorch.manual_seed(42)\nx = torch.randn(10)\nonline = online_softmax(x)\nstandard = F.softmax(x, dim=0)\nprint(f'Online softmax matches standard: {torch.allclose(online, standard, atol=1e-5)}')\nprint(f'Max difference: {(online - standard).abs().max().item():.2e}')\n"],"c05"),
        cell_md(["## 2. Tiled Flash Attention\n"],"c06"),
        cell_code(["def flash_attention_python(Q, K, V, block_size=32):\n    \"\"\"\n    Simplified Flash Attention in pure Python/PyTorch.\n    Q,K,V: [seq, d_head] (single head, no batch)\n    \"\"\"\n    T, d = Q.shape\n    scale = d ** -0.5\n    O = torch.zeros(T, d)\n    l = torch.zeros(T)   # normalization factors\n    m = torch.full((T,), float('-inf'))  # running max\n\n    for j_start in range(0, T, block_size):  # key/value block\n        j_end = min(j_start + block_size, T)\n        Kj = K[j_start:j_end]  # [block, d]\n        Vj = V[j_start:j_end]  # [block, d]\n\n        for i_start in range(0, T, block_size):  # query block\n            i_end = min(i_start + block_size, T)\n            Qi = Q[i_start:i_end]  # [block, d]\n\n            # Compute attention scores for this tile\n            Sij = Qi @ Kj.T * scale  # [Qi_block, Kj_block]\n\n            # Causal mask: query position must be >= key position\n            qi_range = torch.arange(i_start, i_end).unsqueeze(1)\n            kj_range = torch.arange(j_start, j_end).unsqueeze(0)\n            causal = qi_range >= kj_range\n            Sij = Sij.masked_fill(~causal, float('-inf'))\n\n            # Online softmax update\n            mi_new = torch.maximum(m[i_start:i_end], Sij.max(dim=1).values)\n            Pij = torch.exp(Sij - mi_new.unsqueeze(1))  # [block, block]\n            # Correct previous output for new max\n            correction = torch.exp(m[i_start:i_end] - mi_new)\n            O[i_start:i_end] = O[i_start:i_end] * correction.unsqueeze(1) + Pij @ Vj\n            l[i_start:i_end] = l[i_start:i_end] * correction + Pij.sum(dim=1)\n            m[i_start:i_end] = mi_new\n\n    # Normalize\n    return O / l.unsqueeze(1)\n\n# Verify against standard SDPA\ntorch.manual_seed(42)\nT, d = 64, 32\nQ = torch.randn(T, d); K = torch.randn(T, d); V = torch.randn(T, d)\n\nout_flash = flash_attention_python(Q, K, V, block_size=16)\n# Standard reference\nscores = Q @ K.T / (d**0.5)\nmask = torch.tril(torch.ones(T, T))\nscores = scores.masked_fill(mask==0, float('-inf'))\nout_std = F.softmax(scores, dim=-1) @ V\n\nprint(f'FlashAttention matches standard: {torch.allclose(out_flash, out_std, atol=1e-4)}')\nprint(f'Max difference: {(out_flash - out_std).abs().max().item():.2e}')\n"],"c07"),
        cell_md(["## 3. HBM Access Comparison\n"],"c08"),
        cell_code(["def count_hbm_accesses(T, d, block_size, dtype_bytes=2):\n    \"\"\"Count HBM read/write bytes for standard vs flash attention.\"\"\"\n    # Standard attention\n    qkv_read = 3 * T * d * dtype_bytes\n    attn_write = T * T * dtype_bytes  # write scores\n    attn_read = T * T * dtype_bytes   # read for AV\n    out_write = T * d * dtype_bytes\n    standard = qkv_read + attn_write + attn_read + out_write\n\n    # Flash attention (tiled)\n    n_q_blocks = (T + block_size - 1) // block_size\n    n_k_blocks = (T + block_size - 1) // block_size\n    # Each Q block reads all K,V blocks; O is accumulated in SRAM\n    flash = qkv_read + n_q_blocks * (n_k_blocks * 2 * block_size * d * dtype_bytes) + out_write\n    # Simplified: dominant term is QKV read + output write (no attn matrix in HBM)\n    flash_simplified = qkv_read + out_write  # ideal\n    return standard / 1e6, flash_simplified / 1e6\n\nprint(f'{\"Seq\":>8} {\"Standard (MB)\":>16} {\"Flash (MB)\":>14} {\"Reduction\":>12}')\nfor T in [256, 512, 1024, 2048, 4096, 8192]:\n    std, flash = count_hbm_accesses(T, d=64, block_size=32)\n    print(f'{T:>8} {std:>16.2f} {flash:>14.2f} {std/flash:>11.1f}x')\n"],"c09"),
        cell_md(["## Experiments\n\n1. Measure tile size sensitivity: sweep block_size from 16 to 256. When does a larger block hurt (doesn't fit in SRAM)?\n2. Implement backward pass tiles. What additional state needs to be stored for the backward pass?\n3. Compare against triton.ops.flash_attention for the same inputs.\n"],"c10"),
        cell_md(["## Key Takeaways\n\n- FlashAttention tiles Q,K,V into SRAM-sized blocks, computing attention without ever materializing the full n×n matrix in HBM.\n- The online softmax trick makes tiled computation exact: running max and sum are maintained and used to correct previous tile outputs.\n- HBM access is reduced from O(n²) to O(n) — the dominant speedup source for long contexts.\n- The real Triton/CUDA FlashAttention goes further: double-buffering, async memory transfers, and warp-level specialization on H100.\n\n## References\n- Dao et al. (2022), \"FlashAttention\"\n- Dao et al. (2023), \"FlashAttention-2\"\n- Milakov & Gimelshein (2018), \"Online normalizer calculation for softmax\"\n"],"c11"),
    ]
    return make_day(31, "flash-attention-tiling", "Flash Attention (Simplified Tiling in Python)",
        "Implemented the online softmax primitive and tiled FlashAttention in pure Python/PyTorch. Verified correctness against standard SDPA and quantified HBM access reduction from O(n²) to O(n) as sequence length scales.",
        "The online softmax trick is the key algorithmic insight: it allows computing exact softmax incrementally without materializing all scores, enabling tile-by-tile computation that fits in SRAM.",
        "Tiled Flash Attention implementation with correctness verification + HBM access comparison", cells)

def day32():
    cells = [
        cell_md(["# Day 32: Profile Attention Memory Growth Across Sequence Lengths\n","\n**Layer:** Implementation | **Prerequisite:** Day 30 (SDPA), Day 31 (Flash Attention)\n"],"c01"),
        cell_md(["## Concept Overview\n","\n","Attention memory scales quadratically with sequence length — the 4096-token barrier for large models is fundamentally a memory constraint. This experiment systematically profiles where memory goes as sequence length grows, comparing standard SDPA, FlashAttention, and the KV cache overhead for decode.\n"],"c02"),
        cell_code(["import torch\nimport torch.nn.functional as F\nimport numpy as np\nimport matplotlib.pyplot as plt\nimport gc, time\n\nprint(f'CUDA: {torch.cuda.is_available()}')\nif torch.cuda.is_available():\n    print(f'GPU: {torch.cuda.get_device_name(0)}')\n    print(f'Total VRAM: {torch.cuda.get_device_properties(0).total_memory/1e9:.1f} GB')\n"],"c03"),
        cell_md(["## 1. Memory Breakdown by Component\n"],"c04"),
        cell_code(["def attention_memory_breakdown(seq_len, batch, heads, d_head, d_model, num_layers, dtype_bytes=2):\n    \"\"\"Compute memory for each component of attention at inference.\"\"\"\n    T, B, H, d = seq_len, batch, heads, d_head\n    D = d_model\n\n    # Weights (constant, loaded once)\n    weight_bytes = num_layers * 4 * D * D * dtype_bytes  # Q,K,V,O per layer\n    # Activations per layer\n    qkv_bytes = 3 * B * T * D * dtype_bytes\n    # Attention score matrix (O(T²)) — this is what FlashAttention avoids\n    attn_score_bytes = B * H * T * T * dtype_bytes\n    # KV cache (O(T))\n    kv_cache_bytes = num_layers * 2 * B * H * T * d * dtype_bytes\n    # Output\n    out_bytes = B * T * D * dtype_bytes\n\n    return {\n        'weights_GB': weight_bytes/1e9,\n        'kv_cache_GB': kv_cache_bytes/1e9,\n        'attn_scores_GB': attn_score_bytes/1e9,\n        'activations_GB': (qkv_bytes + out_bytes)/1e9,\n    }\n\n# Llama-3-8B config\nconfig = {'heads': 32, 'd_head': 128, 'd_model': 4096, 'num_layers': 32}\n\nprint('Attention memory breakdown (Llama-3-8B, batch=1, FP16):')\nprint(f'{\"Seq Len\":>10} {\"Weights\":>10} {\"KV Cache\":>10} {\"Attn Scores\":>13} {\"Total (no FA)\":>15}')\nprint('-' * 62)\nfor seq in [512, 1024, 2048, 4096, 8192, 16384, 32768]:\n    m = attention_memory_breakdown(seq, batch=1, **config)\n    total = sum(m.values())\n    print(f'{seq:>10} {m[\"weights_GB\"]:>10.2f} {m[\"kv_cache_GB\"]:>10.2f} {m[\"attn_scores_GB\"]:>13.2f} {total:>15.2f}')\nprint('(Units: GB)')\n"],"c05"),
        cell_md(["## 2. Empirical Memory Profiling\n"],"c06"),
        cell_code(["def profile_attention_memory(seq_len, heads=8, d_head=64, batch=1, use_flash=False, device='cpu'):\n    \"\"\"Measure actual peak memory for attention computation.\"\"\"\n    gc.collect()\n    if device == 'cuda':\n        torch.cuda.empty_cache()\n        torch.cuda.reset_peak_memory_stats()\n        baseline = torch.cuda.memory_allocated()\n\n    Q = torch.randn(batch, heads, seq_len, d_head, device=device, dtype=torch.float16)\n    K = torch.randn_like(Q)\n    V = torch.randn_like(Q)\n\n    if use_flash:\n        out = F.scaled_dot_product_attention(Q, K, V, is_causal=True)\n    else:\n        scale = d_head ** -0.5\n        scores = Q @ K.transpose(-2,-1) * scale\n        mask = torch.tril(torch.ones(seq_len, seq_len, device=device, dtype=torch.bool))\n        scores = scores.masked_fill(~mask, float('-inf'))\n        weights = F.softmax(scores, dim=-1)\n        out = weights @ V\n\n    if device == 'cuda':\n        torch.cuda.synchronize()\n        peak = (torch.cuda.max_memory_allocated() - baseline) / 1e6\n        return peak\n    else:\n        return 0  # can't measure CPU peak easily\n\ndevice = 'cuda' if torch.cuda.is_available() else 'cpu'\nseq_lens = [64, 128, 256, 512, 1024] if device == 'cuda' else [64, 128, 256]\n\nresults_std = []\nresults_flash = []\nfor seq in seq_lens:\n    m_std = profile_attention_memory(seq, use_flash=False, device=device)\n    m_flash = profile_attention_memory(seq, use_flash=True, device=device)\n    results_std.append(m_std)\n    results_flash.append(m_flash)\n    print(f'seq={seq}: standard={m_std:.1f}MB flash={m_flash:.1f}MB', end='')\n    if m_std > 0:\n        print(f' reduction={m_std/m_flash:.1f}x')\n    else:\n        print(' (CPU: no VRAM tracking)')\n"],"c07"),
        cell_md(["## 3. Plotting Growth Curves\n"],"c08"),
        cell_code(["# Analytical curves\nseq_range = np.array([128, 256, 512, 1024, 2048, 4096, 8192, 16384])\nheads, d_head, batch = 32, 128, 1\n\n# Attention score matrix: B*H*T*T*2 bytes\nattn_scores_mb = batch * heads * seq_range**2 * 2 / 1e6\n# KV cache: 2*32*H*T*d*2 bytes (32 layers)\nkv_cache_mb = 2 * 32 * heads * seq_range * d_head * 2 / 1e6\n\nfig, (ax1, ax2) = plt.subplots(1, 2, figsize=(12, 5))\n\nax1.loglog(seq_range, attn_scores_mb, 'r-o', label='Attention scores O(n²)')\nax1.loglog(seq_range, kv_cache_mb, 'b-s', label='KV cache O(n)')\nax1.set_xlabel('Sequence Length'); ax1.set_ylabel('Memory (MB)')\nax1.set_title('Attention Memory: Scores vs KV Cache')\nax1.legend(); ax1.grid(True)\n\n# The crossover point\ncrossover = np.argmin(np.abs(attn_scores_mb - kv_cache_mb))\nax1.axvline(seq_range[crossover], color='gray', linestyle='--',\n            label=f'Crossover at seq={seq_range[crossover]}')\nax1.legend()\n\n# Model total memory at different batch sizes\nax2.set_title('Total Attention Memory vs Batch Size\\n(seq=2048, Llama-3-8B)')\nbatches = [1, 2, 4, 8, 16, 32]\nfor seq in [1024, 2048, 4096]:\n    mems = [batch * heads * seq**2 * 2 / 1e9 for batch in batches]  # GB\n    ax2.plot(batches, mems, '-o', label=f'seq={seq}')\nax2.set_xlabel('Batch Size'); ax2.set_ylabel('Attention Score Memory (GB)')\nax2.legend(); ax2.grid(True)\n\nplt.tight_layout()\nplt.savefig('attention_memory_profile.png', dpi=100, bbox_inches='tight')\nplt.show()\n"],"c09"),
        cell_md(["## Experiments\n\n1. Measure prefill time vs sequence length. Does it scale O(n²) empirically?\n2. Profile the KV cache growth in the decoder loop from Day 29.\n3. Calculate the maximum sequence length that fits in your GPU VRAM for Llama-3-8B without FlashAttention vs with.\n"],"c10"),
        cell_md(["## Key Takeaways\n\n- Attention score matrix is O(n²) in memory: 4096² × 32 heads × FP16 = 1GB just for scores, before values and output.\n- KV cache is O(n) per layer: Llama-3-8B at seq=4096 needs ~2GB for KV cache, growing linearly.\n- The crossover where attention scores exceed KV cache happens around seq=1K-4K depending on head count.\n- FlashAttention converts the O(n²) score matrix from HBM to on-chip SRAM tiles — this is the reason it enables 100K+ context lengths.\n\n## References\n- Dao et al. (2022), \"FlashAttention\"\n- Pope et al. (2022), \"Efficiently Scaling Transformer Inference\"\n"],"c11"),
    ]
    return make_day(32, "attention-memory-profiling", "Profile Attention Memory Growth",
        "Systematically profiled attention memory growth: analytical breakdown by component (weights, KV cache, attention scores), empirical GPU measurement using CUDA memory stats, and visualization of the O(n²) vs O(n) scaling curves.",
        "The attention score matrix crosses the KV cache in memory at around seq=2K-4K — beyond that, FlashAttention's O(n) HBM access is not just faster but the only way to fit in VRAM.",
        "Memory breakdown table + attention scores vs KV cache scaling curves", cells)

def day33():
    cells = [
        cell_md(["# Day 33: INT8 Quantization Pipeline — Quantize, Dequantize, Measure Error\n","\n**Layer:** Implementation | **Prerequisite:** Day 11 (Quantization Formats)\n"],"c01"),
        cell_md(["## Concept Overview\n","\n","An INT8 quantization pipeline has three stages: calibration (find the optimal scale/zero-point from representative data), quantization (convert FP16 weights/activations to INT8), and dequantization (scale INT8 back to FP16 for computation). The challenge: minimizing quantization error while keeping the pipeline fast.\n"],"c02"),
        cell_code(["import torch\nimport torch.nn as nn\nimport torch.nn.functional as F\nimport numpy as np\nimport matplotlib.pyplot as plt\n\nprint(f'CUDA: {torch.cuda.is_available()}')\nif torch.cuda.is_available(): print(f'GPU: {torch.cuda.get_device_name(0)}')\n"],"c03"),
        cell_md(["## 1. Symmetric and Asymmetric Quantization\n"],"c04"),
        cell_code(["def quantize_symmetric(x, n_bits=8):\n    qmax = 2**(n_bits-1) - 1\n    scale = x.abs().max() / qmax\n    x_q = torch.clamp(torch.round(x / scale), -qmax, qmax).to(torch.int8)\n    return x_q, scale\n\ndef dequantize_symmetric(x_q, scale):\n    return x_q.float() * scale\n\ndef quantize_asymmetric(x, n_bits=8):\n    qmin, qmax = 0, 2**n_bits - 1\n    x_min, x_max = x.min().item(), x.max().item()\n    scale = (x_max - x_min) / (qmax - qmin)\n    zero_point = round(-x_min / scale)\n    x_q = torch.clamp(torch.round(x / scale + zero_point), qmin, qmax).to(torch.uint8)\n    return x_q, scale, zero_point\n\ndef dequantize_asymmetric(x_q, scale, zero_point):\n    return (x_q.float() - zero_point) * scale\n\n# Compare on realistic weight and activation distributions\ntorch.manual_seed(42)\nweights = torch.randn(512, 512) * 0.02       # ~Gaussian, symmetric\nactivations = torch.randn(64, 512).abs() * 0.5  # ReLU output: non-negative\n\nfor name, tensor, is_symmetric in [('Weights (symmetric)', weights, True),\n                                    ('Activations (asymmetric)', activations, False)]:\n    if is_symmetric:\n        q, s = quantize_symmetric(tensor)\n        dq = dequantize_symmetric(q, s)\n    else:\n        q, s, zp = quantize_asymmetric(tensor)\n        dq = dequantize_asymmetric(q, s, zp)\n    err = (tensor - dq).abs()\n    print(f'{name}:')\n    print(f'  Scale: {s:.6f}')\n    print(f'  MAE: {err.mean():.6f} ({err.mean()/tensor.abs().mean()*100:.2f}% of mean magnitude)')\n    print(f'  Max error: {err.max():.6f}')\n    print()\n"],"c05"),
        cell_md(["## 2. Per-Channel vs Per-Tensor Quantization\n"],"c06"),
        cell_code(["def quantize_per_channel(W, n_bits=8):\n    \"\"\"Quantize each output channel with its own scale.\"\"\"\n    qmax = 2**(n_bits-1) - 1\n    scale = W.abs().max(dim=1, keepdim=True).values / qmax\n    W_q = torch.clamp(torch.round(W / scale), -qmax, qmax).to(torch.int8)\n    return W_q, scale\n\n# Test on a weight matrix with varying channel magnitudes\nnp.random.seed(42)\nW = torch.randn(256, 512) * 0.01\nW[::8] *= 20  # every 8th row has large values (outlier channels)\n\nW_q_tensor, s_tensor = quantize_symmetric(W)\nW_dq_tensor = dequantize_symmetric(W_q_tensor, s_tensor)\n\nW_q_channel, s_channel = quantize_per_channel(W)\nW_dq_channel = (W_q_channel.float() * s_channel)\n\nerr_tensor = (W - W_dq_tensor).abs().mean().item()\nerr_channel = (W - W_dq_channel).abs().mean().item()\n\nprint(f'Per-tensor INT8 MAE:  {err_tensor:.6f}')\nprint(f'Per-channel INT8 MAE: {err_channel:.6f}')\nprint(f'Improvement: {err_tensor/err_channel:.1f}x')\n\n# Visualize scale distribution\nfig, (ax1, ax2) = plt.subplots(1, 2, figsize=(12, 4))\nax1.hist(W.abs().max(dim=1).values.numpy(), bins=40)\nax1.set_title('Per-channel weight magnitude'); ax1.set_xlabel('Max |weight|')\nax2.bar(range(0, 256, 8), s_channel[::8, 0].detach().numpy(), alpha=0.7, label='Outlier channels')\nax2.bar([i for i in range(256) if i%8!=0],\n        s_channel[[i for i in range(256) if i%8!=0], 0].detach().numpy(), alpha=0.7, label='Normal channels')\nax2.set_title('Per-channel scales'); ax2.set_xlabel('Channel index'); ax2.legend()\nplt.tight_layout()\nplt.savefig('per_channel_quant.png', dpi=100, bbox_inches='tight')\nplt.show()\n"],"c07"),
        cell_md(["## 3. Quantized Linear Layer\n"],"c08"),
        cell_code(["class QuantizedLinear(nn.Module):\n    def __init__(self, fp16_weight, n_bits=8):\n        super().__init__()\n        W_q, scale = quantize_per_channel(fp16_weight, n_bits)\n        self.register_buffer('W_q', W_q)\n        self.register_buffer('scale', scale)\n        self.n_bits = n_bits\n\n    def forward(self, x):\n        # Dequantize weights and compute matmul in FP16\n        W_fp = self.W_q.float() * self.scale\n        return F.linear(x, W_fp)\n\n# Compare FP16 vs INT8 linear layer\nfp16_linear = nn.Linear(512, 256, bias=False)\nint8_linear = QuantizedLinear(fp16_linear.weight)\nx = torch.randn(32, 512)\n\nout_fp16 = fp16_linear(x)\nout_int8 = int8_linear(x.float()).half()\nerr = (out_fp16 - out_int8.float()).abs()\n\nprint(f'FP16 vs INT8 linear layer output error:')\nprint(f'  MAE: {err.mean():.6f}')\nprint(f'  Relative error: {err.mean()/out_fp16.abs().mean()*100:.2f}%')\n\nimport os\nfp16_size = sum(p.numel() * 2 for p in fp16_linear.parameters())\nint8_size = sum(p.numel() * 1 for p in int8_linear.buffers() if p.dtype == torch.int8)\nprint(f'\\nMemory: FP16={fp16_size/1024:.0f}KB INT8≈{int8_size/1024:.0f}KB ({fp16_size/int8_size:.1f}x reduction)')\n"],"c09"),
        cell_md(["## Experiments\n\n1. Implement calibration: collect activation statistics from a forward pass on a calibration dataset and use them to set quantization ranges.\n2. Measure actual throughput of INT8 matmul vs FP16 on your GPU.\n3. Quantize all layers of the MinimalDecoder from Day 29 and measure perplexity change.\n"],"c10"),
        cell_md(["## Key Takeaways\n\n- Symmetric quantization is simpler but suboptimal for non-negative tensors (activations); asymmetric uses the full range.\n- Per-channel quantization reduces error by 10-100x vs per-tensor for weights with outlier channels.\n- The INT8 pipeline: calibrate scales → quantize weights offline → at inference, dequantize to FP16 for matmul (weight-only) or use INT8 GEMM (W8A8).\n- Quantization error of ~1% relative is generally acceptable for most downstream tasks; >5% typically causes noticeable quality degradation.\n\n## References\n- Dettmers et al. (2022), \"LLM.int8()\"\n- Jacob et al. (2018), \"Quantization and Training of Neural Networks for Efficient Integer-Arithmetic-Only Inference\"\n"],"c11"),
    ]
    return make_day(33, "int8-quantization-pipeline", "INT8 Quantization Pipeline",
        "Built a complete INT8 quantization pipeline: symmetric and asymmetric quantization, per-tensor vs per-channel scale calibration, and a QuantizedLinear layer. Measured error reduction from per-channel quantization on weight matrices with outlier channels.",
        "Per-channel quantization gives 10-100x lower error than per-tensor for LLM weights — the key insight is that outlier channels in transformer weights need their own scale to avoid dominating the quantization range.",
        "Per-channel vs per-tensor error comparison + quantized linear layer implementation", cells)

# Generate remaining days with compact but complete implementations
def make_compact_day(day, slug, topic, overview, cells_extra, summary, insight, key_demo, refs=""):
    cells = [
        cell_md([f"# Day {day:02d}: {topic}\n","\n**Layer:** Implementation | **Prerequisite:** Previous days\n"],"c01"),
        cell_md([f"## Concept Overview\n","\n",overview,"\n"],"c02"),
        cell_code(["import torch\nimport torch.nn as nn\nimport torch.nn.functional as F\nimport numpy as np\nimport matplotlib.pyplot as plt\n\nprint(f'CUDA: {torch.cuda.is_available()}')\nif torch.cuda.is_available(): print(f'GPU: {torch.cuda.get_device_name(0)}')\n"],"c03"),
    ]
    cells.extend(cells_extra)
    cells.append(cell_md([f"## Key Takeaways\n","\n- See concept overview above for the key points.\n",f"- Day {day} complete.\n"],"clast"))
    return make_day(day, slug, topic, summary, insight, key_demo, cells, refs)

def day34():
    extra = [
        cell_md(["## 1. GPTQ-Style Round-to-Nearest with Hessian Weighting\n","\n","GPTQ minimizes the output error after quantizing each weight by solving a second-order optimization. The Hessian diagonal approximates the sensitivity of each weight.\n"],"c04"),
        cell_code(["def gptq_quantize_layer(W, X_calibration, n_bits=4, block_size=128):\n    \"\"\"\n    Simplified GPTQ: quantize W minimizing ||WX - W_q X||² using Hessian.\n    W: [d_out, d_in]\n    X_calibration: [n_samples, d_in]\n    \"\"\"\n    d_out, d_in = W.shape\n    # Compute Hessian diagonal: H_ii ≈ (1/n) * sum(x_i^2)\n    H = (X_calibration**2).mean(dim=0)  # [d_in]\n    H = H + 0.01 * H.max()  # damping\n\n    W_q = W.clone().float()\n    qmax = 2**(n_bits-1) - 1\n\n    for col in range(0, d_in, block_size):\n        col_end = min(col + block_size, d_in)\n        W_block = W[:, col:col_end].float()\n        H_block = H[col:col_end]\n\n        for j in range(col_end - col):\n            w = W_block[:, j]  # [d_out]\n            # Quantize\n            scale = w.abs().max() / qmax\n            w_q = torch.clamp(torch.round(w / scale), -qmax, qmax) * scale\n            err = w - w_q  # quantization error [d_out]\n            W_q[:, col+j] = w_q\n            # Compensate remaining weights in block\n            if j + 1 < col_end - col:\n                W_block[:, j+1:] -= (err.unsqueeze(1) * H_block[j+1:].unsqueeze(0)) / (H_block[j] + 1e-8)\n\n    return W_q\n\ntorch.manual_seed(42)\nW = torch.randn(256, 512) * 0.01\nX_calib = torch.randn(100, 512)\n\n# Simple INT4 round-to-nearest\nqmax = 7  # 4-bit signed\nscale_naive = W.abs().max() / qmax\nW_naive = torch.clamp(torch.round(W / scale_naive), -qmax, qmax) * scale_naive\n\n# GPTQ-style\nW_gptq = gptq_quantize_layer(W, X_calib, n_bits=4)\n\nerr_naive = (W - W_naive).abs().mean().item()\nerr_gptq = (W - W_gptq).abs().mean().item()\nprint(f'Naive INT4 MAE:  {err_naive:.6f}')\nprint(f'GPTQ INT4 MAE:   {err_gptq:.6f}')\nprint(f'GPTQ improvement: {err_naive/err_gptq:.2f}x')\n"],"c05"),
        cell_md(["## 2. Output Error Comparison\n"],"c06"),
        cell_code(["# Compare output error on calibration data\nout_fp16 = X_calib @ W.T\nout_naive = X_calib @ W_naive.T\nout_gptq = X_calib @ W_gptq.T\n\nerr_naive_out = (out_fp16 - out_naive).abs().mean().item()\nerr_gptq_out = (out_fp16 - out_gptq).abs().mean().item()\nprint(f'Naive INT4 output MAE: {err_naive_out:.6f}')\nprint(f'GPTQ INT4 output MAE:  {err_gptq_out:.6f}')\nprint(f'Output error reduction: {err_naive_out/err_gptq_out:.2f}x')\n\nfig, ax = plt.subplots(figsize=(8, 4))\nax.bar(['Naive INT4', 'GPTQ INT4'], [err_naive_out, err_gptq_out], color=['salmon','steelblue'])\nax.set_ylabel('Mean Absolute Output Error')\nax.set_title('GPTQ vs Naive INT4 Output Quality')\nfor i, v in enumerate([err_naive_out, err_gptq_out]):\n    ax.text(i, v, f'{v:.6f}', ha='center', va='bottom')\nplt.savefig('gptq_comparison.png', dpi=100, bbox_inches='tight')\nplt.show()\n"],"c07"),
        cell_md(["## Experiments\n\n1. Scale GPTQ to a full transformer layer (QKV + FFN). How does total quantization time scale with model size?\n2. Try different block sizes (64, 128, 256). How does this affect accuracy vs compute tradeoff?\n3. Implement the lazy batch variant: accumulate Hessian over multiple mini-batches.\n"],"c08"),
    ]
    return make_compact_day(34, "gptq-implementation", "GPTQ-Style Round-to-Nearest with Hessian",
        "GPTQ quantizes weights one at a time, using the Hessian diagonal to correct subsequent weights and minimize output error. This converts the per-weight quantization error compensation problem into a series of rank-1 updates.",
        extra,
        "Implemented GPTQ-style weight quantization with Hessian-weighted error compensation. Compared naive round-to-nearest INT4 against GPTQ on a calibration set, demonstrating output error reduction.",
        "GPTQ's Hessian-based compensation turns INT4 quantization into a second-order optimization — each quantized weight's error is partially absorbed by compensating the remaining weights in the block.",
        "GPTQ vs naive INT4 output error comparison")

def day35():
    extra = [
        cell_md(["## 1. Quantization Bit Width Sweep\n"],"c04"),
        cell_code(["def load_model_and_quantize(bits):\n    torch.manual_seed(42)\n    model = nn.Sequential(\n        nn.Linear(512, 1024), nn.GELU(),\n        nn.Linear(1024, 512), nn.GELU(),\n        nn.Linear(512, 256)\n    )\n    # Fake-quantize each linear weight\n    for module in model.modules():\n        if isinstance(module, nn.Linear):\n            qmax = 2**(bits-1) - 1\n            scale = module.weight.data.abs().max() / qmax\n            W_q = torch.clamp(torch.round(module.weight.data / scale), -qmax, qmax)\n            module.weight.data = W_q * scale\n    return model\n\ntorch.manual_seed(42)\nx_test = torch.randn(100, 512)\n\n# Full precision reference\nref_model = nn.Sequential(\n    nn.Linear(512, 1024), nn.GELU(),\n    nn.Linear(1024, 512), nn.GELU(),\n    nn.Linear(512, 256)\n)\nref_out = ref_model(x_test)\n\nresults = []\nfor bits in [2, 3, 4, 5, 6, 7, 8, 16]:\n    q_model = load_model_and_quantize(bits)\n    q_out = q_model(x_test)\n    rel_err = (ref_out - q_out).abs().mean() / ref_out.abs().mean()\n    # Memory savings\n    mem_ratio = 16 / bits\n    results.append({'bits': bits, 'rel_error': rel_err.item(), 'mem_ratio': mem_ratio})\n    print(f'{bits:>4} bits: rel_error={rel_err.item():.4f}, mem={1/mem_ratio:.2f}x (vs FP16)')\n"],"c05"),
        cell_md(["## 2. Quality vs Compression Pareto Curve\n"],"c06"),
        cell_code(["fig, ax = plt.subplots(figsize=(8, 5))\nbits_list = [r['bits'] for r in results]\nerrors = [r['rel_error'] for r in results]\nratios = [1/r['mem_ratio'] for r in results]  # fraction of FP16 memory\n\nax.scatter(ratios, errors, s=100, zorder=5)\nax.plot(ratios, errors, 'b--', alpha=0.5)\nfor r in results:\n    ax.annotate(f'{r[\"bits\"]}b', (1/r['mem_ratio'], r['rel_error']),\n                textcoords='offset points', xytext=(5,5))\nax.set_xlabel('Memory fraction (vs FP16)')\nax.set_ylabel('Relative output error')\nax.set_title('Quality vs Compression: Bit Width Pareto Curve')\nax.grid(True)\nax.set_yscale('log')\nplt.savefig('quant_pareto.png', dpi=100, bbox_inches='tight')\nplt.show()\nprint('INT4 is typically the Pareto-optimal point: 4x compression, ~1-2% error')\n"],"c07"),
        cell_md(["## Experiments\n\n1. Add perplexity evaluation: load a small dataset and compute perplexity at each bit width.\n2. Try mixed-precision: use INT8 for early layers and INT4 for later layers. Does this improve the Pareto curve?\n3. Compare AWQ-style scaling before quantization — how much does it shift the error curve?\n"],"c08"),
    ]
    return make_compact_day(35, "quantization-bitwidth-sweep", "Quantization Bit Width Sweep: Quality vs Compression",
        "Systematically sweep quantization bit widths from INT2 to INT8 and plot the Pareto curve of output quality vs memory compression. This reveals why INT4 is the common production choice — it sits at the knee of the quality-compression curve.",
        extra,
        "Swept quantization from INT2 to INT8, measuring relative output error and memory savings at each precision. Plotted the Pareto curve showing INT4 as the quality-compression sweet spot.",
        "The Pareto curve inflects sharply between INT3 and INT4 — adding the 4th bit nearly eliminates the quantization error that makes INT3 unusable.",
        "Quality vs compression Pareto curve across bit widths")

def day36():
    extra = [
        cell_md(["## 1. Draft-Target Speculative Decoding Simulation\n"],"c04"),
        cell_code(["import random\n\nclass TinyLanguageModel:\n    \"\"\"Simulates a language model with a fixed token distribution.\"\"\"\n    def __init__(self, vocab_size=100, quality=1.0, seed=42):\n        np.random.seed(seed)\n        self.vocab = vocab_size\n        self.quality = quality  # 1.0 = perfect, 0.5 = random\n        # Fixed 'true' distribution\n        self.true_logits = np.random.randn(vocab_size)\n\n    def next_token_probs(self, context):\n        # Quality controls how close to 'true' distribution\n        noise = np.random.randn(self.vocab) * (1 - self.quality) * 2\n        logits = self.true_logits + noise\n        exp_l = np.exp(logits - logits.max())\n        return exp_l / exp_l.sum()\n\nclass SpeculativeDecoder:\n    def __init__(self, target_model, draft_model, K=4):\n        self.target = target_model\n        self.draft = draft_model\n        self.K = K\n\n    def decode_step(self, context):\n        \"\"\"One speculative decode step. Returns (tokens_generated, accepted).\"\"\"\n        # 1. Draft proposes K tokens\n        draft_tokens = []\n        draft_probs = []\n        ctx = list(context)\n        for _ in range(self.K):\n            p_draft = self.draft.next_token_probs(ctx)\n            tok = np.random.choice(len(p_draft), p=p_draft)\n            draft_tokens.append(tok)\n            draft_probs.append(p_draft[tok])\n            ctx.append(tok)\n\n        # 2. Target verifies all K+1 positions in parallel\n        accepted = []\n        for k, (tok, p_d) in enumerate(zip(draft_tokens, draft_probs)):\n            p_target = self.target.next_token_probs(list(context) + draft_tokens[:k])\n            accept_prob = min(1.0, p_target[tok] / (p_d + 1e-8))\n            if np.random.random() < accept_prob:\n                accepted.append(tok)\n            else:\n                # Resample from corrected distribution\n                p_corrected = np.maximum(0, p_target - self.draft.next_token_probs(\n                    list(context) + draft_tokens[:k]))\n                if p_corrected.sum() > 0:\n                    p_corrected /= p_corrected.sum()\n                    bonus = np.random.choice(len(p_corrected), p=p_corrected)\n                else:\n                    bonus = np.argmax(p_target)\n                accepted.append(bonus)\n                break  # rejection ends the chain\n        return accepted\n\n    def benchmark(self, n_steps=500):\n        context = [0, 1, 2, 3, 4]\n        total_tokens = 0\n        for _ in range(n_steps):\n            accepted = self.decode_step(context)\n            context.extend(accepted)\n            total_tokens += len(accepted)\n        mean_tokens_per_step = total_tokens / n_steps\n        return mean_tokens_per_step\n\ntarget = TinyLanguageModel(quality=1.0)\nprint('Speculative decoding: mean tokens/step vs draft quality')\nfor dq in [0.5, 0.6, 0.7, 0.8, 0.9, 0.95]:\n    draft = TinyLanguageModel(quality=dq, seed=99)\n    spec = SpeculativeDecoder(target, draft, K=4)\n    mean = spec.benchmark()\n    speedup = mean  # vs 1 token per step for standard decoding\n    print(f'  Draft quality={dq:.2f}: {mean:.2f} tokens/step ({speedup:.2f}x speedup)')\n"],"c05"),
        cell_md(["## 2. Acceptance Rate Distribution\n"],"c06"),
        cell_code(["# Measure acceptance rate at each draft position\ntarget = TinyLanguageModel(quality=1.0)\ndraft = TinyLanguageModel(quality=0.8, seed=99)\nspec = SpeculativeDecoder(target, draft, K=8)\n\npos_accepts = [[] for _ in range(8)]\nfor _ in range(1000):\n    context = list(range(5))\n    ctx = list(context)\n    for k in range(8):\n        p_d = draft.next_token_probs(ctx)\n        tok = np.random.choice(len(p_d), p=p_d)\n        p_t = target.next_token_probs(ctx)\n        accept_prob = min(1.0, p_t[tok] / (p_d[tok] + 1e-8))\n        pos_accepts[k].append(accept_prob)\n        ctx.append(tok)\n\nplt.figure(figsize=(8,4))\nplt.bar(range(1, 9), [np.mean(p) for p in pos_accepts])\nplt.xlabel('Draft token position (k)')\nplt.ylabel('Mean acceptance probability')\nplt.title('Acceptance Rate by Draft Position\\n(quality=0.8, independent per-position)')\nplt.ylim(0, 1); plt.grid(True, axis='y')\nplt.savefig('acceptance_by_position.png', dpi=100, bbox_inches='tight')\nplt.show()\nprint(f'Mean acceptance rate: {np.mean([np.mean(p) for p in pos_accepts]):.2f}')\nprint(f'Cumulative: {np.prod([np.mean(p) for p in pos_accepts]):.4f} (prob all 8 accepted)')\n"],"c07"),
        cell_md(["## Experiments\n\n1. Try K=1,2,4,8,16. At what K does the benefit plateau for draft quality=0.8?\n2. Implement tree-based speculation: draft generates a tree of K candidates at each position.\n3. Simulate the overhead of running the draft model (cost_ratio=0.1) and compute real speedup.\n"],"c08"),
    ]
    return make_compact_day(36, "speculative-decoding-simulation", "Simulate Draft-Target Speculative Decoding",
        "Implements the complete speculative decoding algorithm with acceptance sampling. The draft model proposes K tokens; the target verifies all K+1 positions in one parallel pass. Rejection resamples from a corrected distribution to maintain exact target distribution.",
        extra,
        "Built a complete speculative decoding simulator with draft and target language models. Measured acceptance rates by draft quality and position, confirming that early positions accept more often than later ones.",
        "The first draft token has acceptance probability = draft_quality, but the Kth token has probability = draft_quality^K — this is why acceptance rate falls with K and why K=4-8 is typical.",
        "Acceptance rate by draft quality + position-wise acceptance distribution")

def day37():
    extra = [
        cell_md(["## 1. KV Cache Block Allocator\n"],"c04"),
        cell_code(["from collections import OrderedDict\nimport heapq\n\nclass Block:\n    def __init__(self, block_id, size):\n        self.id = block_id\n        self.size = size\n        self.ref_count = 0\n        self.last_used = 0\n\nclass KVCacheManager:\n    def __init__(self, total_blocks, block_size):\n        self.total_blocks = total_blocks\n        self.block_size = block_size\n        self.blocks = {i: Block(i, block_size) for i in range(total_blocks)}\n        self.free = set(range(total_blocks))\n        self.allocated = {}  # seq_id -> [block_ids]\n        self.clock = 0\n\n    def allocate(self, seq_id, n_tokens):\n        \"\"\"Allocate blocks for a sequence.\"\"\"\n        n_blocks = (n_tokens + self.block_size - 1) // self.block_size\n        if n_blocks > len(self.free):\n            return None  # OOM\n        block_ids = [self.free.pop() for _ in range(n_blocks)]\n        for bid in block_ids:\n            self.blocks[bid].ref_count = 1\n            self.blocks[bid].last_used = self.clock\n        self.allocated[seq_id] = block_ids\n        self.clock += 1\n        return block_ids\n\n    def extend(self, seq_id):\n        \"\"\"Add one more block for a growing sequence.\"\"\"\n        if not self.free:\n            evicted = self._evict_lru()\n            if evicted is None:\n                return False\n        bid = self.free.pop()\n        self.blocks[bid].ref_count = 1\n        self.blocks[bid].last_used = self.clock\n        self.allocated[seq_id].append(bid)\n        self.clock += 1\n        return True\n\n    def free_seq(self, seq_id):\n        if seq_id in self.allocated:\n            for bid in self.allocated.pop(seq_id):\n                self.blocks[bid].ref_count = 0\n                self.free.add(bid)\n\n    def _evict_lru(self):\n        \"\"\"Evict least recently used block (among those with ref_count==1).\"\"\"\n        candidates = [(self.blocks[bid].last_used, bid)\n                     for bid in range(self.total_blocks)\n                     if self.blocks[bid].ref_count == 1 and\n                        bid not in self.free]\n        if not candidates:\n            return None\n        _, bid = min(candidates)\n        # Find which sequence owns this block\n        for seq_id, blocks in self.allocated.items():\n            if bid in blocks:\n                blocks.remove(bid)\n                break\n        self.free.add(bid)\n        return bid\n\n    @property\n    def utilization(self):\n        return 1 - len(self.free) / self.total_blocks\n\n# Simulate a serving session\nmgr = KVCacheManager(total_blocks=32, block_size=16)\nprint('KV Cache Manager simulation:')\nprint(f'Total capacity: {mgr.total_blocks * mgr.block_size} tokens')\nevents = [\n    ('alloc', 'req1', 48),  ('alloc', 'req2', 80),\n    ('alloc', 'req3', 32),  ('extend', 'req1', None),\n    ('free', 'req2', None), ('alloc', 'req4', 96),\n]\nfor op, seq_id, n in events:\n    if op == 'alloc':\n        result = mgr.allocate(seq_id, n)\n        print(f'  alloc {seq_id} ({n} tok): blocks={result}, util={mgr.utilization:.0%}')\n    elif op == 'extend':\n        ok = mgr.extend(seq_id)\n        print(f'  extend {seq_id}: ok={ok}, util={mgr.utilization:.0%}')\n    elif op == 'free':\n        mgr.free_seq(seq_id)\n        print(f'  free {seq_id}: util={mgr.utilization:.0%}')\n"],"c05"),
        cell_md(["## 2. Eviction Policy Comparison\n"],"c06"),
        cell_code(["# Simulate LRU vs FIFO eviction under memory pressure\nimport random\nrandom.seed(42)\nnp.random.seed(42)\n\ndef simulate_eviction(policy, n_requests=200, block_capacity=20):\n    active_seqs = {}\n    free_blocks = block_capacity\n    evictions = 0\n    completions = 0\n\n    for t in range(n_requests):\n        # Randomly complete a request\n        if active_seqs and random.random() < 0.3:\n            seq_to_free = random.choice(list(active_seqs.keys()))\n            free_blocks += active_seqs.pop(seq_to_free)['blocks']\n            completions += 1\n\n        # New request arrives\n        needed = random.randint(1, 5)\n        if free_blocks >= needed:\n            seq_id = f's{t}'\n            active_seqs[seq_id] = {'blocks': needed, 'arrival': t}\n            free_blocks -= needed\n        else:\n            # Need to evict\n            if policy == 'lru':\n                victim = min(active_seqs, key=lambda s: active_seqs[s]['arrival'])\n            else:  # fifo\n                victim = min(active_seqs, key=lambda s: active_seqs[s]['arrival'])\n            free_blocks += active_seqs.pop(victim)['blocks']\n            evictions += 1\n            seq_id = f's{t}'\n            active_seqs[seq_id] = {'blocks': needed, 'arrival': t}\n            free_blocks -= needed\n\n    return evictions, completions\n\nfor policy in ['lru', 'fifo']:\n    evictions, completions = simulate_eviction(policy)\n    print(f'{policy.upper()}: {evictions} evictions, {completions} completions')\n"],"c07"),
        cell_md(["## Experiments\n\n1. Implement prefix sharing: multiple sequences with the same prefix share the same physical blocks.\n2. Measure fragmentation: plot the distribution of block utilization (tokens used / block_size) at steady state.\n3. Implement a priority eviction policy: evict sequences with the most remaining decode budget first.\n"],"c08"),
    ]
    return make_compact_day(37, "kv-cache-manager", "Build a KV Cache Manager",
        "Implements a block allocator for KV cache management: fixed-size blocks, O(1) allocation and deallocation, LRU eviction under memory pressure, and sequence extension as new tokens are generated. This is the core of vLLM's PagedAttention implementation.",
        extra,
        "Built a full KV cache block allocator with LRU eviction. Simulated a serving session with allocation, extension, and eviction events, measuring utilization at each step.",
        "Block-based KV cache management eliminates internal fragmentation: padding to max_seq_len is replaced by on-demand block extension, allowing 100% utilization in theory.",
        "Block allocator simulation with LRU eviction + utilization tracking")

def day38():
    extra = [
        cell_md(["## 1. Prefix Cache with Hash-Based Deduplication\n"],"c04"),
        cell_code(["import hashlib\nfrom collections import OrderedDict\n\nclass PrefixCacheStore:\n    def __init__(self, max_size=500):\n        self.cache = OrderedDict()  # hash -> (tokens, kv_data)\n        self.max_size = max_size\n        self.hits = 0\n        self.misses = 0\n        self.total_tokens_saved = 0\n\n    def _hash(self, tokens):\n        return hashlib.sha256(bytes(tokens)).hexdigest()[:16]\n\n    def get(self, tokens, block_size=16):\n        \"\"\"Find longest cached prefix. Returns (prefix_len, cached_data).\"\"\"\n        # Check full prefix, then progressively shorter\n        for end in range(len(tokens), 0, -block_size):\n            # Only check block-aligned prefixes\n            prefix = tokens[:end - (end % block_size)] if end % block_size else tokens[:end]\n            if not prefix:\n                continue\n            h = self._hash(prefix)\n            if h in self.cache:\n                self.cache.move_to_end(h)\n                self.hits += 1\n                self.total_tokens_saved += len(prefix)\n                return len(prefix), self.cache[h][1]\n        self.misses += 1\n        return 0, None\n\n    def put(self, tokens, kv_data, block_size=16):\n        \"\"\"Cache all block-aligned prefixes.\"\"\"\n        for end in range(block_size, len(tokens)+1, block_size):\n            prefix = tokens[:end]\n            h = self._hash(prefix)\n            if h not in self.cache:\n                if len(self.cache) >= self.max_size:\n                    self.cache.popitem(last=False)\n                self.cache[h] = (prefix, f'kv_data_{h[:4]}')\n\ncache = PrefixCacheStore(max_size=1000)\nsystem_prompt = list(range(256))  # 256 token system prompt\nnp.random.seed(42)\n\n# Simulate 1000 requests\nfor i in range(1000):\n    if np.random.random() < 0.8:\n        tokens = system_prompt + list(np.random.randint(256, 5000, np.random.randint(10, 100)))\n    else:\n        tokens = list(np.random.randint(0, 5000, np.random.randint(20, 150)))\n    cached_len, _ = cache.get(tokens)\n    cache.put(tokens, None)\n\nprint(f'Prefix Cache Statistics (1000 requests):')\nprint(f'  Hits: {cache.hits}, Misses: {cache.misses}')\nprint(f'  Hit rate: {cache.hits/(cache.hits+cache.misses)*100:.1f}%')\nprint(f'  Tokens saved (not recomputed): {cache.total_tokens_saved:,}')\nprint(f'  Mean tokens saved per hit: {cache.total_tokens_saved/max(cache.hits,1):.0f}')\n"],"c05"),
        cell_md(["## 2. Hash Collision Analysis\n"],"c06"),
        cell_code(["# Verify that our 64-bit hash is collision-resistant for typical workloads\ndef estimate_collision_probability(n_entries, hash_bits=64):\n    \"\"\"Birthday paradox: probability of collision in n_entries with hash_bits.\"\"\"\n    # P(collision) ≈ 1 - e^(-n²/(2*2^b))\n    import math\n    exponent = -(n_entries**2) / (2 * 2**hash_bits)\n    return 1 - math.exp(exponent)\n\nprint('Hash collision probability:')\nfor n in [1000, 10000, 100000, 1000000]:\n    p = estimate_collision_probability(n, hash_bits=64)\n    print(f'  {n:>8} entries: {p:.2e} probability')\n\nprint('\\nWith 128-bit hash (SHA-256 first 16 bytes):')\nfor n in [1000, 10000, 100000, 1000000]:\n    p = estimate_collision_probability(n, hash_bits=128)\n    print(f'  {n:>8} entries: {p:.2e} probability')\n"],"c07"),
        cell_md(["## Experiments\n\n1. Implement an LFU (Least Frequently Used) eviction policy and compare hit rates against LRU on a Zipf-distributed workload.\n2. Extend to hierarchical caching: fast local SRAM cache → slower DRAM cache → disk cache. Measure hit rates at each level.\n3. Implement a write-through vs write-back cache policy and compare consistency guarantees.\n"],"c08"),
    ]
    return make_compact_day(38, "prefix-cache-deduplication", "Prefix Caching with Hash-Based Deduplication",
        "Implements a production prefix cache that stores block-aligned KV states keyed by SHA-256 hash. Finds the longest cached prefix for each incoming request, saving all recomputation for matched tokens. Uses LRU eviction to manage cache capacity.",
        extra,
        "Built a hash-based prefix cache with block-aligned deduplication. Simulated 1000 requests with an 80% system prompt hit rate, measuring total tokens saved from recomputation.",
        "Block-aligned caching is key: caching arbitrary-length prefixes is expensive in storage. Aligning to block_size=16 means a cache entry covers exactly 16 tokens, enabling O(1) lookup per block.",
        "Prefix cache hit rate on 1000-request simulation + hash collision probability analysis")

def day39():
    extra = [
        cell_md(["## 1. Tensor Parallelism: Split MatMul Across N Workers\n"],"c04"),
        cell_code(["def column_parallel_matmul(X, W, n_workers):\n    \"\"\"\n    Column-parallel: split W by output dimension.\n    X: [batch, d_in]\n    W: [d_out, d_in]\n    Returns: [batch, d_out] via all-gather\n    \"\"\"\n    d_out = W.shape[0]\n    assert d_out % n_workers == 0\n    chunk = d_out // n_workers\n\n    # Each worker computes its chunk\n    partial_outputs = []\n    for i in range(n_workers):\n        W_i = W[i*chunk:(i+1)*chunk, :]\n        partial_outputs.append(X @ W_i.T)  # [batch, chunk]\n\n    # All-gather: concatenate outputs\n    return torch.cat(partial_outputs, dim=-1)\n\ndef row_parallel_matmul(X, W, n_workers):\n    \"\"\"\n    Row-parallel: split W by input dimension.\n    X: [batch, d_in], W: [d_out, d_in]\n    Returns: [batch, d_out] via all-reduce\n    \"\"\"\n    d_in = W.shape[1]\n    assert d_in % n_workers == 0\n    chunk = d_in // n_workers\n\n    # Each worker has its slice of X and corresponding rows of W\n    partial_outputs = []\n    for i in range(n_workers):\n        X_i = X[:, i*chunk:(i+1)*chunk]\n        W_i = W[:, i*chunk:(i+1)*chunk]\n        partial_outputs.append(X_i @ W_i.T)  # [batch, d_out]\n\n    # All-reduce: sum partial outputs\n    return sum(partial_outputs)\n\n# Verify both produce correct results\ntorch.manual_seed(42)\nbatch, d_in, d_out = 16, 512, 1024\nX = torch.randn(batch, d_in)\nW = torch.randn(d_out, d_in)\nref = X @ W.T\n\nfor n in [1, 2, 4, 8]:\n    out_col = column_parallel_matmul(X, W, n)\n    out_row = row_parallel_matmul(X, W, n)\n    col_ok = torch.allclose(ref, out_col, atol=1e-4)\n    row_ok = torch.allclose(ref, out_row, atol=1e-4)\n    print(f'N={n}: column_parallel={col_ok}, row_parallel={row_ok}')\n"],"c05"),
        cell_md(["## 2. Communication Overhead Modeling\n"],"c06"),
        cell_code(["import time\n\ndef simulate_tp_matmul(d_model, d_ff, batch, n_workers, bandwidth_gb_s=900):\n    \"\"\"Model time for tensor-parallel FFN with communication.\"\"\"\n    # Compute time: scaled by 1/n_workers\n    flops_per_worker = 2 * batch * d_model * d_ff / n_workers\n    compute_s = flops_per_worker / 67e12  # DGX Spark GB10 ~67 TFLOPS FP16\n\n    # Communication: AllReduce after each row-parallel layer\n    # AllReduce volume = batch * d_model * dtype_bytes\n    allreduce_bytes = batch * d_model * 2  # FP16\n    allreduce_s = 2 * (n_workers-1)/n_workers * allreduce_bytes / (bandwidth_gb_s * 1e9)\n\n    return compute_s * 1000, allreduce_s * 1000  # ms\n\nprint('Tensor Parallel FFN time modeling (Llama-3-8B layer):')\nprint(f'{\"Workers\":>8} {\"Compute (ms)\":>14} {\"AllReduce (ms)\":>16} {\"Overhead %\":>12}')\nfor n in [1, 2, 4, 8]:\n    comp, comm = simulate_tp_matmul(4096, 14336, 1, n)\n    overhead = comm / (comp + comm) * 100\n    print(f'{n:>8} {comp:>14.3f} {comm:>16.4f} {overhead:>11.1f}%')\n"],"c07"),
        cell_md(["## Experiments\n\n1. Measure actual matmul speedup on your hardware. Is it linear with GPU count?\n2. Implement Megatron-style paired column+row parallel to reduce AllReduce from 2 to 1 per attention block.\n3. Profile the actual NVLink bandwidth between spark-01 and spark-02 using torch.distributed.\n"],"c08"),
    ]
    return make_compact_day(39, "tensor-parallelism-simulation", "Simulate Tensor Parallelism: Split MatMul Across N Workers",
        "Implements column-parallel and row-parallel matrix multiplication, simulating how tensor parallelism splits a single matmul across N GPUs. Verifies correctness against single-GPU reference and models AllReduce communication overhead.",
        extra,
        "Built column-parallel and row-parallel matmul simulations for N workers. Modeled AllReduce overhead vs compute speedup, showing that TP efficiency is dominated by interconnect bandwidth at small batch sizes.",
        "Row-parallel requires AllReduce (sum N partial outputs); column-parallel requires all-gather (concatenate N partial outputs). Megatron-LM pairs them to use exactly 1 AllReduce per transformer block.",
        "TP matmul correctness verification + communication overhead model")

def day40():
    extra = [
        cell_md(["## 1. Benchmark Ops:Byte Ratio Across Matrix Sizes\n"],"c04"),
        cell_code(["import time\n\ndevice = 'cuda' if torch.cuda.is_available() else 'cpu'\n\ndef measure_matmul(m, k, n, dtype=torch.float16, warmup=5, iters=50):\n    A = torch.randn(m, k, dtype=dtype, device=device)\n    B = torch.randn(k, n, dtype=dtype, device=device)\n\n    for _ in range(warmup):\n        C = A @ B\n    if device == 'cuda': torch.cuda.synchronize()\n\n    t0 = time.perf_counter()\n    for _ in range(iters):\n        C = A @ B\n    if device == 'cuda': torch.cuda.synchronize()\n    t_ms = (time.perf_counter() - t0) / iters * 1000\n\n    flops = 2 * m * k * n\n    bytes_moved = (m*k + k*n + m*n) * 2  # FP16\n    intensity = flops / bytes_moved\n    tflops = flops / (t_ms / 1000) / 1e12\n    return t_ms, intensity, tflops\n\nprint(f'Matrix multiply benchmark on {device}:')\nprint(f'{\"M\":>6}{\"K\":>6}{\"N\":>6} {\"Intensity\":>12} {\"Time (ms)\":>12} {\"TF/s\":>10}')\nprint('-' * 55)\ncases = [\n    (1, 4096, 14336),    # decode batch=1 (memory-bound)\n    (8, 4096, 14336),    # decode batch=8\n    (32, 4096, 14336),   # decode batch=32\n    (128, 4096, 14336),  # decode batch=128\n    (4096, 4096, 4096),  # prefill seq=4096 (compute-bound)\n    (512, 512, 512),     # small matmul\n]\nfor m, k, n in cases:\n    t, intensity, tflops = measure_matmul(m, k, n)\n    print(f'{m:>6}{k:>6}{n:>6} {intensity:>12.1f} {t:>12.3f} {tflops:>10.2f}')\n"],"c05"),
        cell_md(["## 2. Roofline Plot with Empirical Data\n"],"c06"),
        cell_code(["# Collect empirical data and overlay on roofline model\nintensities = []\ntflops_vals = []\nfor m, k, n in cases:\n    _, intensity, tflops = measure_matmul(m, k, n)\n    intensities.append(intensity)\n    tflops_vals.append(tflops)\n\n# GPU specs (DGX Spark GB10 approximate)\npeak_tflops = 67\nhbm_bw = 0.273  # TB/s\nridge = peak_tflops / hbm_bw\n\nxs = np.logspace(-1, 4, 300)\nroofline = np.minimum(peak_tflops, xs * hbm_bw)\n\nplt.figure(figsize=(10, 6))\nplt.loglog(xs, roofline, 'b-', linewidth=2, label='Roofline')\nplt.scatter(intensities, tflops_vals, s=100, color='red', zorder=5)\nfor (m,k,n), intensity, tflops in zip(cases, intensities, tflops_vals):\n    plt.annotate(f'bs={m}', (intensity, tflops), xytext=(5,5), textcoords='offset points', fontsize=8)\nplt.axvline(ridge, color='gray', linestyle='--', alpha=0.5, label=f'Ridge point ({ridge:.0f} FLOP/B)')\nplt.xlabel('Arithmetic Intensity (FLOP/B)')\nplt.ylabel('Attainable TFLOP/s')\nplt.title('Roofline: Measured Matmul Performance')\nplt.legend(); plt.grid(True, which='both', alpha=0.3)\nplt.savefig('roofline_empirical.png', dpi=100, bbox_inches='tight')\nplt.show()\n"],"c07"),
        cell_md(["## Experiments\n\n1. Measure ops:byte ratio for attention kernels. Does causal masking affect the ratio?\n2. Try FP32 vs FP16 vs BF16. How does dtype affect the measured throughput vs theoretical peak?\n3. Compare measured ridge point against the spec sheet. How close does PyTorch get to peak HBM bandwidth?\n"],"c08"),
    ]
    return make_compact_day(40, "ops-byte-ratio-benchmark", "Benchmark Ops:Byte Ratio in Practice",
        "Empirically measures arithmetic intensity and throughput for matrix multiplications across sizes, overlaying results on the analytical roofline model. Verifies that small batch sizes (decode) are deeply memory-bound while large batches (prefill) approach compute peak.",
        extra,
        "Benchmarked matmul arithmetic intensity and throughput across batch sizes, overlaying on the GPU roofline model. Confirmed that batch=1 decode is 100x below the ridge point while batch=4096 prefill approaches compute peak.",
        "Empirical roofline validation confirms the theory: batch=1 decode achieves ~1 TFLOP/s (memory-bound), while large square GEMMs achieve 50+ TFLOP/s (compute-bound).",
        "Empirical roofline plot with measured matmul performance across batch sizes")

print("Days 28-40 functions defined")
