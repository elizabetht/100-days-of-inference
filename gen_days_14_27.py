#!/usr/bin/env python3
"""Generate days 14-27 notebooks."""
import json, os, sys
sys.path.insert(0, '/home/nvidia/src/github.com/elizabetht/100-days-of-inference')
from gen_notebooks import cell_md, cell_code, write_nb, write_readme, validate

def day14():
    cells = [
        cell_md(["# Day 14: KV Cache — Prefix Caching & Cache-Aware Routing\n","> *Inference Engineering* — Chapter 5.3 | Philip Kiely, Baseten Books 2026\n","\n**Layer:** Runtime | **Prerequisite:** Day 07 (vLLM/PagedAttention)\n"],"c01"),
        cell_md(["## Concept Overview\n","\n",
            "Prefix caching stores previously computed KV states for common prompt prefixes, allowing reuse across requests without recomputation. ",
            "A system prompt shared across 1000 concurrent users only needs to be computed once. ",
            "Cache-aware routing directs new requests to the GPU instance that already holds the relevant KV cache blocks, maximizing cache hit rates. ",
            "Together, these techniques convert O(prompt_len) compute per request into O(prompt_len / cache_hit_rate) amortized cost.\n"],"c02"),
        cell_code(["import torch\nimport numpy as np\nimport matplotlib.pyplot as plt\nimport hashlib\nfrom collections import OrderedDict\n\nprint(f'CUDA available: {torch.cuda.is_available()}')\nif torch.cuda.is_available():\n    print(f'GPU: {torch.cuda.get_device_name(0)}')\n"],"c03"),
        cell_md(["## 1. Hash-Based Prefix Cache\n","\n","Cache keys are computed from token sequence hashes. Block-level hashing (per 16 tokens) enables partial prefix matches.\n"],"c04"),
        cell_code(["class PrefixCache:\n    def __init__(self, max_blocks=1000, block_size=16):\n        self.cache = OrderedDict()  # hash -> kv_block (LRU)\n        self.max_blocks = max_blocks\n        self.block_size = block_size\n        self.hits = 0\n        self.misses = 0\n\n    def _block_hash(self, tokens):\n        return hashlib.sha256(bytes(tokens)).hexdigest()[:16]\n\n    def get(self, token_sequence):\n        \"\"\"Return (num_cached_tokens, list_of_block_ids).\"\"\"\n        blocks = []\n        for start in range(0, len(token_sequence), self.block_size):\n            block_tokens = token_sequence[start:start+self.block_size]\n            if len(block_tokens) < self.block_size:\n                break  # only cache complete blocks\n            h = self._block_hash(block_tokens)\n            if h in self.cache:\n                blocks.append(self.cache[h])\n                self.cache.move_to_end(h)  # LRU update\n                self.hits += 1\n            else:\n                self.misses += 1\n                break  # stop at first miss (prefix must be contiguous)\n        return len(blocks) * self.block_size, blocks\n\n    def put(self, token_sequence, block_data_fn):\n        \"\"\"Insert blocks for completed sequence.\"\"\"\n        for start in range(0, len(token_sequence), self.block_size):\n            block_tokens = token_sequence[start:start+self.block_size]\n            if len(block_tokens) < self.block_size:\n                break\n            h = self._block_hash(block_tokens)\n            if h not in self.cache:\n                if len(self.cache) >= self.max_blocks:\n                    self.cache.popitem(last=False)  # evict LRU\n                self.cache[h] = f'kv_block_{h}'\n\n# Simulate requests with a shared system prompt\ncache = PrefixCache(max_blocks=500)\nsystem_prompt = list(range(128))  # 128 token system prompt\nnp.random.seed(42)\n\nprint('Prefix cache simulation with shared system prompt (128 tokens):')\nsaved_tokens = 0\nfor i in range(100):\n    # 80% of requests share the system prompt; 20% are unique\n    if np.random.random() < 0.8:\n        tokens = system_prompt + list(np.random.randint(128, 1000, size=np.random.randint(10, 50)))\n    else:\n        tokens = list(np.random.randint(0, 1000, size=np.random.randint(10, 100)))\n    cached_len, _ = cache.get(tokens)\n    saved_tokens += cached_len\n    cache.put(tokens, lambda t: f'kv_{t}')\n\ntotal_tokens = sum(len(system_prompt) + 30 for _ in range(100))\nprint(f'Total requests: 100')\nprint(f'Cache hits: {cache.hits}, misses: {cache.misses}')\nprint(f'Tokens saved (not recomputed): {saved_tokens}')\nprint(f'Compute savings: {saved_tokens/total_tokens*100:.0f}%')\n"],"c05"),
        cell_md(["## 2. Cache-Aware Routing\n","\n","When multiple GPU instances hold copies of the KV cache, routing new requests to the instance with the highest prefix overlap minimizes total compute.\n"],"c06"),
        cell_code(["class CacheAwareRouter:\n    def __init__(self, num_workers):\n        self.workers = [PrefixCache(max_blocks=200) for _ in range(num_workers)]\n        self.num_workers = num_workers\n        self.load = [0] * num_workers\n\n    def route(self, token_sequence, max_load=20):\n        \"\"\"Route to worker with highest cache coverage (if load allows).\"\"\"\n        best_worker = 0\n        best_cached = 0\n\n        for i, worker in enumerate(self.workers):\n            if self.load[i] >= max_load:\n                continue\n            cached_len, _ = worker.get(token_sequence)\n            if cached_len > best_cached:\n                best_cached = cached_len\n                best_worker = i\n\n        self.load[best_worker] += 1\n        return best_worker, best_cached\n\n    def complete(self, worker_id, token_sequence):\n        self.load[worker_id] -= 1\n        self.workers[worker_id].put(token_sequence, None)\n\n# Simulate routing\nnp.random.seed(42)\nrouter = CacheAwareRouter(num_workers=4)\n\n# Seed each worker with different system prompts\nsystem_prompts = {\n    0: list(range(0, 128)),\n    1: list(range(128, 256)),\n    2: list(range(256, 384)),\n    3: list(range(384, 512)),\n}\nfor worker_id, sp in system_prompts.items():\n    router.workers[worker_id].put(sp, None)\n\nrouting_decisions = []\nfor i in range(200):\n    # Request uses one of the 4 system prompts\n    sp_idx = np.random.randint(0, 4)\n    tokens = system_prompts[sp_idx] + list(np.random.randint(512, 2000, size=30))\n    worker, cached = router.route(tokens)\n    routing_decisions.append({'correct_worker': sp_idx, 'routed_to': worker, 'cached_tokens': cached})\n    router.complete(worker, tokens)\n\ncorrect = sum(1 for d in routing_decisions if d['correct_worker'] == d['routed_to'])\nmean_cached = np.mean([d['cached_tokens'] for d in routing_decisions])\nprint(f'Cache-aware routing results:')\nprint(f'  Routed to optimal worker: {correct}/200 ({correct/2:.0f}%)')\nprint(f'  Mean cached tokens per request: {mean_cached:.0f}')\n"],"c07"),
        cell_md(["## 3. KV Cache Memory Sizing\n","\n","Calculating KV cache requirements for production deployments.\n"],"c08"),
        cell_code(["def kv_cache_size_gb(num_layers, num_kv_heads, d_head, max_seq_len, dtype_bytes=2):\n    \"\"\"KV cache memory per sequence in GB.\"\"\"\n    return 2 * num_layers * num_kv_heads * d_head * max_seq_len * dtype_bytes / 1e9\n\nmodels = [\n    ('Llama-3-8B',  32, 8,  128, 'GQA'),\n    ('Llama-3-70B', 80, 8,  128, 'GQA'),\n    ('Mistral-7B',  32, 8,  128, 'GQA'),\n    ('Qwen2.5-7B',  28, 4,  128, 'GQA'),\n]\nseq_lens = [4096, 8192, 32768, 131072]\n\nprint(f'{\"Model\":<20}', end='')\nfor sl in seq_lens:\n    print(f' {sl//1024}K seq (GB)', end='')\nprint()\nprint('-' * 80)\nfor name, layers, kv_heads, d_head, attn_type in models:\n    print(f'{name:<20}', end='')\n    for sl in seq_lens:\n        gb = kv_cache_size_gb(layers, kv_heads, d_head, sl)\n        print(f' {gb:>12.3f}', end='')\n    print()\n"],"c09"),
        cell_md(["## Experiments: Try These\n",
            "\n",
            "1. **Hit rate vs system prompt length**: Vary system prompt length (64 to 4096 tokens) and measure prefix cache hit rate for 1000 requests.\n",
            "2. **LRU vs LFU eviction**: Implement both LRU and LFU eviction policies. Which achieves higher hit rates on realistic workloads (Zipf prefix distribution)?\n",
            "3. **Consistent hashing for routing**: Replace the greedy router with consistent hashing (prefix hash → worker). How does it handle worker failures?\n"],"c10"),
        cell_md(["## Key Takeaways\n",
            "\n",
            "- Prefix caching stores completed KV blocks keyed by token hash; block-level hashing enables partial prefix matches and LRU eviction.\n",
            "- With an 80% hit rate on a 128-token system prompt, prefix caching saves >60% of prefill compute across all requests.\n",
            "- Cache-aware routing directs requests to the worker with the most matching KV blocks, maximizing hit rates under load.\n",
            "- GQA (Grouped Query Attention) dramatically reduces KV cache size vs MHA — Llama-3-8B uses 8 KV heads vs 32 query heads, 4x smaller KV cache.\n",
            "\n## References\n- *Inference Engineering* Ch 5.3 — Philip Kiely, Baseten Books 2026\n- vLLM prefix caching documentation\n- SGLang RadixAttention paper\n"],"c11"),
    ]
    p = write_nb(14, "kv-cache-prefix-caching", cells)
    write_readme(14,"KV Cache: Prefix Caching & Cache-Aware Routing","Runtime",
        "Built a hash-based prefix cache with LRU eviction and simulated cache-aware routing across 4 workers. Measured compute savings from shared system prompt caching and analyzed KV cache memory requirements for production model deployments.",
        "Cache-aware routing is essential: routing requests randomly across workers with prefix caches is almost as bad as no cache at all. Routing by prefix hash converts a cache from per-worker to effectively cluster-wide.",
        "kv-cache-prefix-caching","Prefix cache hit rate simulation + cache-aware routing accuracy",
        "- *Inference Engineering* Ch 5.3 (Philip Kiely, Baseten Books 2026)\n- vLLM prefix caching implementation")
    return validate(p)

def day15():
    cells = [
        cell_md(["# Day 15: Model Parallelism — Tensor & Expert\n","> *Inference Engineering* — Chapter 5.4 | Philip Kiely, Baseten Books 2026\n","\n**Layer:** Runtime | **Prerequisite:** Day 03 (MoE), Day 10 (Disaggregation)\n"],"c01"),
        cell_md(["## Concept Overview\n","\n",
            "Model parallelism splits a model across multiple GPUs when it doesn't fit on one device. ",
            "Tensor parallelism (TP) shards individual weight matrices across GPUs — each GPU holds 1/N of each layer's weights and computes a partial result, requiring an AllReduce after each layer. ",
            "Expert parallelism (EP) in MoE models places different experts on different GPUs — a token is routed to the GPU holding its expert via All-to-All communication. ",
            "These two strategies can be combined for the largest models.\n"],"c02"),
        cell_code(["import torch\nimport torch.nn as nn\nimport torch.nn.functional as F\nimport numpy as np\nimport matplotlib.pyplot as plt\n\nprint(f'CUDA available: {torch.cuda.is_available()}')\nif torch.cuda.is_available():\n    print(f'GPU count: {torch.cuda.device_count()}')\n"],"c03"),
        cell_md(["## 1. Tensor Parallelism — Column and Row Splitting\n","\n",
            "For a linear layer $Y = XW + b$:\n",
            "- **Column parallel:** Split $W$ by columns. Each GPU computes $Y_i = X W_i$. Results are concatenated: $Y = [Y_1, Y_2, \\ldots]$.\n",
            "- **Row parallel:** Split $W$ by rows, split $X$ by columns. Each GPU computes $Y_i = X_i W_i$. Results are summed: $Y = \\sum_i Y_i$ (AllReduce).\n",
            "\nMegatron-LM pairs column-parallel with row-parallel to minimize communication: one AllReduce per transformer layer instead of one per matmul.\n"],"c04"),
        cell_code(["class ColumnParallelLinear(nn.Module):\n    \"\"\"Simulate column-parallel linear layer (no actual multi-GPU).\"\"\"\n    def __init__(self, in_features, out_features, tp_size):\n        super().__init__()\n        assert out_features % tp_size == 0\n        self.tp_size = tp_size\n        self.out_per_rank = out_features // tp_size\n        # Each rank holds out_per_rank columns\n        self.weights = nn.ParameterList([\n            nn.Parameter(torch.randn(self.out_per_rank, in_features) * 0.01)\n            for _ in range(tp_size)\n        ])\n\n    def forward(self, x):\n        # Simulate each rank's partial computation\n        partial_outputs = [F.linear(x, w) for w in self.weights]\n        return torch.cat(partial_outputs, dim=-1)  # all-gather equivalent\n\nclass RowParallelLinear(nn.Module):\n    \"\"\"Simulate row-parallel linear layer.\"\"\"\n    def __init__(self, in_features, out_features, tp_size):\n        super().__init__()\n        assert in_features % tp_size == 0\n        self.tp_size = tp_size\n        self.in_per_rank = in_features // tp_size\n        self.weights = nn.ParameterList([\n            nn.Parameter(torch.randn(out_features, self.in_per_rank) * 0.01)\n            for _ in range(tp_size)\n        ])\n\n    def forward(self, x):\n        # Scatter input across ranks\n        x_parts = x.chunk(self.tp_size, dim=-1)\n        partial_outputs = [F.linear(xp, w) for xp, w in zip(x_parts, self.weights)]\n        return sum(partial_outputs)  # all-reduce equivalent\n\ntp_size = 4\ncol = ColumnParallelLinear(512, 2048, tp_size)\nrow = RowParallelLinear(2048, 512, tp_size)\nx = torch.randn(8, 32, 512)\n\nout_col = col(x)\nout_row = row(out_col)\nprint(f'Input:              {x.shape}')\nprint(f'Column parallel:    {out_col.shape} (each rank: {2048//tp_size} features)')\nprint(f'Row parallel:       {out_row.shape}')\nprint(f'Communication: 1 AllReduce after row-parallel (vs {tp_size} without Megatron pairing)')\n\n# Memory savings\ntotal_params = 512*2048 + 2048*512\nparams_per_rank = total_params // tp_size\nprint(f'\\nParams per rank: {params_per_rank:,} ({100/tp_size:.0f}% of total {total_params:,})')\n"],"c05"),
        cell_md(["## 2. Communication Cost Analysis\n","\n","AllReduce cost determines TP scalability. For ring AllReduce on N GPUs with D bytes:\n$t_{\\text{AllReduce}} \\approx 2 \\cdot \\frac{N-1}{N} \\cdot \\frac{D}{\\text{bandwidth}}$\n"],"c06"),
        cell_code(["def allreduce_time_ms(tensor_bytes, tp_size, bandwidth_gb_s):\n    \"\"\"Ring AllReduce time estimate.\"\"\"\n    return 2 * (tp_size - 1) / tp_size * tensor_bytes / (bandwidth_gb_s * 1e9) * 1000\n\n# For a Llama-3-70B forward pass\nd_model = 8192\nbatch_seq_tokens = 512  # batch=4, seq=128\ntensor_bytes = batch_seq_tokens * d_model * 2  # FP16\n\nprint('AllReduce overhead per transformer layer (Llama-3-70B, 512 tokens):')\nprint(f'{\"TP Size\":>8} {\"NVLink (900GB/s)\":>18} {\"InfiniBand (25GB/s)\":>22} {\"PCIe (32GB/s)\":>16}')\nfor tp in [1, 2, 4, 8]:\n    t_nvlink = allreduce_time_ms(tensor_bytes, tp, 900) if tp > 1 else 0\n    t_ib = allreduce_time_ms(tensor_bytes, tp, 25) if tp > 1 else 0\n    t_pcie = allreduce_time_ms(tensor_bytes, tp, 32) if tp > 1 else 0\n    print(f'{tp:>8} {t_nvlink:>18.3f} {t_ib:>22.3f} {t_pcie:>16.3f}')\n\nprint('\\nTP is efficient on NVLink (intra-node) but expensive on InfiniBand (inter-node)')\n"],"c07"),
        cell_md(["## 3. Expert Parallelism\n","\n","In MoE models, each GPU hosts a subset of experts. The All-to-All communication pattern: ",
            "tokens are routed to remote GPUs holding their target experts, computed there, and sent back.\n"],"c08"),
        cell_code(["def simulate_expert_parallel(num_experts, ep_size, num_tokens, top_k=2):\n    \"\"\"Simulate expert parallelism routing.\"\"\"\n    experts_per_rank = num_experts // ep_size\n    np.random.seed(42)\n\n    # Route tokens: each gets top-k experts\n    assignments = np.random.randint(0, num_experts, size=(num_tokens, top_k))\n    rank_assignments = assignments // experts_per_rank\n\n    # Count cross-rank traffic\n    local_tokens = 0\n    remote_tokens = 0\n    for token_idx in range(num_tokens):\n        home_rank = token_idx % ep_size  # simplified\n        for expert_rank in rank_assignments[token_idx]:\n            if expert_rank == home_rank:\n                local_tokens += 1\n            else:\n                remote_tokens += 1\n\n    a2a_bytes = remote_tokens * 4096 * 2  # tokens × hidden_dim × FP16\n    return local_tokens, remote_tokens, a2a_bytes\n\nprint('Expert Parallelism Communication Analysis (Mixtral-8x7B config):')\nprint(f'{\"EP Size\":>8} {\"Local Tokens\":>14} {\"Remote Tokens\":>15} {\"A2A Data (MB)\":>15}')\nfor ep_size in [1, 2, 4, 8]:\n    local, remote, a2a = simulate_expert_parallel(num_experts=8, ep_size=ep_size, num_tokens=512)\n    print(f'{ep_size:>8} {local:>14} {remote:>15} {a2a/1e6:>15.1f}')\n"],"c09"),
        cell_md(["## Experiments: Try These\n",
            "\n",
            "1. **TP scaling efficiency**: Measure actual matmul time across TP sizes 1, 2, 4 on your hardware. At what TP size does communication overhead dominate compute savings?\n",
            "2. **Pipeline parallelism**: Implement a simple pipeline parallel transformer (2 GPUs, each holds half the layers). Measure pipeline bubble overhead.\n",
            "3. **TP + EP combined**: For a 64-expert MoE model on 8 GPUs with EP=8 and TP=2, draw the communication pattern. How many cross-GPU messages per forward pass?\n"],"c10"),
        cell_md(["## Key Takeaways\n",
            "\n",
            "- Tensor parallelism splits individual weight matrices across GPUs, requiring one AllReduce per transformer layer — efficient on NVLink, expensive on InfiniBand.\n",
            "- Megatron-style TP pairs column-parallel and row-parallel to reduce AllReduce count from 2 per matmul pair to 1 per transformer block.\n",
            "- Expert parallelism places different experts on different GPUs; All-to-All communication routes tokens to the correct expert GPU.\n",
            "- TP is best within a node (NVLink); pipeline parallelism is better across nodes (no AllReduce, just point-to-point activations).\n",
            "\n## References\n- *Inference Engineering* Ch 5.4 — Philip Kiely, Baseten Books 2026\n- Shoeybi et al. (2019), \"Megatron-LM: Training Multi-Billion Parameter Language Models\"\n- Lepikhin et al. (2020), \"GShard: Scaling Giant Models with Conditional Computation\"\n"],"c11"),
    ]
    p = write_nb(15, "model-parallelism", cells)
    write_readme(15,"Model Parallelism: Tensor & Expert","Runtime",
        "Implemented column-parallel and row-parallel linear layers simulating tensor parallelism across N GPUs. Analyzed AllReduce communication cost on NVLink vs InfiniBand vs PCIe, and simulated expert parallelism routing for MoE models.",
        "Tensor parallelism's efficiency entirely depends on interconnect: at 900 GB/s NVLink the AllReduce overhead is negligible; at 25 GB/s InfiniBand it dominates for large TP degrees. This is why TP is intra-node and PP is inter-node.",
        "model-parallelism","AllReduce cost vs TP size + expert parallelism communication analysis",
        "- *Inference Engineering* Ch 5.4 (Philip Kiely, Baseten Books 2026)\n- Shoeybi et al. (2019), \"Megatron-LM\"")
    return validate(p)

def day16():
    cells = [
        cell_md(["# Day 16: Disaggregation — Prefill/Decode Split\n","> *Inference Engineering* — Chapter 5.5 | Philip Kiely, Baseten Books 2026\n","\n**Layer:** Runtime | **Prerequisite:** Day 10 (Dynamo), Day 15 (Model Parallelism)\n"],"c01"),
        cell_md(["## Concept Overview\n","\n",
            "Chapter 5.5 extends the disaggregation concept from Day 10 (Dynamo architecture) to the algorithmic and systems design level. ",
            "Prefill-decode disaggregation is not just about separate workers — it's about optimal resource allocation given their fundamentally different compute profiles. ",
            "Prefill workers need high compute (H100s), decode workers need high memory bandwidth. ",
            "The optimal ratio of prefill:decode workers depends on input:output length ratio, SLO requirements, and model size. ",
            "This day focuses on modeling and optimizing that ratio.\n"],"c02"),
        cell_code(["import torch\nimport numpy as np\nimport matplotlib.pyplot as plt\nfrom scipy.optimize import minimize_scalar\n\nprint(f'CUDA available: {torch.cuda.is_available()}')\nif torch.cuda.is_available():\n    print(f'GPU: {torch.cuda.get_device_name(0)}')\n"],"c03"),
        cell_md(["## 1. Prefill and Decode Time Models\n","\n","Building analytical models for prefill and decode time as functions of model config and hardware.\n"],"c04"),
        cell_code(["class InferenceTimeModel:\n    def __init__(self, model_name, num_layers, d_model, num_heads, d_head, num_kv_heads,\n                 peak_flops_tflops, hbm_bw_gb_s, dtype_bytes=2):\n        self.name = model_name\n        self.L = num_layers\n        self.D = d_model\n        self.H = num_heads\n        self.Hkv = num_kv_heads\n        self.dh = d_head\n        self.peak_flops = peak_flops_tflops * 1e12\n        self.hbm_bw = hbm_bw_gb_s * 1e9\n        self.dtype = dtype_bytes\n\n    def prefill_time_ms(self, seq_len, batch=1):\n        \"\"\"Compute-bound prefill: dominated by FLOPs.\"\"\"\n        # Attention FLOPs\n        attn = self.L * (4 * batch * seq_len * self.D**2 +\n                         2 * batch * seq_len**2 * self.D)\n        # FFN FLOPs\n        ffn = self.L * 2 * batch * seq_len * self.D * 4 * self.D\n        total_flops = attn + ffn\n        return total_flops / self.peak_flops * 1000\n\n    def decode_time_ms(self, kv_len, batch=1):\n        \"\"\"Memory-bound decode: dominated by weight loading.\"\"\"\n        # Weight bytes per step (approximation)\n        attn_bytes = self.L * 4 * self.D * self.D * self.dtype  # Q,K,V,O projections\n        ffn_bytes = self.L * 3 * self.D * 4 * self.D * self.dtype  # SwiGLU\n        total_bytes = (attn_bytes + ffn_bytes)\n        return total_bytes / self.hbm_bw * 1000  # per token\n\n# DGX Spark GB10 specs\nspark = InferenceTimeModel(\n    'DGX Spark GB10',\n    num_layers=32, d_model=4096, num_heads=32, d_head=128, num_kv_heads=8,\n    peak_flops_tflops=67, hbm_bw_gb_s=273\n)\n\nprint('Prefill time vs sequence length (Llama-3-8B on DGX Spark):')\nfor seq in [128, 256, 512, 1024, 2048, 4096]:\n    t = spark.prefill_time_ms(seq)\n    print(f'  seq={seq:>5}: {t:>8.2f} ms')\n\ndecode_t = spark.decode_time_ms(0)\nprint(f'\\nDecode time per token: {decode_t:.2f} ms (memory-bound, batch=1)')\nprint(f'Max decode throughput: {1000/decode_t:.0f} tokens/sec (single stream)')\n"],"c05"),
        cell_md(["## 2. Optimal Worker Ratio\n","\n","Given a traffic mix (mean input length $L_{in}$, mean output length $L_{out}$), what ratio of prefill:decode workers minimizes total cost while meeting SLOs?\n"],"c06"),
        cell_code(["def worker_balance_analysis(mean_input_len, mean_output_len, total_workers=8):\n    \"\"\"\n    Find optimal prefill:decode split for given traffic.\n    Returns metrics for each possible split.\n    \"\"\"\n    results = []\n    for n_prefill in range(1, total_workers):\n        n_decode = total_workers - n_prefill\n\n        # Prefill capacity: requests per second each worker can handle\n        t_prefill_ms = spark.prefill_time_ms(mean_input_len)\n        prefill_rps = n_prefill * (1000 / t_prefill_ms)\n\n        # Decode capacity: tokens per second each worker can handle\n        t_decode_ms = spark.decode_time_ms(mean_input_len)  # approx per token\n        decode_tps = n_decode * (1000 / t_decode_ms)\n        decode_rps = decode_tps / mean_output_len  # requests per second\n\n        # Bottleneck throughput\n        throughput = min(prefill_rps, decode_rps)\n        utilization_prefill = throughput / prefill_rps\n        utilization_decode = throughput / decode_rps\n\n        results.append({\n            'n_prefill': n_prefill, 'n_decode': n_decode,\n            'throughput': throughput,\n            'util_prefill': utilization_prefill,\n            'util_decode': utilization_decode,\n        })\n\n    return results\n\nfig, axes = plt.subplots(1, 2, figsize=(12, 5))\n\nfor ax, (title, l_in, l_out) in zip(axes, [\n    ('Short prompts (l_in=128, l_out=512)', 128, 512),\n    ('Long prompts (l_in=2048, l_out=256)', 2048, 256),\n]):\n    results = worker_balance_analysis(l_in, l_out)\n    n_p = [r['n_prefill'] for r in results]\n    throughputs = [r['throughput'] for r in results]\n    best_idx = np.argmax(throughputs)\n\n    ax.plot(n_p, throughputs, 'b-o')\n    ax.axvline(n_p[best_idx], color='red', linestyle='--',\n               label=f'Optimal: {n_p[best_idx]} prefill, {8-n_p[best_idx]} decode')\n    ax.set_xlabel('# Prefill Workers')\n    ax.set_ylabel('Throughput (req/s)')\n    ax.set_title(title)\n    ax.legend(fontsize=8)\n    ax.grid(True)\n\nplt.tight_layout()\nplt.savefig('prefill_decode_balance.png', dpi=100, bbox_inches='tight')\nplt.show()\nprint('Saved prefill_decode_balance.png')\n"],"c07"),
        cell_md(["## 3. TTFT vs TPOT SLO Tradeoffs\n","\n","Disaggregation lets you optimize TTFT (Time to First Token) and TPOT (Time Per Output Token) independently by allocating resources to each phase.\n"],"c08"),
        cell_code(["# Model TTFT and TPOT as a function of worker allocation\ndef slo_analysis(request_rate_rps, n_prefill, n_decode, l_in=512, l_out=256):\n    \"\"\"Compute expected TTFT and TPOT under M/M/c queuing approximation.\"\"\"\n    # Prefill system\n    t_prefill = spark.prefill_time_ms(l_in) / 1000  # seconds\n    prefill_load = request_rate_rps * t_prefill / n_prefill  # utilization\n    if prefill_load >= 1.0:\n        ttft_s = float('inf')\n    else:\n        ttft_s = t_prefill / (1 - prefill_load)  # M/M/1 approximation\n\n    # Decode system\n    t_decode_per_token = spark.decode_time_ms(l_in) / 1000\n    total_decode_demand = request_rate_rps * l_out * t_decode_per_token\n    decode_load = total_decode_demand / n_decode\n    if decode_load >= 1.0:\n        tpot_s = float('inf')\n    else:\n        tpot_s = t_decode_per_token / (1 - decode_load)\n\n    return ttft_s * 1000, tpot_s * 1000  # ms\n\nprint('TTFT and TPOT at different worker allocations (4 total workers, 5 req/s):')\nprint(f'{\"Prefill\":>8} {\"Decode\":>8} {\"TTFT (ms)\":>12} {\"TPOT (ms)\":>12}')\nfor n_p in range(1, 4):\n    n_d = 4 - n_p\n    ttft, tpot = slo_analysis(5.0, n_p, n_d)\n    ttft_str = f'{ttft:.0f}' if ttft < 1e6 else 'OVERlOAD'\n    tpot_str = f'{tpot:.1f}' if tpot < 1e6 else 'OVERLOAD'\n    print(f'{n_p:>8} {n_d:>8} {ttft_str:>12} {tpot_str:>12}')\n"],"c09"),
        cell_md(["## Experiments: Try These\n",
            "\n",
            "1. **Traffic shift**: Simulate a traffic shift from short to long prompts. How quickly should the system reallocate workers between prefill and decode?\n",
            "2. **Heterogeneous hardware**: Assign H100s to prefill (high compute) and A100s to decode (high bandwidth). Does this change the optimal ratio?\n",
            "3. **Continuous profiling**: Build a monitoring tool that measures actual prefill and decode times and adjusts worker allocation dynamically.\n"],"c10"),
        cell_md(["## Key Takeaways\n",
            "\n",
            "- Prefill-decode disaggregation enables separate optimization of TTFT (prefill-constrained) and TPOT (decode-constrained) with independent resource allocation.\n",
            "- The optimal prefill:decode worker ratio depends on the input:output length ratio — short prompts/long outputs → more decode workers.\n",
            "- Analytical queuing models (M/M/c) give good intuition for SLO violations before system saturation at ~70-80% utilization.\n",
            "- In practice, autoscaling should continuously measure prefill and decode queue depths independently and scale each component separately.\n",
            "\n## References\n- *Inference Engineering* Ch 5.5 — Philip Kiely, Baseten Books 2026\n- Patel et al. (2023), \"Splitwise: Efficient Generative LLM Inference Using Phase Splitting\"\n- Zhong et al. (2024), \"DistServe\"\n"],"c11"),
    ]
    p = write_nb(16, "prefill-decode-disaggregation", cells)
    write_readme(16,"Disaggregation: Prefill/Decode Split","Runtime",
        "Built an analytical inference time model for prefill and decode phases and optimized the worker ratio for different traffic patterns. Modeled TTFT and TPOT SLOs under queuing theory and showed how optimal allocation changes with input:output length ratio.",
        "The optimal prefill:decode ratio is not 50:50 — it depends entirely on the input/output length mix. Short prompts with long outputs need 6-7x more decode workers than prefill workers.",
        "prefill-decode-disaggregation","Optimal worker allocation curves for different prompt length distributions",
        "- *Inference Engineering* Ch 5.5 (Philip Kiely, Baseten Books 2026)\n- Patel et al. (2023), \"Splitwise\"\n- Zhong et al. (2024), \"DistServe\"")
    return validate(p)

def day17():
    cells = [
        cell_md(["# Day 17: GPU Architecture — SMs, Memory Hierarchy, HBM\n","> *Inference Engineering* — Chapter 3.1 | Philip Kiely, Baseten Books 2026\n","\n**Layer:** Infrastructure | **Prerequisite:** Day 04 (Arithmetic Intensity)\n"],"c01"),
        cell_md(["## Concept Overview\n","\n",
            "A modern GPU (Hopper H100) has 132 Streaming Multiprocessors (SMs), each with 128 CUDA cores. ",
            "The memory hierarchy spans registers (fastest, per-thread) → L1/shared SRAM (per-SM) → L2 cache → HBM (high-bandwidth memory). ",
            "HBM is the AI accelerator's defining feature: 3.35 TB/s bandwidth on H100 vs ~100 GB/s for CPU DRAM. ",
            "Understanding this hierarchy is key to writing efficient kernels and understanding why roofline analysis matters.\n"],"c02"),
        cell_code(["import torch\nimport numpy as np\nimport matplotlib.pyplot as plt\nimport time\n\nprint(f'CUDA available: {torch.cuda.is_available()}')\nif torch.cuda.is_available():\n    print(f'GPU: {torch.cuda.get_device_name(0)}')\n    props = torch.cuda.get_device_properties(0)\n    print(f'SM count: {props.multi_processor_count}')\n    print(f'Total memory: {props.total_memory/1e9:.1f} GB')\n    print(f'Max threads per SM: {props.max_threads_per_multi_processor}')\n    print(f'Shared memory per SM: {props.shared_memory_per_multiprocessor/1024:.0f} KB')\n    print(f'L2 cache: {props.l2_cache_size/1e6:.0f} MB')\n"],"c03"),
        cell_md(["## 1. GPU Memory Hierarchy\n","\n","Access latency and bandwidth at each level of the hierarchy determines what can be computed efficiently.\n"],"c04"),
        cell_code(["# GPU Memory Hierarchy (H100 SXM)\nhierarchy = [\n    ('Registers',       '256 KB/SM',  '~1 cycle',    'N/A',          'per-thread'),\n    ('L1/Shared SRAM',  '228 KB/SM',  '~30 cycles',  '33 TB/s',      'per-SM'),\n    ('L2 Cache',        '50 MB',      '~100 cycles', '12 TB/s',      'all SMs'),\n    ('HBM3',            '80 GB',      '~400 cycles', '3.35 TB/s',    'all SMs'),\n    ('NVLink',          'N/A',        '~1000 cycles','900 GB/s',     'multi-GPU'),\n    ('PCIe 5.0',        'N/A',        '~100K cycles','128 GB/s',     'CPU-GPU'),\n    ('CPU DRAM',        '768 GB',     '~300 cycles', '~100 GB/s',    'host'),\n]\n\nprint('H100 SXM Memory Hierarchy:')\nprint(f'{\"Level\":<22} {\"Capacity\":<15} {\"Latency\":<15} {\"Bandwidth\":<15} {\"Scope\":<15}')\nprint('-' * 82)\nfor level, cap, lat, bw, scope in hierarchy:\n    print(f'{level:<22} {cap:<15} {lat:<15} {bw:<15} {scope:<15}')\n"],"c05"),
        cell_md(["## 2. Streaming Multiprocessor (SM) Anatomy\n","\n","Each SM has CUDA cores for FP32/INT32, Tensor Cores for matrix ops, special function units (SFUs), and shared SRAM.\n"],"c06"),
        cell_code(["# SM utilization model\ndef sm_occupancy(threads_per_block, registers_per_thread, shared_mem_per_block,\n                max_threads_per_sm=2048, max_regs_per_sm=65536, max_shared_mem_per_sm=228*1024):\n    \"\"\"Estimate SM occupancy (fraction of max concurrent warps).\"\"\"\n    warps_per_block = (threads_per_block + 31) // 32\n    # Limits from different resources\n    limit_threads = max_threads_per_sm // threads_per_block\n    limit_regs = max_regs_per_sm // (registers_per_thread * threads_per_block) if registers_per_thread > 0 else 999\n    limit_shared = max_shared_mem_per_sm // shared_mem_per_block if shared_mem_per_block > 0 else 999\n    max_blocks = min(limit_threads, limit_regs, limit_shared, 32)  # max 32 blocks per SM\n    active_warps = max_blocks * warps_per_block\n    max_warps = max_threads_per_sm // 32\n    occupancy = min(active_warps, max_warps) / max_warps\n    return occupancy, max_blocks, min(limit_threads, limit_regs, limit_shared)\n\nprint('SM Occupancy Analysis:')\nprint(f'{\"Threads/Block\":>15} {\"Regs/Thread\":>13} {\"Shared (KB)\":>13} {\"Occupancy\":>11} {\"Limiter\":>12}')\nfor (tpb, rpt, smem_kb) in [\n    (128,  32, 4),\n    (256,  64, 4),\n    (512,  32, 0),\n    (1024, 32, 0),\n    (256,  64, 64),\n    (128, 128, 0),\n]:\n    occ, max_b, limiter_count = sm_occupancy(tpb, rpt, smem_kb*1024)\n    print(f'{tpb:>15} {rpt:>13} {smem_kb:>13} {occ:>11.0%} {limiter_count:>12}')\n"],"c07"),
        cell_md(["## 3. Measuring HBM Bandwidth in Practice\n","\n","Measure how close to peak HBM bandwidth a memory-bound operation achieves on the target GPU.\n"],"c08"),
        cell_code(["def measure_hbm_bandwidth(size_mb=512):\n    if not torch.cuda.is_available():\n        print('GPU not available')\n        return\n\n    n = size_mb * 1024 * 1024 // 2\n    a = torch.randn(n, dtype=torch.float16, device='cuda')\n    b = torch.empty_like(a)\n\n    # Warmup\n    for _ in range(5):\n        b.copy_(a)\n    torch.cuda.synchronize()\n\n    times = []\n    for _ in range(20):\n        t0 = time.perf_counter()\n        b.copy_(a)\n        torch.cuda.synchronize()\n        times.append(time.perf_counter() - t0)\n\n    bytes_moved = 2 * n * 2  # read + write\n    bw_gb_s = bytes_moved / np.median(times) / 1e9\n    print(f'HBM Bandwidth measurement ({size_mb} MB transfer):')\n    print(f'  Median time: {np.median(times)*1000:.3f} ms')\n    print(f'  Bandwidth: {bw_gb_s:.1f} GB/s')\n    print(f'  (DGX Spark spec: ~273 GB/s)')\n    print(f'  Efficiency: {bw_gb_s/273*100:.0f}% of peak')\n\nmeasure_hbm_bandwidth()\n"],"c09"),
        cell_md(["## Experiments: Try These\n",
            "\n",
            "1. **L2 cache effects**: Measure bandwidth for tensor sizes from 1KB to 1GB. Plot bandwidth vs size — identify where L2 cache and HBM transitions occur.\n",
            "2. **Occupancy optimization**: Profile a simple kernel with `torch.profiler`. Look at `sm_efficiency` and `achieved_occupancy` metrics.\n",
            "3. **Warp divergence**: Write two versions of a conditional kernel — one with aligned conditions (all warps take same branch) and one with divergence. Measure the throughput difference.\n"],"c10"),
        cell_md(["## Key Takeaways\n",
            "\n",
            "- GPUs have 100s of SMs, each with 1000s of CUDA cores — parallelism happens at the warp level (32 threads executing in lockstep).\n",
            "- The memory hierarchy spans registers → shared SRAM → L2 → HBM, with 3-400x bandwidth difference between levels.\n",
            "- HBM (High Bandwidth Memory) stacked on-package is what makes GPUs viable for LLM inference: 3.35 TB/s on H100 vs 100 GB/s for CPU DRAM.\n",
            "- SM occupancy determines how well the GPU can hide memory latency — high occupancy is needed for memory-bound kernels.\n",
            "\n## References\n- *Inference Engineering* Ch 3.1 — Philip Kiely, Baseten Books 2026\n- NVIDIA H100 Architecture White Paper\n- CUDA Programming Guide: Memory Hierarchy\n"],"c11"),
    ]
    p = write_nb(17, "gpu-architecture-sms-hbm", cells)
    write_readme(17,"GPU Architecture: SMs, Memory Hierarchy, HBM","Infrastructure",
        "Mapped the H100's memory hierarchy from registers to HBM, measured achievable HBM bandwidth on the target GPU, and analyzed SM occupancy limits from threads, registers, and shared memory constraints.",
        "HBM on-package memory is the defining LLM inference hardware feature: at 3.35 TB/s, it's 33x faster than CPU DRAM — but it's still the bottleneck for memory-bound decode.",
        "gpu-architecture-sms-hbm","HBM bandwidth measurement + SM occupancy analysis across kernel configurations",
        "- *Inference Engineering* Ch 3.1 (Philip Kiely, Baseten Books 2026)\n- NVIDIA H100 Architecture White Paper")
    return validate(p)

def day18():
    cells = [
        cell_md(["# Day 18: GPU Generations — Hopper, Ada, Blackwell, Rubin\n","> *Inference Engineering* — Chapter 3.2 | Philip Kiely, Baseten Books 2026\n","\n**Layer:** Infrastructure | **Prerequisite:** Day 17 (GPU Architecture)\n"],"c01"),
        cell_md(["## Concept Overview\n","\n",
            "NVIDIA's GPU generations follow a predictable scaling trajectory. Ampere (A100) → Hopper (H100) brought FP8 Tensor Cores and NVLink 4.0. ",
            "Ada Lovelace (RTX 40xx, L40S) added Ada Tensor Cores and higher FP8 throughput for edge/cloud inference. ",
            "Blackwell (B100, B200, GB200) doubles Hopper's FP8 throughput and introduces FP4 precision, GB200 NVL72 racks, and a new interconnect architecture. ",
            "Rubin follows Blackwell. Each generation roughly doubles the inference-relevant compute: 2x the FP8 TFLOP/s and 2x the memory bandwidth.\n"],"c02"),
        cell_code(["import numpy as np\nimport matplotlib.pyplot as plt\nimport torch\n\nprint(f'CUDA available: {torch.cuda.is_available()}')\nif torch.cuda.is_available():\n    print(f'GPU: {torch.cuda.get_device_name(0)}')\n"],"c03"),
        cell_md(["## 1. Generation Comparison\n","\n","Comparing key inference-relevant specs across GPU generations.\n"],"c04"),
        cell_code(["gpu_gens = [\n    # (name, year, fp16_tflops, fp8_tflops, fp4_tflops, hbm_gb, hbm_bw_tbs, nvlink_bw_gbs, process_nm)\n    ('A100 SXM4',  2020, 312,   None,  None, 80,  2.0,  600,  7),\n    ('H100 SXM5',  2022, 989,   1979,  None, 80,  3.35, 900,  4),\n    ('H200 SXM',   2023, 989,   1979,  None, 141, 4.8,  900,  4),\n    ('L40S',       2023, 362,   733,   None, 48,  0.864,None, 4),\n    ('B100 SXM',   2024, 1800,  3500,  7000, 192, 8.0,  1800, 4),\n    ('B200 SXM',   2024, 2250,  4500,  9000, 192, 8.0,  1800, 4),\n    ('GB10 (Spark)',2025, 67,   134,   None, 128, 0.273,None, 4),\n]\n\nprint(f'{\"GPU\":<15} {\"Year\":>6} {\"FP16\":>8} {\"FP8\":>8} {\"FP4\":>8} {\"HBM\":>6} {\"BW\":>6} {\"Node\":>6}')\nprint(f'{\"\":15} {\"\":6} {\"TFLOPS\":>8} {\"TFLOPS\":>8} {\"TFLOPS\":>8} {\"(GB)\":>6} {\"(TB/s)\":>6} {\"(nm)\":>6}')\nprint('-' * 70)\nfor row in gpu_gens:\n    name, year, fp16, fp8, fp4, hbm, bw, _, nm = row\n    fp8_s = f'{fp8:.0f}' if fp8 else 'N/A'\n    fp4_s = f'{fp4:.0f}' if fp4 else 'N/A'\n    print(f'{name:<15} {year:>6} {fp16:>8.0f} {fp8_s:>8} {fp4_s:>8} {hbm:>6} {bw:>6.2f} {nm:>6}')\n"],"c05"),
        cell_md(["## 2. Generation-Over-Generation Scaling\n","\n","Each major generation roughly doubles FP8 throughput — plotting the scaling trajectory.\n"],"c06"),
        cell_code(["gens = [\n    ('A100', 2020, 312),\n    ('H100', 2022, 989*2),   # FP8 ≈ 2x FP16\n    ('B200', 2024, 4500),    # FP8\n]\n\nfig, axes = plt.subplots(1, 2, figsize=(12, 5))\n\nax = axes[0]\nyears = [g[1] for g in gens]\ntflops = [g[2] for g in gens]\nnames = [g[0] for g in gens]\nax.semilogy(years, tflops, 'b-o', markersize=10)\nfor y, t, n in zip(years, tflops, names):\n    ax.annotate(f'{n}\\n{t:.0f}T', (y, t), textcoords='offset points', xytext=(5,5))\nax.set_xlabel('Year')\nax.set_ylabel('FP8 TFLOP/s')\nax.set_title('NVIDIA GPU FP8 Throughput by Generation')\nax.grid(True, which='both')\n\n# Bandwidth scaling\nax2 = axes[1]\nbw_data = [('A100', 2020, 2.0), ('H100', 2022, 3.35), ('H200', 2023, 4.8), ('B100', 2024, 8.0)]\nyrs = [d[1] for d in bw_data]\nbws = [d[2] for d in bw_data]\nnms = [d[0] for d in bw_data]\nax2.plot(yrs, bws, 'r-o', markersize=10)\nfor y, b, n in zip(yrs, bws, nms):\n    ax2.annotate(f'{n}\\n{b}TB/s', (y, b), textcoords='offset points', xytext=(5,5))\nax2.set_xlabel('Year')\nax2.set_ylabel('HBM Bandwidth (TB/s)')\nax2.set_title('HBM Bandwidth Scaling')\nax2.grid(True)\n\nplt.tight_layout()\nplt.savefig('gpu_generations.png', dpi=100, bbox_inches='tight')\nplt.show()\nprint('Saved gpu_generations.png')\n"],"c07"),
        cell_md(["## 3. New Features by Generation\n","\n","Each generation adds capabilities beyond raw throughput that affect inference architecture decisions.\n"],"c08"),
        cell_code(["features = {\n    'A100 (Ampere)': [\n        'FP16/BF16 Tensor Cores (3rd gen)',\n        'A100 Multi-Instance GPU (MIG) — up to 7 instances',\n        'NVLink 3.0: 600 GB/s bidirectional',\n        'HBM2e: 2.0 TB/s',\n        'sparsity acceleration (2:4 structured)',\n    ],\n    'H100 (Hopper)': [\n        'FP8 Tensor Cores (first generation)',\n        'Transformer Engine: auto FP8/FP16 selection',\n        'NVLink 4.0: 900 GB/s',\n        'HBM3: 3.35 TB/s',\n        'In-order Tensor Core pipeline',\n        'Confidential Computing support',\n    ],\n    'B200 (Blackwell)': [\n        'FP4 Tensor Cores (first generation)',\n        '2nd gen Transformer Engine',\n        'NVLink 5.0: 1800 GB/s',\n        'HBM3e: 8.0 TB/s',\n        'RAS (Reliability/Availability/Serviceability) engine',\n        'Decompression engine for database workloads',\n    ],\n}\nfor gen, feats in features.items():\n    print(f'{gen}:')\n    for f in feats:\n        print(f'  + {f}')\n    print()\n"],"c09"),
        cell_md(["## 4. Choosing Hardware for Inference\n","\n","The right GPU depends on model size, latency requirements, and cost per token.\n"],"c10"),
        cell_code(["# Cost-efficiency model: tokens per dollar\ncost_data = [\n    # (GPU, cost_hr_cloud, fp8_tflops, hbm_gb, hbm_bw_tbs)\n    ('A100 80GB', 3.5,  312*2,  80,  2.0),\n    ('H100 80GB', 10.0, 1979,   80,  3.35),\n    ('H200',      12.0, 1979,   141, 4.8),\n    ('L40S',      4.5,  733,    48,  0.864),\n    ('B200',      20.0, 4500,   192, 8.0),\n]\n\n# For decode-bound workload: throughput ~ min(compute, BW)\n# Decode throughput ∝ HBM BW (memory-bound)\n# Prefill throughput ∝ FP8 TFLOPS (compute-bound)\nprint(f'{\"GPU\":<15} {\"$/hr\":>6} {\"Decode (BW-bound)\":>20} {\"Prefill (compute)\":>20}')\nprint(f'{\"\":<15} {\"\":>6} {\"BW/cost\":>20} {\"TFLOPS/cost\":>20}')\nprint('-' * 64)\nfor name, cost, tflops, hbm, bw in cost_data:\n    print(f'{name:<15} {cost:>6.1f} {bw/cost:>20.2f} TB/s/$ {tflops/cost:>16.0f} TF/$')\n"],"c11"),
        cell_md(["## Experiments: Try These\n",
            "\n",
            "1. **FP8 vs FP16 benchmark**: Run a GEMM benchmark with FP8 and FP16 on H100 (if available). Measure the actual throughput ratio vs the theoretical 2x.\n",
            "2. **NVLink vs PCIe**: Benchmark point-to-point bandwidth on your DGX Spark pair using `torch.distributed`. How close to spec is the measured rate?\n",
            "3. **MIG partitioning**: Configure MIG on a Spark GPU with `nvidia-smi mig` commands. Profile inference latency and throughput at 1g.10gb vs 2g.20gb vs 7g.80gb.\n"],"c12"),
        cell_md(["## Key Takeaways\n",
            "\n",
            "- Each NVIDIA GPU generation roughly doubles FP8 throughput and HBM bandwidth every 2 years.\n",
            "- H100 introduced FP8 Tensor Cores and the Transformer Engine — the first GPU architecturally optimized for LLM inference.\n",
            "- Blackwell adds FP4, doubles H100's compute/bandwidth, and GB200 NVL72 racks enable 72-GPU NVLink domains for giant model serving.\n",
            "- For memory-bound decode workloads, HBM bandwidth per dollar is the key metric — not raw TFLOP/s.\n",
            "\n## References\n- *Inference Engineering* Ch 3.2 — Philip Kiely, Baseten Books 2026\n- NVIDIA Hopper Architecture White Paper\n- NVIDIA Blackwell Architecture Technical Brief\n"],"c13"),
    ]
    p = write_nb(18, "gpu-generations", cells)
    write_readme(18,"GPU Generations: Hopper, Ada, Blackwell, Rubin","Infrastructure",
        "Surveyed NVIDIA GPU generations from A100 through B200, comparing FP8/FP4 throughput, HBM bandwidth, NVLink speed, and new architectural features. Analyzed cost-efficiency for decode vs prefill workloads.",
        "H100 was the first GPU architecturally designed for LLM inference (FP8 Tensor Cores + Transformer Engine). Blackwell doubles it. For pure decode workloads, HBM bandwidth per dollar matters more than peak TFLOP/s.",
        "gpu-generations","Generation-over-generation scaling charts (TFLOP/s and bandwidth) + cost-efficiency analysis",
        "- *Inference Engineering* Ch 3.2 (Philip Kiely, Baseten Books 2026)\n- NVIDIA H100 and Blackwell Architecture White Papers")
    return validate(p)

def day19():
    cells = [
        cell_md(["# Day 19: Multi-GPU Instances & Multi-Instance GPU (MIG)\n","> *Inference Engineering* — Chapter 3.3 | Philip Kiely, Baseten Books 2026\n","\n**Layer:** Infrastructure | **Prerequisite:** Day 17 (GPU Architecture), Day 18 (GPU Generations)\n"],"c01"),
        cell_md(["## Concept Overview\n","\n",
            "Multi-Instance GPU (MIG) partitions a single A100/H100 into up to 7 isolated GPU instances, each with dedicated compute engines, memory, and memory bandwidth. ",
            "Unlike time-sharing (MPS), MIG provides hard isolation — one instance cannot see another's memory or affect its performance. ",
            "For inference, MIG enables multi-tenancy: running multiple smaller models on one GPU with guaranteed quality-of-service, ",
            "or splitting a GPU for different request types (e.g., interactive vs batch). ",
            "The tradeoff: smaller instances have less bandwidth and less total memory, limiting what models you can serve.\n"],"c02"),
        cell_code(["import numpy as np\nimport matplotlib.pyplot as plt\nimport torch\n\nprint(f'CUDA available: {torch.cuda.is_available()}')\nif torch.cuda.is_available():\n    print(f'GPU: {torch.cuda.get_device_name(0)}')\n    props = torch.cuda.get_device_properties(0)\n    print(f'Total memory: {props.total_memory/1e9:.1f} GB')\n"],"c03"),
        cell_md(["## 1. MIG Instance Types (H100)\n","\n","H100 supports 7 MIG instance sizes, each with different compute and memory allocations.\n"],"c04"),
        cell_code(["# H100 80GB MIG profiles\nmig_profiles = [\n    # (profile_name, gpu_slices, memory_gb, memory_bw_gb_s, fp16_tflops)\n    ('7g.80gb',  7, 79.5, 3350, 989),   # full GPU\n    ('4g.40gb',  4, 40.0, 1900, 495),\n    ('3g.40gb',  3, 39.5, 1900, 371),\n    ('2g.20gb',  2, 19.5, 950,  247),\n    ('1g.20gb',  1, 19.5, 475,  124),\n    ('1g.10gb',  1, 9.75, 475,  124),\n    ('1g.5gb',   1, 4.75, 475,  124),\n]\n\nprint('H100 MIG Instance Profiles:')\nprint(f'{\"Profile\":<12} {\"Slices\":>8} {\"Memory\":>10} {\"BW (GB/s)\":>12} {\"FP16 TF\":>10} {\"Max insts\":>10}')\nprint('-' * 64)\nfor name, slices, mem, bw, tflops in mig_profiles:\n    max_instances = 7 // slices\n    print(f'{name:<12} {slices:>8} {mem:>9.1f}G {bw:>12} {tflops:>10} {max_instances:>10}')\n"],"c05"),
        cell_md(["## 2. Multi-Tenancy Use Cases\n","\n","MIG enables running multiple workloads with strict isolation — each gets guaranteed resources.\n"],"c06"),
        cell_code(["# Model what fits in each MIG instance\nmodels = [\n    # (name, params_b, fp16_gb, min_bw_needed_gb_s)\n    ('GPT-2 117M',    0.117,  0.23, 10),\n    ('Llama-3.2-1B',  1.0,   2.0,  20),\n    ('Llama-3.2-3B',  3.0,   6.0,  50),\n    ('Llama-3-8B',    8.0,   16.0, 80),\n    ('Llama-3-70B',  70.0,  140.0, 400),\n]\n\nprint('Model-to-MIG-Profile Mapping (can it fit + have sufficient BW?):')\nprint(f'{\"Model\":<20}', end='')\nfor name, _, _, _ in mig_profiles[:5]:\n    print(f' {name:>10}', end='')\nprint()\nprint('-' * 70)\nfor mname, params, fp16_gb, min_bw in models:\n    print(f'{mname:<20}', end='')\n    for pname, slices, mem, bw, tflops in mig_profiles[:5]:\n        fits = '✓' if fp16_gb <= mem and bw >= min_bw else ('~' if fp16_gb <= mem else '✗')\n        print(f' {fits:>10}', end='')\n    print()\nprint('\\n✓ = fits + BW ok | ~ = fits, BW marginal | ✗ = OOM')\n"],"c07"),
        cell_md(["## 3. MIG vs Time-Sharing vs Dedicated\n","\n","Comparing isolation, efficiency, and overhead for different GPU sharing strategies.\n"],"c08"),
        cell_code(["strategies = {\n    'Dedicated GPU': {\n        'isolation': 'Full',\n        'overhead': 'None',\n        'latency_predictability': 'High',\n        'utilization_when_idle': 'Low',\n        'memory_isolation': True,\n        'best_for': 'Latency-sensitive production models',\n    },\n    'MIG': {\n        'isolation': 'Hardware',\n        'overhead': 'None',\n        'latency_predictability': 'High',\n        'utilization_when_idle': 'Low (per instance)',\n        'memory_isolation': True,\n        'best_for': 'Multiple models, guaranteed QoS',\n    },\n    'MPS (Multi-Process)': {\n        'isolation': 'Software',\n        'overhead': 'Low',\n        'latency_predictability': 'Medium',\n        'utilization_when_idle': 'Medium',\n        'memory_isolation': False,\n        'best_for': 'Batch workloads, known-safe tenants',\n    },\n    'Time-sharing': {\n        'isolation': 'Time',\n        'overhead': 'Context switch',\n        'latency_predictability': 'Low',\n        'utilization_when_idle': 'High',\n        'memory_isolation': False,\n        'best_for': 'Dev/test, non-latency-sensitive',\n    },\n}\nfor name, props in strategies.items():\n    print(f'{name}:')\n    for k, v in props.items():\n        print(f'  {k:<30} {v}')\n    print()\n"],"c09"),
        cell_md(["## Experiments: Try These\n",
            "\n",
            "1. **MIG partitioning**: On spark-01, run `sudo nvidia-smi mig -cgi 2g.20gb,2g.20gb,2g.20gb -C` and profile inference latency at each partition size.\n",
            "2. **Isolation test**: Run two models simultaneously in different MIG instances. Verify that one model's memory usage doesn't affect the other's latency.\n",
            "3. **Utilization modeling**: For a given request rate (req/s) and model config, calculate the optimal MIG profile that minimizes cost while meeting P95 latency SLO.\n"],"c10"),
        cell_md(["## Key Takeaways\n",
            "\n",
            "- MIG partitions a physical GPU into up to 7 isolated instances with dedicated compute, memory, and bandwidth — hard isolation, not time-sharing.\n",
            "- Each MIG slice gets 1/N of the GPU's resources; a 1g.10gb instance gets ~1/7th of H100's compute and ~1/7th of its bandwidth.\n",
            "- MIG is ideal for multi-tenant serving of different models with latency SLOs — each tenant gets guaranteed resources.\n",
            "- The decision between MIG, MPS, and dedicated GPUs depends on isolation requirements, model sizes, and traffic predictability.\n",
            "\n## References\n- *Inference Engineering* Ch 3.3 — Philip Kiely, Baseten Books 2026\n- NVIDIA Multi-Instance GPU User Guide\n- NVIDIA A100/H100 MIG documentation\n"],"c11"),
    ]
    p = write_nb(19, "mig-multi-instance-gpu", cells)
    write_readme(19,"Multi-GPU Instances & Multi-Instance GPU (MIG)","Infrastructure",
        "Explored MIG partitioning strategies for H100, mapped model sizes to MIG instance profiles, and compared MIG vs MPS vs time-sharing for multi-tenant inference workloads.",
        "MIG's key advantage over software multi-tenancy: a noisy neighbor in a different MIG slice literally cannot steal your GPU resources — the memory bandwidth and compute engines are physically separate.",
        "mig-multi-instance-gpu","Model-to-MIG-profile fit matrix + multi-tenancy strategy comparison",
        "- *Inference Engineering* Ch 3.3 (Philip Kiely, Baseten Books 2026)\n- NVIDIA MIG User Guide")
    return validate(p)

def day20():
    cells = [
        cell_md(["# Day 20: Containerization — Docker & NVIDIA NIMs\n","> *Inference Engineering* — Chapter 7.1 | Philip Kiely, Baseten Books 2026\n","\n**Layer:** Infrastructure | **Prerequisite:** Day 19 (MIG)\n"],"c01"),
        cell_md(["## Concept Overview\n","\n",
            "Containerization is the standard deployment unit for inference services. Docker containers encapsulate the model, runtime, and dependencies into a reproducible image. ",
            "NVIDIA Container Toolkit (nvidia-docker) passes GPUs to containers with the correct drivers. ",
            "NVIDIA NIMs (NIM Inference Microservices) are pre-built, optimized containers for NVIDIA-supported models — each NIM includes the model weights, TensorRT-LLM engine, and an OpenAI-compatible API server. ",
            "NIMs abstract away the runtime complexity: you pull an image, run it, and get an API endpoint.\n"],"c02"),
        cell_code(["import subprocess, os, json\nprint('Container and NIM concepts demonstration')\nprint('(No Docker daemon required for code analysis)')\n"],"c03"),
        cell_md(["## 1. Dockerfile Structure for Inference Servers\n","\n","A well-structured inference Dockerfile has four layers: base image, system deps, Python deps, and model/code.\n"],"c04"),
        cell_code(["dockerfile_content = '''\n# Stage 1: Base CUDA image\nFROM nvcr.io/nvidia/cuda:12.4.1-cudnn9-runtime-ubuntu22.04\n\n# System dependencies\nRUN apt-get update && apt-get install -y \\\\\n    python3.10 python3-pip git curl \\\\\n    && rm -rf /var/lib/apt/lists/*\n\n# Python dependencies (cached layer)\nCOPY requirements.txt /app/requirements.txt\nRUN pip install --no-cache-dir -r /app/requirements.txt\n\n# Application code\nCOPY . /app\nWORKDIR /app\n\n# Model weights (use volume or download at runtime, not in image)\nENV MODEL_PATH=/models/llama-3-8b\nENV MAX_MODEL_LEN=8192\nENV TENSOR_PARALLEL_SIZE=1\n\n# Health check\nHEALTHCHECK --interval=30s --timeout=10s --retries=3 \\\\\n    CMD curl -f http://localhost:8000/health || exit 1\n\n# Expose port\nEXPOSE 8000\n\n# Run vLLM server\nCMD [\"python3\", \"-m\", \"vllm.entrypoints.openai.api_server\", \\\\\n     \"--model\", \"/models/llama-3-8b\", \\\\\n     \"--port\", \"8000\", \\\\\n     \"--tensor-parallel-size\", \"1\"]\n'''\nprint('Sample inference Dockerfile:')\nprint(dockerfile_content)\n\n# Layer size analysis\nprint('Typical layer sizes:')\nlayers = [\n    ('Base CUDA 12.4 runtime', '1.2 GB', 'Pulled from NVIDIA NGC, cached across deployments'),\n    ('Python + system deps',   '0.5 GB', 'Changes rarely — cache-friendly'),\n    ('Python requirements',    '0.8 GB', 'vLLM, transformers, etc.'),\n    ('Application code',       '0.01 GB','Changes frequently — keep thin'),\n    ('Model weights (volume)', '16 GB',  'NOT in image — mounted at runtime'),\n]\nfor name, size, note in layers:\n    print(f'  {name:<30} {size:<10} {note}')\n"],"c05"),
        cell_md(["## 2. NVIDIA Container Toolkit\n","\n","nvidia-docker passes GPUs to containers. The key flags: `--gpus all` or `--gpus device=0,1`.\n"],"c06"),
        cell_code(["print('NVIDIA Container Toolkit usage:')\nprint()\ncommands = [\n    ('Run with all GPUs',\n     'docker run --gpus all -p 8000:8000 my-inference-server'),\n    ('Run with specific GPU',\n     'docker run --gpus device=0 -p 8000:8000 my-inference-server'),\n    ('Run with MIG instance',\n     'docker run --gpus \\\"device=MIG-GPU-xxx:0\\\" -p 8000:8000 my-inference-server'),\n    ('With model weights volume',\n     'docker run --gpus all -v /models:/models -p 8000:8000 my-server'),\n    ('With shared memory for vLLM',\n     'docker run --gpus all --shm-size=10g -p 8000:8000 vllm-server'),\n]\nfor desc, cmd in commands:\n    print(f'  {desc}:')\n    print(f'    {cmd}')\n    print()\n\nprint('Environment variables for model config:')\nenv_vars = [\n    ('MODEL_NAME',              'Model identifier (HuggingFace or local path)'),\n    ('MAX_MODEL_LEN',           'Maximum context length'),\n    ('TENSOR_PARALLEL_SIZE',    'Number of GPUs for tensor parallelism'),\n    ('GPU_MEMORY_UTILIZATION',  'Fraction of GPU memory to use (e.g., 0.9)'),\n    ('QUANTIZATION',            'Quantization method (awq, gptq, int8)'),\n]\nfor var, desc in env_vars:\n    print(f'  {var:<30} {desc}')\n"],"c07"),
        cell_md(["## 3. NVIDIA NIMs\n","\n","NIMs are pre-optimized inference containers from NVIDIA NGC. They include compiled TensorRT-LLM engines and expose an OpenAI-compatible API.\n"],"c08"),
        cell_code(["print('NVIDIA NIM: What\\'s inside')\nprint()\nnim_contents = [\n    'Compiled TensorRT-LLM engine (GPU-arch specific)',\n    'OpenAI-compatible REST API (/v1/completions, /v1/chat/completions)',\n    'Health check endpoint (/health)',\n    'Metrics endpoint (/metrics) — Prometheus format',\n    'Batching and scheduling logic',\n    'Model weights (baked in or fetched from NGC)',\n]\nfor item in nim_contents:\n    print(f'  + {item}')\n\nprint()\nprint('Running a Llama-3-8B NIM:')\nprint('''\n  # Pull and run (requires NGC API key)\n  export NGC_API_KEY=<your_key>\n  docker login nvcr.io --username \\\\$oauthtoken --password $NGC_API_KEY\n\n  docker run --gpus all \\\\\n    -e NGC_API_KEY=$NGC_API_KEY \\\\\n    -v /models:/opt/nim/.cache \\\\\n    -p 8000:8000 \\\\\n    nvcr.io/nim/meta/llama-3.1-8b-instruct:latest\n\n  # Test\n  curl http://localhost:8000/v1/chat/completions \\\\\n    -H \"Content-Type: application/json\" \\\\\n    -d \\'{\"model\": \"meta/llama-3.1-8b-instruct\",\n          \"messages\": [{\"role\": \"user\", \"content\": \"Hello!\"}]}\\'\\n''')\n\nprint('NIM vs custom vLLM container tradeoffs:')\nfor aspect, nim_val, custom_val in [\n    ('Setup time',     'Minutes',              'Hours'),\n    ('Optimization',   'NVIDIA-tuned TRT-LLM', 'Manual tuning needed'),\n    ('Flexibility',    'NVIDIA-supported models', 'Any model'),\n    ('Custom kernels', 'No',                   'Yes'),\n    ('Debugging',      'Black box',            'Full access'),\n    ('Cost',           'NGC subscription',     'Free'),\n]:\n    print(f'  {aspect:<20} {nim_val:<30} {custom_val}')\n"],"c09"),
        cell_md(["## Experiments: Try These\n",
            "\n",
            "1. **Build a vLLM Docker image**: Write a Dockerfile, build it, and run a Llama-3.2-1B inference server on spark-01. Test with curl.\n",
            "2. **Multi-stage build**: Optimize the Dockerfile with multi-stage build to separate build dependencies from runtime. Measure image size reduction.\n",
            "3. **NIM trial**: Pull a NIM container from NGC and compare TTFT against your hand-built vLLM container on the same GPU.\n"],"c10"),
        cell_md(["## Key Takeaways\n",
            "\n",
            "- Docker containers provide reproducible inference deployments; model weights should be mounted as volumes (not baked into the image).\n",
            "- NVIDIA Container Toolkit passes GPUs to containers; `--gpus` controls device assignment including MIG instance routing.\n",
            "- NIMs are pre-optimized TensorRT-LLM containers with OpenAI-compatible APIs — the fastest path to production for NVIDIA-supported models.\n",
            "- Layer caching matters: put rarely-changing layers (base image, Python deps) at the top; frequently-changing code at the bottom.\n",
            "\n## References\n- *Inference Engineering* Ch 7.1 — Philip Kiely, Baseten Books 2026\n- NVIDIA Container Toolkit documentation\n- NVIDIA NIM documentation (NGC)\n"],"c11"),
    ]
    p = write_nb(20, "containerization-docker-nims", cells)
    write_readme(20,"Containerization: Docker & NVIDIA NIMs","Infrastructure",
        "Designed a production-grade inference Dockerfile with optimal layer caching, explored NVIDIA Container Toolkit GPU passthrough, and analyzed NVIDIA NIMs as pre-optimized deployment packages.",
        "Model weights must never be in the Docker image — they're 16-140GB and make images unmaintainable. Mount as volumes, bake the runtime only.",
        "containerization-docker-nims","Dockerfile layer structure analysis + NIM vs custom container comparison",
        "- *Inference Engineering* Ch 7.1 (Philip Kiely, Baseten Books 2026)\n- NVIDIA Container Toolkit documentation\n- NVIDIA NIM documentation")
    return validate(p)

def day21():
    cells = [
        cell_md(["# Day 21: Autoscaling — Concurrency, Batching & Cold Starts\n","> *Inference Engineering* — Chapter 7.2 | Philip Kiely, Baseten Books 2026\n","\n**Layer:** Infrastructure | **Prerequisite:** Day 20 (Containers)\n"],"c01"),
        cell_md(["## Concept Overview\n","\n",
            "Inference autoscaling adds or removes GPU replicas based on traffic signals. ",
            "The key metrics are: concurrency (simultaneous in-flight requests), queue depth, and GPU utilization. ",
            "Scaling on concurrency is better than CPU-style scaling on utilization — a GPU at 30% utilization may be serving 10 concurrent requests perfectly well. ",
            "Cold start latency (time to load a model from disk to GPU memory) is the primary challenge: a 70B model may take 60-90 seconds to load, during which incoming traffic queues up. ",
            "Strategies to mitigate cold starts: model warm pools, pre-loading, and predictive scaling based on time-of-day patterns.\n"],"c02"),
        cell_code(["import numpy as np\nimport matplotlib.pyplot as plt\nfrom collections import deque\nimport time, random\n\nprint('Autoscaling simulation environment loaded')\n"],"c03"),
        cell_md(["## 1. Scaling Triggers: Concurrency vs Utilization\n","\n","GPU utilization is a poor scaling signal for LLM inference — it's consistently high during decode regardless of load level.\n"],"c04"),
        cell_code(["# Simulate why GPU utilization is a bad scaling metric\nnp.random.seed(42)\nT = 100  # time steps\n\n# Scenario: requests arrive at variable rate\narrival_rate = np.concatenate([\n    np.ones(25) * 2,   # low load\n    np.ones(25) * 8,   # high load\n    np.ones(25) * 20,  # overload\n    np.ones(25) * 4,   # medium\n])\n\nMAX_CONCURRENCY = 10\nqueue = deque()\nactive = []\ntimes = range(T)\nconcurrency_series = []\nutilization_series = []\nqueue_depth_series = []\n\nfor t in times:\n    # New arrivals\n    n_new = np.random.poisson(arrival_rate[t])\n    for _ in range(n_new):\n        queue.append({'arrival': t, 'duration': np.random.randint(3, 12)})\n\n    # Fill active slots\n    while queue and len(active) < MAX_CONCURRENCY:\n        active.append(queue.popleft())\n\n    # GPU utilization: high whenever any request is active\n    util = 1.0 if active else 0.0  # GPU is \"busy\" whenever anything is running\n\n    # Tick active requests\n    active = [r for r in active if r['duration'] > 0]\n    for r in active:\n        r['duration'] -= 1\n\n    concurrency_series.append(len(active))\n    utilization_series.append(util * 100)\n    queue_depth_series.append(len(queue))\n\nfig, axes = plt.subplots(3, 1, figsize=(12, 9), sharex=True)\naxes[0].plot(times, concurrency_series, label='Concurrency')\naxes[0].axhline(MAX_CONCURRENCY * 0.8, color='red', linestyle='--', label='Scale threshold (80%)')\naxes[0].set_ylabel('Active Requests'); axes[0].legend()\naxes[1].plot(times, utilization_series, label='GPU Utilization %', color='orange')\naxes[1].set_ylabel('GPU Util %'); axes[1].legend()\naxes[1].set_ylim(0, 110)\naxes[2].fill_between(times, queue_depth_series, label='Queue Depth', alpha=0.5, color='red')\naxes[2].set_ylabel('Queue Depth'); axes[2].set_xlabel('Time'); axes[2].legend()\nplt.suptitle('Autoscaling Metrics: Concurrency vs GPU Utilization')\nplt.tight_layout()\nplt.savefig('autoscaling_metrics.png', dpi=100, bbox_inches='tight')\nplt.show()\nprint('Note: GPU utilization stays high (100%) even during overload — concurrency and queue depth are better signals')\n"],"c05"),
        cell_md(["## 2. Cold Start Latency Model\n","\n","Model load time is the dominant cold start cost. Understanding the components helps design mitigation strategies.\n"],"c06"),
        cell_code(["def model_load_time_estimate(model_params_b, disk_bw_gb_s=3.0, gpu_bw_gb_s=10.0,\n                             tokenizer_s=1.0, kv_alloc_s=2.0, dtype_bytes=2):\n    \"\"\"Estimate model cold start time.\"\"\"\n    model_gb = model_params_b * dtype_bytes\n    disk_load_s = model_gb / disk_bw_gb_s\n    gpu_transfer_s = model_gb / gpu_bw_gb_s\n    compile_s = model_params_b * 0.5  # rough: 0.5s per billion params for TRT compile\n    total_s = disk_load_s + gpu_transfer_s + tokenizer_s + kv_alloc_s\n    return {\n        'disk_load': disk_load_s,\n        'gpu_transfer': gpu_transfer_s,\n        'tokenizer': tokenizer_s,\n        'kv_alloc': kv_alloc_s,\n        'total': total_s,\n    }\n\nmodels = [(1, 'Llama-3.2-1B'), (8, 'Llama-3-8B'), (70, 'Llama-3-70B'), (405, 'Llama-3-405B')]\n\nprint('Model cold start time estimates:')\nprint(f'{\"Model\":<20} {\"Disk Load\":>12} {\"GPU Xfer\":>10} {\"Total\":>8}')\nfor params, name in models:\n    t = model_load_time_estimate(params)\n    print(f'{name:<20} {t[\"disk_load\"]:>11.1f}s {t[\"gpu_transfer\"]:>9.1f}s {t[\"total\"]:>7.1f}s')\n\nprint('\\nCold start mitigation strategies:')\nstrategies = [\n    ('Model warm pool',       'Keep N idle replicas pre-loaded; route to them immediately'),\n    ('Predictive scaling',    'Scale up before traffic spikes (time-of-day, cron)'),\n    ('Faster storage',        'NVMe SSD or RAM disk for model weights'),\n    ('Lazy loading',          'Load only the requested layers (attention first, FFN second)'),\n    ('Shared model volumes',  'Multiple containers sharing same model files via NFS/S3FS'),\n]\nfor name, desc in strategies:\n    print(f'  {name:<25} {desc}')\n"],"c07"),
        cell_md(["## 3. Autoscaling Policy\n","\n","A simple reactive autoscaling policy based on concurrency queue depth.\n"],"c08"),
        cell_code(["class InferenceAutoscaler:\n    def __init__(self, min_replicas=1, max_replicas=8,\n                 target_concurrency=8, cold_start_s=30):\n        self.replicas = min_replicas\n        self.min_replicas = min_replicas\n        self.max_replicas = max_replicas\n        self.target_concurrency = target_concurrency\n        self.cold_start_s = cold_start_s\n        self.pending_replicas = 0  # starting up\n        self.scale_events = []\n\n    def tick(self, t, current_concurrency, queue_depth):\n        # Desired replicas based on concurrency\n        desired = max(self.min_replicas,\n                     int(np.ceil(current_concurrency / self.target_concurrency)))\n        # Emergency scale if queue is building\n        if queue_depth > self.target_concurrency * 2:\n            desired = min(self.max_replicas, desired + 1)\n\n        desired = min(desired, self.max_replicas)\n        effective = self.replicas  # cold-start replicas not yet effective\n\n        if desired > effective + self.pending_replicas:\n            added = desired - effective - self.pending_replicas\n            self.pending_replicas += added\n            self.scale_events.append(('scale_up', t, added))\n        elif desired < effective and self.pending_replicas == 0:\n            removed = effective - desired\n            self.replicas -= removed\n            self.scale_events.append(('scale_down', t, removed))\n\n        # Simulate replicas coming online after cold start\n        if self.pending_replicas > 0 and t % int(self.cold_start_s) == 0:\n            came_online = self.pending_replicas\n            self.replicas += came_online\n            self.pending_replicas = 0\n\n        return self.replicas\n\nscaler = InferenceAutoscaler(min_replicas=1, max_replicas=8, target_concurrency=8)\n\nreplica_history = []\nfor t, (conc, qdepth) in enumerate(zip(concurrency_series, queue_depth_series)):\n    r = scaler.tick(t, conc, qdepth)\n    replica_history.append(r)\n\nplt.figure(figsize=(12, 4))\nplt.subplot(2, 1, 1)\nplt.plot(concurrency_series, label='Concurrency')\nplt.plot(queue_depth_series, label='Queue Depth', alpha=0.7)\nplt.legend(); plt.ylabel('Count')\nplt.subplot(2, 1, 2)\nplt.step(range(T), replica_history, where='post', label='Replicas', color='green')\nplt.ylabel('Replica Count'); plt.xlabel('Time')\nplt.legend()\nplt.tight_layout()\nplt.savefig('autoscaler_response.png', dpi=100, bbox_inches='tight')\nplt.show()\nprint(f'Scale events: {scaler.scale_events[:5]}')\n"],"c09"),
        cell_md(["## Experiments: Try These\n",
            "\n",
            "1. **Measure actual cold start**: SSH to spark-01 and time `vllm serve meta-llama/Llama-3.2-1B-Instruct` from cold. How long until the first request is served?\n",
            "2. **Hysteresis**: Add a cooldown period to the autoscaler (don't scale down for N seconds after a scale-up). How does this affect steady-state replica count under bursty traffic?\n",
            "3. **Predictive scaling**: Implement a scaler that reads historical traffic patterns and pre-scales 5 minutes before expected peaks. Test on a synthetic daily pattern.\n"],"c10"),
        cell_md(["## Key Takeaways\n",
            "\n",
            "- Scale on concurrency (in-flight requests) or queue depth, not GPU utilization — GPU is saturated at any non-zero load level.\n",
            "- Cold start latency scales with model size and disk bandwidth — a 70B model takes 60-90 seconds to load, creating a scaling lag.\n",
            "- Model warm pools (idle pre-loaded replicas) are the most reliable cold start mitigation — at the cost of idle GPU spend.\n",
            "- Autoscaling hysteresis prevents thrashing: add a cooldown period and require sustained load before scaling decisions.\n",
            "\n## References\n- *Inference Engineering* Ch 7.2 — Philip Kiely, Baseten Books 2026\n- Kubernetes HPA documentation\n- KEDA (Kubernetes Event-Driven Autoscaling) documentation\n"],"c11"),
    ]
    p = write_nb(21, "autoscaling-concurrency-cold-starts", cells)
    write_readme(21,"Autoscaling: Concurrency, Batching & Cold Starts","Infrastructure",
        "Built an inference autoscaler simulation showing why GPU utilization is a poor scaling signal. Modeled cold start latency for different model sizes and implemented a concurrency-based autoscaling policy with scale event tracking.",
        "GPU utilization is nearly binary for LLM inference — it's high whenever any request is being processed. Concurrency and queue depth are the correct scaling signals.",
        "autoscaling-concurrency-cold-starts","Concurrency vs GPU utilization metrics + autoscaler response simulation",
        "- *Inference Engineering* Ch 7.2 (Philip Kiely, Baseten Books 2026)\n- KEDA autoscaling documentation")
    return validate(p)

def day22():
    cells = [
        cell_md(["# Day 22: Routing, Load Balancing & Queueing\n","> *Inference Engineering* — Chapter 7.2.3 | Philip Kiely, Baseten Books 2026\n","\n**Layer:** Infrastructure | **Prerequisite:** Day 21 (Autoscaling)\n"],"c01"),
        cell_md(["## Concept Overview\n","\n",
            "The load balancer sits in front of GPU workers and decides which worker handles each request. ",
            "Simple round-robin is insufficient for inference: workers have heterogeneous KV cache state, and load varies per request (prefill length matters). ",
            "Least-connections routing sends to the worker with the fewest in-flight requests. ",
            "Cache-aware routing (Day 14) additionally considers prefix cache overlap. ",
            "Priority queuing separates interactive (latency-sensitive) from batch (throughput-optimized) traffic.\n"],"c02"),
        cell_code(["import numpy as np\nimport matplotlib.pyplot as plt\nimport heapq, time\nfrom collections import deque\n\nprint('Load balancing simulation environment')\n"],"c03"),
        cell_md(["## 1. Routing Strategies\n","\n","Compare round-robin, least-connections, and weighted routing.\n"],"c04"),
        cell_code(["class Worker:\n    def __init__(self, worker_id, speed=1.0):\n        self.id = worker_id\n        self.speed = speed\n        self.active_requests = 0\n        self.total_processed = 0\n        self.latencies = []\n\n    def process(self, duration):\n        actual_duration = duration / self.speed\n        self.active_requests += 1\n        # Simulate completion (synchronous for demo)\n        self.active_requests -= 1\n        self.total_processed += 1\n        self.latencies.append(actual_duration)\n        return actual_duration\n\ndef simulate_routing(strategy, num_workers=4, num_requests=1000, seed=42):\n    np.random.seed(seed)\n    # Workers with different speeds (heterogeneous)\n    workers = [Worker(i, speed=1.0 + i*0.1) for i in range(num_workers)]\n    latencies = []\n    rr_counter = 0\n\n    request_durations = np.random.exponential(50, num_requests)  # ms\n\n    for i, dur in enumerate(request_durations):\n        if strategy == 'round_robin':\n            idx = i % num_workers\n        elif strategy == 'least_connections':\n            idx = min(range(num_workers), key=lambda x: workers[x].active_requests)\n        elif strategy == 'random':\n            idx = np.random.randint(num_workers)\n        elif strategy == 'weighted_round_robin':\n            # Route proportionally to worker speed\n            weights = [w.speed for w in workers]\n            idx = np.random.choice(num_workers, p=np.array(weights)/sum(weights))\n\n        # Simulate the request\n        workers[idx].active_requests += 1\n        lat = dur / workers[idx].speed\n        workers[idx].active_requests -= 1\n        workers[idx].total_processed += 1\n        latencies.append(lat)\n\n    return {\n        'mean_lat': np.mean(latencies),\n        'p99_lat': np.percentile(latencies, 99),\n        'max_lat': np.max(latencies),\n        'worker_loads': [w.total_processed for w in workers],\n    }\n\nprint(f'{\"Strategy\":<25} {\"Mean (ms)\":>10} {\"P99 (ms)\":>10} {\"Max (ms)\":>10} {\"Load dist\":>15}')\nprint('-' * 75)\nfor strategy in ['round_robin', 'least_connections', 'random', 'weighted_round_robin']:\n    r = simulate_routing(strategy)\n    load = ','.join(str(l) for l in r['worker_loads'])\n    print(f'{strategy:<25} {r[\"mean_lat\"]:>10.1f} {r[\"p99_lat\"]:>10.1f} {r[\"max_lat\"]:>10.1f} [{load}]')\n"],"c05"),
        cell_md(["## 2. Priority Queuing\n","\n","Separate interactive and batch traffic into different queues to meet latency SLOs for interactive while maximizing throughput for batch.\n"],"c06"),
        cell_code(["class PriorityQueue:\n    def __init__(self):\n        self._queue = []\n        self._count = 0\n\n    def push(self, priority, item):\n        # Lower priority number = higher priority\n        heapq.heappush(self._queue, (priority, self._count, item))\n        self._count += 1\n\n    def pop(self):\n        return heapq.heappop(self._queue)[2]\n\n    def __len__(self):\n        return len(self._queue)\n\ndef simulate_priority_queue(num_requests=500, seed=42):\n    np.random.seed(seed)\n    pq = PriorityQueue()\n    normal_q = deque()\n\n    requests = []\n    for i in range(num_requests):\n        is_interactive = np.random.random() < 0.2  # 20% interactive\n        duration = np.random.exponential(30 if is_interactive else 100)\n        arrival = i * 0.5  # requests arrive 0.5 time units apart\n        requests.append({'id': i, 'interactive': is_interactive,\n                         'duration': duration, 'arrival': arrival})\n        if is_interactive:\n            pq.push(0, requests[-1])  # high priority\n        else:\n            pq.push(1, requests[-1])  # low priority\n\n    # Process with priority ordering\n    interactive_waits = []\n    batch_waits = []\n    t = 0\n    served = 0\n    while len(pq) > 0:\n        req = pq.pop()\n        wait = max(0, t - req['arrival'])\n        if req['interactive']:\n            interactive_waits.append(wait)\n        else:\n            batch_waits.append(wait)\n        t += req['duration'] / 100  # normalize\n        served += 1\n\n    return interactive_waits, batch_waits\n\niwait, bwait = simulate_priority_queue()\nprint(f'Priority Queuing Results:')\nprint(f'  Interactive requests: {len(iwait)}')\nprint(f'    Mean wait: {np.mean(iwait):.2f} | P99 wait: {np.percentile(iwait,99):.2f}')\nprint(f'  Batch requests: {len(bwait)}')\nprint(f'    Mean wait: {np.mean(bwait):.2f} | P99 wait: {np.percentile(bwait,99):.2f}')\nprint(f'  Interactive gets {np.mean(bwait)/np.mean(iwait):.1f}x lower wait time')\n"],"c07"),
        cell_md(["## 3. Queueing Theory: Sizing for SLOs\n","\n","M/M/c queue model: arrival rate $\\lambda$, service rate $\\mu$, $c$ servers. At utilization $\\rho = \\lambda/(c \\cdot \\mu)$, wait time $W \\propto 1/(1-\\rho)$.\n"],"c08"),
        cell_code(["from math import factorial\n\ndef erlang_c(c, rho):\n    \"\"\"Erlang C formula: P(wait > 0) for M/M/c queue.\"\"\"\n    if rho >= 1:\n        return 1.0\n    a = c * rho  # offered load\n    numerator = (a**c / factorial(c)) * (c / (c - a))\n    denominator = sum(a**k / factorial(k) for k in range(c)) + numerator\n    return numerator / denominator\n\ndef mean_wait_mmc(lam, mu, c):\n    \"\"\"Mean waiting time in M/M/c queue.\"\"\"\n    rho = lam / (c * mu)\n    if rho >= 1:\n        return float('inf')\n    Pc = erlang_c(c, rho)\n    W_queue = Pc / (c * mu * (1 - rho))\n    return W_queue * 1000  # ms\n\n# Model an inference cluster\n# mu = 1 req/sec per worker (50ms mean service time)\nmu = 20.0  # requests per second per worker\n\nprint('M/M/c Queueing Analysis: Workers needed for SLO')\nprint(f'{\"Arrival Rate\":>15} {\"Utilization\":>13}', end='')\nfor c in [1, 2, 4, 8]:\n    print(f' {c}w P95<50ms', end='')\nprint()\nprint('-' * 75)\nfor lam in [5, 10, 15, 20, 30, 40, 60]:\n    print(f'{lam:>15.0f} rps {lam/(1*mu):>12.0%}', end='')\n    for c in [1, 2, 4, 8]:\n        w = mean_wait_mmc(lam, mu, c)\n        ok = 'YES' if w < 50 else 'NO'\n        print(f' {ok:>10}', end='')\n    print()\n"],"c09"),
        cell_md(["## Experiments: Try These\n",
            "\n",
            "1. **Power of two choices**: Implement the \"power of two choices\" algorithm (pick 2 random workers, route to the less loaded). Compare to round-robin and least-connections.\n",
            "2. **Sticky sessions**: Add session affinity to the load balancer (same user → same worker). Measure KV cache hit rate improvement.\n",
            "3. **Circuit breaker**: Implement a circuit breaker that stops sending traffic to workers with >5s response times. Test under simulated worker failure.\n"],"c10"),
        cell_md(["## Key Takeaways\n",
            "\n",
            "- Round-robin is wrong for inference: it ignores request duration variability and KV cache state. Least-connections is the minimum viable strategy.\n",
            "- Priority queuing protects interactive traffic from batch workloads — a 20% interactive / 80% batch mix can give interactive 5-10x better latency.\n",
            "- Erlang C queueing models give good intuition for worker sizing: at 80% utilization, wait times are 5x higher than at 50% utilization.\n",
            "- For cache-aware routing, combine least-connections with prefix hash affinity — route to the least-loaded worker that already has the prefix cached.\n",
            "\n## References\n- *Inference Engineering* Ch 7.2.3 — Philip Kiely, Baseten Books 2026\n- Kleinrock, L. (1975), \"Queueing Systems, Volume 1\"\n- NGINX load balancing documentation\n"],"c11"),
    ]
    p = write_nb(22, "routing-load-balancing-queueing", cells)
    write_readme(22,"Routing, Load Balancing & Queueing","Infrastructure",
        "Implemented and benchmarked four routing strategies (round-robin, least-connections, random, weighted), built a priority queue for interactive vs batch traffic, and applied Erlang C queueing theory to size worker pools for SLOs.",
        "At 80% utilization, M/M/c queuing theory predicts 5x higher wait times than at 50%. This is why inference services must overprovision — running at 50-70% target utilization is not waste, it's SLO headroom.",
        "routing-load-balancing-queueing","Routing strategy comparison + Erlang C SLO sizing table",
        "- *Inference Engineering* Ch 7.2.3 (Philip Kiely, Baseten Books 2026)\n- Erlang C queueing formula")
    return validate(p)

def day23():
    cells = [
        cell_md(["# Day 23: Multi-Cloud Capacity Management\n","> *Inference Engineering* — Chapter 7.3 | Philip Kiely, Baseten Books 2026\n","\n**Layer:** Infrastructure | **Prerequisite:** Day 22 (Routing)\n"],"c01"),
        cell_md(["## Concept Overview\n","\n",
            "Multi-cloud capacity management uses multiple cloud providers and on-premises GPU clusters simultaneously, routing traffic to minimize cost and latency while maintaining availability. ",
            "The key challenges are: heterogeneous GPU availability (spot vs reserved vs on-demand), varying latency by region, and cost optimization across providers. ",
            "A capacity manager maintains a model of available capacity across providers and routes requests based on a combination of cost, latency, and SLO requirements. ",
            "On-premises GPUs (like home lab DGX Sparks) provide a cost-efficient base load, with cloud GPUs handling bursts.\n"],"c02"),
        cell_code(["import numpy as np\nimport matplotlib.pyplot as plt\nfrom dataclasses import dataclass\nfrom typing import List, Optional\n\nprint('Multi-cloud capacity management simulation')\n"],"c03"),
        cell_md(["## 1. Capacity Pool Model\n","\n","Model available GPU capacity across multiple providers with different cost and latency profiles.\n"],"c04"),
        cell_code(["@dataclass\nclass CapacityPool:\n    name: str\n    provider: str\n    region: str\n    gpu_type: str\n    num_gpus: int\n    cost_per_gpu_hr: float\n    latency_ms: float  # network latency to this pool\n    availability: float  # 0-1, fraction of time available (spot has lower availability)\n    is_spot: bool\n\npools = [\n    CapacityPool('spark-cluster', 'on-prem', 'local', 'GB10', 2, 0.0, 0.5, 0.99, False),\n    CapacityPool('aws-us-east-1', 'AWS', 'us-east-1', 'H100', 8, 10.0, 25.0, 0.99, False),\n    CapacityPool('aws-spot', 'AWS', 'us-east-1', 'H100', 16, 4.0, 25.0, 0.70, True),\n    CapacityPool('gcp-us-central', 'GCP', 'us-central1', 'H100', 4, 9.5, 30.0, 0.99, False),\n    CapacityPool('azure-eastus', 'Azure', 'eastus', 'H100', 4, 9.8, 22.0, 0.99, False),\n    CapacityPool('lambda-us', 'Lambda', 'us-west-2', 'H100', 8, 7.5, 45.0, 0.95, False),\n]\n\nprint(f'{\"Pool\":<20} {\"Provider\":<10} {\"GPUs\":>6} {\"$/GPU/hr\":>10} {\"Latency\":>10} {\"Avail\":>8} {\"Spot\":>6}')\nprint('-' * 73)\nfor p in pools:\n    print(f'{p.name:<20} {p.provider:<10} {p.num_gpus:>6} {p.cost_per_gpu_hr:>10.1f} {p.latency_ms:>9.1f}ms {p.availability:>8.0%} {str(p.is_spot):>6}')\n\ntotal_gpus = sum(p.num_gpus for p in pools)\nhourly_cost = sum(p.num_gpus * p.cost_per_gpu_hr for p in pools)\nprint(f'\\nTotal GPU capacity: {total_gpus} GPUs')\nprint(f'Hourly cost if all active: ${hourly_cost:.0f}/hr')\n"],"c05"),
        cell_md(["## 2. Multi-Cloud Routing Policy\n","\n","Route requests to the cheapest available pool that meets the latency SLO.\n"],"c06"),
        cell_code(["def route_request(pools, request_type, latency_slo_ms):\n    \"\"\"\n    Route to cheapest available pool meeting the latency SLO.\n    request_type: 'interactive' | 'batch'\n    \"\"\"\n    available = [p for p in pools if p.availability > 0.9 or not p.is_spot]\n    within_slo = [p for p in available if p.latency_ms <= latency_slo_ms]\n\n    if not within_slo:\n        within_slo = available  # relax SLO if nothing available\n\n    if request_type == 'batch':\n        # For batch: minimize cost, tolerate higher latency\n        return min(within_slo, key=lambda p: p.cost_per_gpu_hr)\n    else:\n        # For interactive: minimize latency among affordable pools\n        affordable = [p for p in within_slo if p.cost_per_gpu_hr < 10.0]\n        if affordable:\n            return min(affordable, key=lambda p: p.latency_ms)\n        return min(within_slo, key=lambda p: p.latency_ms)\n\n# Simulate routing decisions\nprint('Routing decisions:')\nfor req_type, slo in [('interactive', 50), ('interactive', 30), ('batch', 1000), ('batch', 200)]:\n    pool = route_request(pools, req_type, slo)\n    print(f'  {req_type:<15} SLO={slo}ms → {pool.name:<20} ({pool.provider}, ${pool.cost_per_gpu_hr}/hr, {pool.latency_ms}ms)')\n"],"c07"),
        cell_md(["## 3. Cost Optimization — On-Prem as Base Load\n","\n","On-premises hardware handles predictable base load; cloud handles bursts. This hybrid strategy minimizes total cost.\n"],"c08"),
        cell_code(["def cost_analysis(traffic_profile, on_prem_gpus=2, on_prem_cost_hr=0):\n    \"\"\"\n    Compare pure-cloud vs hybrid cost for a traffic profile.\n    traffic_profile: list of (hour, requests_per_second)\n\n    Returns (cloud_only_cost, hybrid_cost, savings)\n    \"\"\"\n    # Assume 1 H100 handles 10 req/s for typical LLM\n    rps_per_gpu = 10.0\n    cloud_cost_per_gpu_hr = 10.0  # $/hr for H100\n\n    cloud_only_cost = 0\n    hybrid_cost = 0\n\n    for hour, rps in traffic_profile:\n        gpus_needed = int(np.ceil(rps / rps_per_gpu))\n        # Cloud only\n        cloud_only_cost += gpus_needed * cloud_cost_per_gpu_hr\n        # Hybrid: on-prem handles up to on_prem_gpus worth\n        cloud_gpus = max(0, gpus_needed - on_prem_gpus)\n        hybrid_cost += cloud_gpus * cloud_cost_per_gpu_hr + on_prem_cost_hr\n\n    savings = cloud_only_cost - hybrid_cost\n    return cloud_only_cost, hybrid_cost, savings\n\n# 24-hour traffic profile (requests per second)\nhours = list(range(24))\n# Typical daily pattern: low at night, peaks in morning/evening\nrps_profile = [\n    (0, 5), (1, 3), (2, 2), (3, 2), (4, 3), (5, 5),\n    (6, 10), (7, 18), (8, 25), (9, 28), (10, 30), (11, 32),\n    (12, 35), (13, 33), (14, 30), (15, 28), (16, 30), (17, 32),\n    (18, 38), (19, 35), (20, 28), (21, 22), (22, 15), (23, 8),\n]\n\ncloud_cost, hybrid_cost, savings = cost_analysis(rps_profile, on_prem_gpus=2)\nprint(f'Daily Cost Comparison (2 on-prem GPUs as base load):')\nprint(f'  Cloud-only:  ${cloud_cost:.0f}/day')\nprint(f'  Hybrid:      ${hybrid_cost:.0f}/day')\nprint(f'  Savings:     ${savings:.0f}/day ({savings/cloud_cost*100:.0f}%)')\n\n# Plot traffic and cost\nfig, (ax1, ax2) = plt.subplots(2, 1, figsize=(12, 7), sharex=True)\nrps_vals = [r[1] for r in rps_profile]\ngpus_needed = [int(np.ceil(r/10)) for r in rps_vals]\non_prem_contribution = [min(2, g) for g in gpus_needed]\ncloud_contribution = [max(0, g-2) for g in gpus_needed]\n\nax1.stackplot(hours, on_prem_contribution, cloud_contribution,\n              labels=['On-prem (DGX Spark)', 'Cloud burst'], alpha=0.7)\nax1.set_ylabel('GPUs Active'); ax1.legend()\nax1.set_title('Hybrid Multi-Cloud Capacity: On-Prem Base + Cloud Burst')\n\ncost_cloud = [g * 10 for g in gpus_needed]\ncost_hybrid = [c * 10 for c in cloud_contribution]\nax2.plot(hours, cost_cloud, label='Cloud-only cost', color='red')\nax2.plot(hours, cost_hybrid, label='Hybrid cost', color='green')\nax2.fill_between(hours, cost_hybrid, cost_cloud, alpha=0.3, color='green', label='Savings')\nax2.set_xlabel('Hour of Day'); ax2.set_ylabel('$/hr'); ax2.legend()\nplt.tight_layout()\nplt.savefig('multi_cloud_cost.png', dpi=100, bbox_inches='tight')\nplt.show()\n"],"c09"),
        cell_md(["## Experiments: Try These\n",
            "\n",
            "1. **Spot instance strategy**: Add spot instance interruption simulation. When a spot pool goes down, measure the failover time and traffic loss.\n",
            "2. **Geo-latency routing**: Add user geolocation to requests and route to the nearest pool meeting cost SLO. How much latency does geo-routing save?\n",
            "3. **Capacity reservation optimization**: Given a budget of $X/day, find the optimal mix of reserved vs on-demand vs spot capacity to minimize expected cost.\n"],"c10"),
        cell_md(["## Key Takeaways\n",
            "\n",
            "- Multi-cloud capacity management routes traffic across providers by combining cost, latency, and availability constraints.\n",
            "- On-premises GPUs as base load with cloud burst is the most cost-efficient pattern: predictable workload at zero marginal cost, burst handled by cloud.\n",
            "- Spot instances can cut GPU costs by 60-70% but require fallback to on-demand when interrupted — build bidding and fallback into the capacity manager.\n",
            "- Route interactive traffic by latency, batch traffic by cost — the same model with two queues and two routing policies.\n",
            "\n## References\n- *Inference Engineering* Ch 7.3 — Philip Kiely, Baseten Books 2026\n- AWS EC2 Spot Instances best practices\n- Multi-cloud LLM serving case studies\n"],"c11"),
    ]
    p = write_nb(23, "multi-cloud-capacity", cells)
    write_readme(23,"Multi-Cloud Capacity Management","Infrastructure",
        "Built a multi-cloud capacity model with on-prem, AWS, GCP, Azure, and Lambda pools. Implemented SLO-aware routing policies and demonstrated the hybrid on-prem/cloud cost optimization strategy on a realistic 24-hour traffic profile.",
        "On-prem GPUs as base load with cloud bursting is ~40-60% cheaper than pure cloud for predictable workloads — the key insight is that hardware amortization breaks the linear cost scaling of pure cloud.",
        "multi-cloud-capacity","Hybrid on-prem/cloud capacity allocation + daily cost comparison",
        "- *Inference Engineering* Ch 7.3 (Philip Kiely, Baseten Books 2026)\n- Multi-cloud inference routing strategies")
    return validate(p)

def day24():
    cells = [
        cell_md(["# Day 24: Zero-Downtime Deployment & Cost Estimation\n","> *Inference Engineering* — Chapter 7.4 | Philip Kiely, Baseten Books 2026\n","\n**Layer:** Infrastructure | **Prerequisite:** Day 23 (Multi-Cloud), Day 21 (Autoscaling)\n"],"c01"),
        cell_md(["## Concept Overview\n","\n",
            "Zero-downtime deployment is required for production inference services — users should not experience errors or elevated latency during model updates. ",
            "Blue-green deployment maintains two identical environments (blue=current, green=new), switches traffic atomically after validation. ",
            "Canary deployment gradually shifts traffic from old to new version, monitoring error rates and latency. ",
            "Cost estimation is the other half of production operations: accurate $/token modeling enables pricing decisions and capacity planning.\n"],"c02"),
        cell_code(["import numpy as np\nimport matplotlib.pyplot as plt\nimport time\nfrom dataclasses import dataclass\nfrom typing import Dict, List\n\nprint('Zero-downtime deployment and cost estimation demo')\n"],"c03"),
        cell_md(["## 1. Blue-Green Deployment\n","\n","Two environments: blue (current) and green (new). Traffic switches atomically after health checks pass.\n"],"c04"),
        cell_code(["@dataclass\nclass DeploymentEnvironment:\n    name: str\n    version: str\n    health: float  # 0-1\n    latency_p99_ms: float\n    error_rate: float  # 0-1\n    active_requests: int = 0\n\nclass BlueGreenDeployment:\n    def __init__(self):\n        self.blue = DeploymentEnvironment('blue', 'v1.0', 1.0, 120, 0.001)\n        self.green = DeploymentEnvironment('green', 'v2.0', 1.0, 110, 0.001)\n        self.active = 'blue'\n        self.traffic_to_green = 0.0\n        self.events = []\n\n    def deploy_new_version(self, new_version):\n        self.green.version = new_version\n        self.green.health = 0.0  # starting up\n        self.events.append(f'[t=0] Deploying {new_version} to green')\n\n    def warmup_green(self, t):\n        \"\"\"Simulate green environment warming up.\"\"\"\n        self.green.health = min(1.0, t / 30.0)  # 30s warmup\n        if self.green.health >= 1.0 and self.traffic_to_green == 0:\n            self.events.append(f'[t={t:.0f}s] Green healthy, starting traffic shift')\n\n    def run_health_checks(self):\n        \"\"\"Validate green before full cutover.\"\"\"\n        checks = [\n            ('HTTP health endpoint', self.green.health > 0.9),\n            ('Error rate < 1%', self.green.error_rate < 0.01),\n            ('P99 latency < 200ms', self.green.latency_p99_ms < 200),\n            ('GPU memory stable', True),\n        ]\n        all_pass = all(v for _, v in checks)\n        print('Green environment health checks:')\n        for name, passed in checks:\n            status = '✓ PASS' if passed else '✗ FAIL'\n            print(f'  {name:<35} {status}')\n        return all_pass\n\n    def switch_traffic(self, fraction):\n        \"\"\"Gradually shift traffic to green.\"\"\"\n        self.traffic_to_green = fraction\n        self.events.append(f'Traffic to green: {fraction:.0%}')\n\n    def full_cutover(self):\n        self.active = 'green'\n        self.traffic_to_green = 1.0\n        self.events.append('Full cutover to green complete')\n\ndeploy = BlueGreenDeployment()\ndeploy.deploy_new_version('v2.1')\n\n# Simulate warmup\nfor t in [5, 10, 20, 30, 35]:\n    deploy.warmup_green(t)\n\npassed = deploy.run_health_checks()\nif passed:\n    print('\\nCanary rollout:')\n    for frac in [0.05, 0.10, 0.25, 0.50, 1.0]:\n        deploy.switch_traffic(frac)\n        print(f'  {frac:.0%} traffic to green (v2.1)')\n    deploy.full_cutover()\n\nprint('\\nDeployment event log:')\nfor event in deploy.events:\n    print(f'  {event}')\n"],"c05"),
        cell_md(["## 2. Canary Deployment with Error Rate Monitoring\n","\n","Canary releases gradually shift traffic while monitoring error rates and latency to catch regressions early.\n"],"c06"),
        cell_code(["class CanaryDeployment:\n    def __init__(self, error_threshold=0.01, latency_threshold_ms=200):\n        self.canary_traffic = 0.0\n        self.error_threshold = error_threshold\n        self.latency_threshold_ms = latency_threshold_ms\n        self.metrics_history = []\n\n    def simulate_step(self, canary_pct, inject_error=False):\n        \"\"\"Simulate one monitoring window.\"\"\"\n        # Baseline error rate ~0.1%, canary might have regression\n        baseline_err = np.random.beta(1, 999)\n        canary_err = np.random.beta(3, 97) if inject_error else np.random.beta(1, 999)\n        # Blended metrics\n        blended_err = (1 - canary_pct) * baseline_err + canary_pct * canary_err\n        canary_latency = 115 + (50 if inject_error else 0) + np.random.normal(0, 5)\n        self.metrics_history.append({\n            'canary_pct': canary_pct,\n            'error_rate': blended_err,\n            'canary_latency': canary_latency,\n        })\n        return blended_err < self.error_threshold and canary_latency < self.latency_threshold_ms\n\n# Normal rollout\ncanary = CanaryDeployment()\nprint('Normal canary rollout (no regression):')\nfor pct in [0.01, 0.05, 0.1, 0.25, 0.5, 1.0]:\n    ok = canary.simulate_step(pct)\n    m = canary.metrics_history[-1]\n    print(f'  {pct:>5.0%} → err={m[\"error_rate\"]*100:.2f}% lat={m[\"canary_latency\"]:.0f}ms {\"OK\" if ok else \"ROLLBACK\"}')\n\n# Rollout with regression at 10%\ncanary2 = CanaryDeployment()\nprint('\\nCanary rollout with regression injected at 10%:')\nfor i, pct in enumerate([0.01, 0.05, 0.1, 0.25, 0.5, 1.0]):\n    inject = i >= 2  # inject regression from 10% onwards\n    ok = canary2.simulate_step(pct, inject_error=inject)\n    m = canary2.metrics_history[-1]\n    status = 'OK' if ok else 'ROLLBACK!'\n    print(f'  {pct:>5.0%} → err={m[\"error_rate\"]*100:.2f}% lat={m[\"canary_latency\"]:.0f}ms {status}')\n    if not ok:\n        print('  ⚠ Rolling back to stable version')\n        break\n"],"c07"),
        cell_md(["## 3. Cost per Token Estimation\n","\n","The fundamental unit cost for an inference service: $/token, factoring in GPU cost, utilization, and throughput.\n"],"c08"),
        cell_code(["def cost_per_token(gpu_cost_hr, gpu_utilization, tokens_per_second_per_gpu,\n                  overhead_pct=0.3):\n    \"\"\"\n    Compute cost per 1M tokens.\n    overhead_pct: fraction for storage, network, CPU, etc.\n    \"\"\"\n    effective_tps = tokens_per_second_per_gpu * gpu_utilization\n    tokens_per_hour = effective_tps * 3600\n    cost_per_token = gpu_cost_hr / tokens_per_hour\n    total_with_overhead = cost_per_token * (1 + overhead_pct)\n    return total_with_overhead * 1e6  # per 1M tokens\n\nscenarios = [\n    # (label, gpu_cost_hr, tps_per_gpu, utilization)\n    ('A100 on AWS (on-demand)', 3.5, 500, 0.7),\n    ('H100 on AWS (on-demand)', 10.0, 2000, 0.7),\n    ('H100 on AWS (reserved)', 7.0, 2000, 0.8),\n    ('DGX Spark (on-prem, amortized)', 0.5, 300, 0.8),  # ~$50/day amortized\n    ('H100 on Lambda Labs', 7.5, 2000, 0.75),\n    ('B200 on AWS (projected)', 18.0, 5000, 0.75),\n]\n\nprint(f'{\"Scenario\":<40} {\"$/1M tokens\":>12}')\nprint('-' * 55)\nfor label, cost_hr, tps, util in scenarios:\n    cost = cost_per_token(cost_hr, util, tps)\n    print(f'{label:<40} ${cost:>11.3f}')\n\nprint('\\nFor context:')\nprint('  OpenAI GPT-4o:  ~$5/1M input, $15/1M output')\nprint('  Claude 3.5 Sonnet: ~$3/1M input, $15/1M output')\nprint('  Self-hosted 70B: $0.5-2/1M tokens')\n"],"c09"),
        cell_md(["## Experiments: Try These\n",
            "\n",
            "1. **Implement a health check**: Write a `/health` endpoint for a vLLM server that checks GPU memory, queue depth, and model loaded status. Return 503 if unhealthy.\n",
            "2. **Cost dashboard**: Build a cost tracking tool that logs tokens processed per minute and accumulates $/token in real time.\n",
            "3. **Shadow deployment**: Implement a shadow deployment where 100% of traffic goes to blue but a copy goes to green (for testing). Compare response differences.\n"],"c10"),
        cell_md(["## Key Takeaways\n",
            "\n",
            "- Blue-green deployment maintains two environments, switches traffic atomically after green passes all health checks — zero request failure during update.\n",
            "- Canary deployment gradually shifts traffic while monitoring error rate and latency — automatically rolls back on regression, stopping impact at the canary percentage.\n",
            "- Cost per token = GPU cost / (tokens per second × utilization × 3600) × overhead — on-prem at 80% utilization can be 3-5x cheaper than cloud at 70%.\n",
            "- Infrastructure-layer decisions (MIG, colocation, reserved capacity) have a larger impact on $/token than optimization-layer decisions (quantization, batching) at scale.\n",
            "\n## References\n- *Inference Engineering* Ch 7.4 — Philip Kiely, Baseten Books 2026\n- Martin Fowler, \"BlueGreenDeployment\" (martinfowler.com)\n- Google SRE Book, Chapter 27: Reliable Product Launches\n"],"c11"),
    ]
    p = write_nb(24, "zero-downtime-deployment-cost", cells)
    write_readme(24,"Zero-Downtime Deployment & Cost Estimation","Infrastructure",
        "Implemented blue-green and canary deployment strategies with health checks and automatic rollback on regression. Built a $/token cost model across GPU types and providers, showing how utilization and amortization dominate the cost equation.",
        "Canary deployment with automatic rollback is essential: at 1% canary traffic, a regression only affects 1% of users — roll back the instant error rate exceeds threshold, with total impact limited.",
        "zero-downtime-deployment-cost","Canary rollout with regression detection + $/1M token cost comparison",
        "- *Inference Engineering* Ch 7.4 (Philip Kiely, Baseten Books 2026)\n- Google SRE Book, Chapter 27")
    return validate(p)

def day25():
    cells = [
        cell_md(["# Day 25: Performance Benchmarking — Tooling & Profiling\n","> *Inference Engineering* — Chapter 4.5 | Philip Kiely, Baseten Books 2026\n","\n**Layer:** Tooling | **Prerequisite:** Phase 1 (Runtime basics)\n"],"c01"),
        cell_md(["## Concept Overview\n","\n",
            "Benchmarking inference systems requires measuring the right metrics: TTFT (Time to First Token), TPOT (Time Per Output Token), and throughput (tokens/sec). ",
            "Standard tools: `vllm bench` for end-to-end server benchmarking, `torch.profiler` for kernel-level profiling, `nsys` (Nsight Systems) for timeline profiling. ",
            "The key benchmarking mistakes: single-threaded benchmarks miss batching effects, warm-up is essential (first requests are slow), and prompt length distributions must match production traffic.\n"],"c02"),
        cell_code(["import torch\nimport numpy as np\nimport matplotlib.pyplot as plt\nimport time\nfrom contextlib import contextmanager\n\nprint(f'CUDA available: {torch.cuda.is_available()}')\nif torch.cuda.is_available():\n    print(f'GPU: {torch.cuda.get_device_name(0)}')\n"],"c03"),
        cell_md(["## 1. Core Metrics: TTFT, TPOT, Throughput\n","\n","Measuring the three key inference latency metrics with a simulated streaming client.\n"],"c04"),
        cell_code(["@contextmanager\ndef cuda_timer(label=''):\n    if torch.cuda.is_available():\n        start = torch.cuda.Event(enable_timing=True)\n        end = torch.cuda.Event(enable_timing=True)\n        start.record()\n        yield\n        end.record()\n        torch.cuda.synchronize()\n        elapsed = start.elapsed_time(end)\n        print(f'{label}: {elapsed:.3f} ms')\n    else:\n        t0 = time.perf_counter()\n        yield\n        print(f'{label}: {(time.perf_counter()-t0)*1000:.3f} ms')\n\ndef simulate_streaming_inference(prompt_tokens, output_tokens, ttft_ms=50, tpot_ms=20):\n    \"\"\"Simulate a streaming inference call and measure metrics.\"\"\"\n    tokens_received = []\n    timestamps = []\n\n    # Prefill (TTFT)\n    time.sleep(ttft_ms / 1000)\n    first_token_time = time.perf_counter()\n    tokens_received.append(1)\n    timestamps.append(first_token_time)\n\n    # Decode (TPOT)\n    start = time.perf_counter()\n    for i in range(output_tokens - 1):\n        time.sleep(tpot_ms / 1000)\n        tokens_received.append(i + 2)\n        timestamps.append(time.perf_counter())\n\n    total_time = timestamps[-1] - (timestamps[0] - ttft_ms/1000)\n\n    return {\n        'ttft_ms': ttft_ms,\n        'tpot_ms': tpot_ms,\n        'total_tokens': output_tokens,\n        'total_time_ms': total_time * 1000,\n        'throughput_tps': output_tokens / total_time,\n    }\n\n# Benchmark different configurations\nprint('Simulated inference metrics:')\nprint(f'{\"Config\":<35} {\"TTFT (ms)\":>10} {\"TPOT (ms)\":>10} {\"Throughput\":>12}')\nfor (prompt, output, ttft, tpot) in [\n    (256, 100, 50, 20),\n    (1024, 100, 150, 20),\n    (256, 500, 50, 20),\n    (256, 100, 50, 5),\n]:\n    m = simulate_streaming_inference(prompt, output, ttft, tpot)\n    label = f'p={prompt} o={output} ttft={ttft}ms tpot={tpot}ms'\n    print(f'{label:<35} {m[\"ttft_ms\"]:>10.0f} {m[\"tpot_ms\"]:>10.0f} {m[\"throughput_tps\"]:>11.1f}t/s')\n"],"c05"),
        cell_md(["## 2. torch.profiler Usage\n","\n","PyTorch profiler traces kernel execution, memory allocation, and CPU→GPU synchronization.\n"],"c06"),
        cell_code(["device = 'cuda' if torch.cuda.is_available() else 'cpu'\n\ndef workload_to_profile(device):\n    torch.manual_seed(42)\n    x = torch.randn(32, 512, dtype=torch.float16, device=device)\n    W1 = torch.randn(512, 2048, dtype=torch.float16, device=device)\n    W2 = torch.randn(2048, 512, dtype=torch.float16, device=device)\n    h = torch.nn.functional.gelu(x @ W1)\n    return h @ W2\n\n# Run with profiler\nwith torch.profiler.profile(\n    activities=[torch.profiler.ProfilerActivity.CPU,\n                torch.profiler.ProfilerActivity.CUDA] if device == 'cuda'\n               else [torch.profiler.ProfilerActivity.CPU],\n    record_shapes=True,\n    with_flops=True,\n) as prof:\n    for _ in range(10):\n        _ = workload_to_profile(device)\n\nprint('Top CUDA kernels by self time:')\nprint(prof.key_averages().table(sort_by='cuda_time_total' if device == 'cuda' else 'cpu_time_total',\n                                 row_limit=8))\n"],"c07"),
        cell_md(["## 3. Throughput vs Latency Tradeoff\n","\n","Batching improves throughput but increases latency. Finding the Pareto-optimal operating point.\n"],"c08"),
        cell_code(["def benchmark_batch_sweep(device, d_model=4096, d_ff=14336, max_batch=64):\n    \"\"\"Measure throughput and latency vs batch size.\"\"\"\n    results = []\n    W = torch.randn(d_ff, d_model, device=device, dtype=torch.float16)\n    W2 = torch.randn(d_model, d_ff, device=device, dtype=torch.float16)\n\n    for batch in [1, 2, 4, 8, 16, 32, 64]:\n        x = torch.randn(batch, d_model, device=device, dtype=torch.float16)\n        # Warmup\n        for _ in range(5):\n            torch.nn.functional.gelu(x @ W.T) @ W2.T\n        if device == 'cuda': torch.cuda.synchronize()\n\n        t0 = time.perf_counter()\n        for _ in range(50):\n            h = torch.nn.functional.gelu(x @ W.T)\n            out = h @ W2.T\n        if device == 'cuda': torch.cuda.synchronize()\n        t1 = time.perf_counter()\n\n        latency_ms = (t1 - t0) / 50 * 1000\n        throughput = batch / latency_ms * 1000  # tokens/sec\n        results.append({'batch': batch, 'latency_ms': latency_ms, 'throughput_tps': throughput})\n\n    return results\n\ndevice = 'cuda' if torch.cuda.is_available() else 'cpu'\nresults = benchmark_batch_sweep(device)\n\nfig, (ax1, ax2) = plt.subplots(1, 2, figsize=(12, 5))\nbatches = [r['batch'] for r in results]\nlats = [r['latency_ms'] for r in results]\nthrpts = [r['throughput_tps'] for r in results]\n\nax1.semilogx(batches, lats, 'b-o')\nax1.set_xlabel('Batch Size'); ax1.set_ylabel('Latency (ms)')\nax1.set_title('Latency vs Batch Size')\nax1.grid(True)\n\nax2.semilogx(batches, thrpts, 'r-o')\nax2.set_xlabel('Batch Size'); ax2.set_ylabel('Throughput (tokens/sec)')\nax2.set_title('Throughput vs Batch Size')\nax2.grid(True)\n\nplt.tight_layout()\nplt.savefig('throughput_latency_tradeoff.png', dpi=100, bbox_inches='tight')\nplt.show()\nprint(f'{\"Batch\":>8} {\"Latency (ms)\":>15} {\"Throughput (t/s)\":>18}')\nfor r in results:\n    print(f'{r[\"batch\"]:>8} {r[\"latency_ms\"]:>15.3f} {r[\"throughput_tps\"]:>18.1f}')\n"],"c09"),
        cell_md(["## Experiments: Try These\n",
            "\n",
            "1. **vllm bench**: Run `python -m vllm.entrypoints.openai.api_server` and use `python benchmarks/benchmark_serving.py` with varying concurrency. Plot TTFT and throughput.\n",
            "2. **Profiler visualization**: Export profiler traces to Chrome trace format (`prof.export_chrome_trace`) and view in `chrome://tracing`.\n",
            "3. **Warm-up effect**: Benchmark a model with and without warmup requests. How many requests does it take to reach steady-state performance?\n"],"c10"),
        cell_md(["## Key Takeaways\n",
            "\n",
            "- TTFT (prefill latency) and TPOT (decode latency per token) are the two fundamental latency metrics for LLM inference — they have different sensitivity to batch size.\n",
            "- torch.profiler reveals kernel-level timing: use it to identify whether compute, memory bandwidth, or launch overhead is the bottleneck.\n",
            "- Throughput and latency are in fundamental tension: larger batches improve throughput by amortizing weight loads but increase per-request latency.\n",
            "- Always warm up before benchmarking — the first N requests trigger JIT compilation, cache misses, and other one-time costs that distort measurements.\n",
            "\n## References\n- *Inference Engineering* Ch 4.5 — Philip Kiely, Baseten Books 2026\n- vLLM benchmarking documentation\n- PyTorch Profiler documentation\n"],"c11"),
    ]
    p = write_nb(25, "performance-benchmarking", cells)
    write_readme(25,"Performance Benchmarking: Tooling & Profiling","Tooling",
        "Built a benchmarking toolkit covering TTFT/TPOT measurement, torch.profiler kernel tracing, and throughput-latency tradeoff analysis across batch sizes. Demonstrated why warm-up is essential and how batch size sweeps reveal the GPU's performance envelope.",
        "The throughput-latency Pareto curve is the fundamental tool for capacity planning: find the batch size where throughput flattens (memory-bound plateau) and latency begins to grow — that's your operating point.",
        "performance-benchmarking","Latency and throughput vs batch size curves + torch.profiler output",
        "- *Inference Engineering* Ch 4.5 (Philip Kiely, Baseten Books 2026)\n- PyTorch Profiler documentation")
    return validate(p)

def day26():
    cells = [
        cell_md(["# Day 26: Observability — Metrics, Tracing & Dashboards\n","> *Inference Engineering* — Chapter 7.4.3 | Philip Kiely, Baseten Books 2026\n","\n**Layer:** Tooling | **Prerequisite:** Day 25 (Benchmarking)\n"],"c01"),
        cell_md(["## Concept Overview\n","\n",
            "Observability for inference systems requires three pillars: metrics (aggregated time-series), traces (per-request latency breakdown), and logs. ",
            "Key metrics: TTFT, TPOT, token throughput, GPU utilization, KV cache utilization, queue depth, and error rate. ",
            "Distributed tracing tracks a single request through the entire stack: from API gateway → scheduler → prefill GPU → decode GPU → response. ",
            "Prometheus collects metrics; Grafana visualizes them. OpenTelemetry provides a standard SDK for trace instrumentation.\n"],"c02"),
        cell_code(["import time, random\nimport numpy as np\nimport matplotlib.pyplot as plt\nfrom dataclasses import dataclass, field\nfrom typing import Optional, Dict, List\nfrom collections import defaultdict\n\nprint('Observability stack simulation')\n"],"c03"),
        cell_md(["## 1. Prometheus Metrics\n","\n","The canonical inference server metrics exposed in Prometheus format.\n"],"c04"),
        cell_code(["class InferenceMetrics:\n    \"\"\"Simulates a Prometheus metrics registry for an inference server.\"\"\"\n    def __init__(self):\n        self.request_count = 0\n        self.error_count = 0\n        self.ttft_histogram = []\n        self.tpot_histogram = []\n        self.token_count = 0\n        self.gpu_util = []\n        self.kv_cache_util = []\n        self.queue_depth = []\n\n    def record_request(self, ttft_ms, tpot_ms, num_output_tokens,\n                       success=True, gpu_util=0.8, kv_util=0.6, queue=2):\n        self.request_count += 1\n        if not success:\n            self.error_count += 1\n        self.ttft_histogram.append(ttft_ms)\n        self.tpot_histogram.append(tpot_ms)\n        self.token_count += num_output_tokens\n        self.gpu_util.append(gpu_util)\n        self.kv_cache_util.append(kv_util)\n        self.queue_depth.append(queue)\n\n    def prometheus_export(self):\n        lines = [\n            f'# HELP inference_requests_total Total inference requests',\n            f'inference_requests_total {self.request_count}',\n            f'# HELP inference_errors_total Total inference errors',\n            f'inference_errors_total {self.error_count}',\n            f'# HELP inference_ttft_milliseconds P50/P95/P99 TTFT',\n        ]\n        if self.ttft_histogram:\n            for p in [50, 95, 99]:\n                v = np.percentile(self.ttft_histogram, p)\n                lines.append(f'inference_ttft_milliseconds{{quantile=\"{p/100}\"}} {v:.2f}')\n        lines += [\n            f'inference_tokens_total {self.token_count}',\n            f'inference_gpu_utilization {np.mean(self.gpu_util):.3f}',\n            f'inference_kv_cache_utilization {np.mean(self.kv_cache_util):.3f}',\n            f'inference_queue_depth {self.queue_depth[-1] if self.queue_depth else 0}',\n        ]\n        return '\\n'.join(lines)\n\n# Simulate traffic\nmetrics = InferenceMetrics()\nnp.random.seed(42)\nfor i in range(500):\n    metrics.record_request(\n        ttft_ms=np.random.lognormal(4.0, 0.3),\n        tpot_ms=np.random.lognormal(3.0, 0.2),\n        num_output_tokens=np.random.randint(50, 500),\n        success=np.random.random() > 0.005,\n        gpu_util=np.clip(np.random.normal(0.78, 0.05), 0, 1),\n        kv_util=np.clip(np.random.normal(0.65, 0.1), 0, 1),\n        queue=np.random.poisson(3),\n    )\n\nprint('/metrics endpoint output (Prometheus format):')\nprint(metrics.prometheus_export())\n"],"c05"),
        cell_md(["## 2. Distributed Tracing\n","\n","Each request gets a trace with child spans for each processing stage. ",
            "Trace data reveals where time is spent: is TTFT dominated by queue wait, network, or actual prefill?\n"],"c06"),
        cell_code(["import uuid\n\n@dataclass\nclass Span:\n    trace_id: str\n    span_id: str\n    parent_id: Optional[str]\n    operation: str\n    start_ms: float\n    end_ms: float\n    tags: Dict = field(default_factory=dict)\n\n    @property\n    def duration_ms(self):\n        return self.end_ms - self.start_ms\n\nclass Tracer:\n    def __init__(self):\n        self.traces = defaultdict(list)\n\n    def trace_request(self, prompt_len, output_len):\n        trace_id = str(uuid.uuid4())[:8]\n        t = 0\n        spans = []\n\n        def add_span(op, duration, parent_id=None, **tags):\n            nonlocal t\n            span = Span(trace_id, str(uuid.uuid4())[:8], parent_id, op, t, t+duration, tags)\n            spans.append(span)\n            t += duration\n            return span.span_id\n\n        # Request lifecycle\n        root_id = add_span('inference_request', 0)\n        add_span('network_ingress', np.random.normal(2, 0.5), root_id)\n        add_span('tokenization', np.random.normal(1, 0.2), root_id, prompt_len=prompt_len)\n        add_span('queue_wait', np.random.exponential(5), root_id)\n        add_span('prefill', np.random.normal(prompt_len * 0.1, 5), root_id,\n                prompt_len=prompt_len, batch_size=4)\n        decode_id = add_span('decode', output_len * np.random.normal(20, 2), root_id,\n                           output_tokens=output_len)\n        add_span('detokenization', np.random.normal(0.5, 0.1), root_id)\n        add_span('network_egress', np.random.normal(1, 0.3), root_id)\n\n        self.traces[trace_id] = spans\n        return trace_id, spans\n\ntracer = Tracer()\ntid, spans = tracer.trace_request(prompt_len=256, output_len=100)\n\nprint(f'Trace {tid}:')\nprint(f'{\"Operation\":<20} {\"Start (ms)\":>12} {\"Duration (ms)\":>15}')\nfor span in spans:\n    print(f'{span.operation:<20} {span.start_ms:>12.1f} {span.duration_ms:>15.1f}')\nprint(f'Total: {spans[-1].end_ms:.1f} ms')\n"],"c07"),
        cell_md(["## 3. Grafana Dashboard Layout\n","\n","Standard inference dashboard panels and their alerting thresholds.\n"],"c08"),
        cell_code(["# Simulate and visualize key dashboard panels\nnp.random.seed(42)\nT = 60  # minutes\ntime_axis = np.arange(T)\n\n# Simulate metrics over time\nttft_p99 = np.random.lognormal(4.5, 0.2, T) * (1 + 0.3 * np.sin(time_axis/10))\nthroughput = 200 + 100 * np.sin(time_axis/15) + np.random.normal(0, 10, T)\nkv_util = np.clip(0.5 + 0.2 * np.sin(time_axis/12) + np.random.normal(0, 0.05, T), 0, 1)\nqueue_depth = np.maximum(0, np.random.poisson(3, T) * (1 + np.sin(time_axis/8)))\nerror_rate = np.clip(np.random.exponential(0.001, T), 0, 0.05)\n\nfig, axes = plt.subplots(2, 3, figsize=(15, 8))\n\nfor ax, data, title, ylabel, threshold, threshold_label in [\n    (axes[0,0], ttft_p99, 'TTFT P99 (ms)', 'ms', 500, 'SLO 500ms'),\n    (axes[0,1], throughput, 'Token Throughput', 'tokens/s', None, None),\n    (axes[0,2], kv_util*100, 'KV Cache Utilization', '%', 90, 'Alert 90%'),\n    (axes[1,0], queue_depth, 'Queue Depth', 'requests', 10, 'Alert 10'),\n    (axes[1,1], error_rate*100, 'Error Rate', '%', 1, 'Alert 1%'),\n]:\n    ax.plot(time_axis, data)\n    ax.set_title(title)\n    ax.set_ylabel(ylabel)\n    ax.set_xlabel('Time (min)')\n    if threshold:\n        ax.axhline(threshold, color='red', linestyle='--', alpha=0.7, label=threshold_label)\n        ax.legend(fontsize=8)\n    ax.grid(True, alpha=0.3)\n\n# GPU utilization heatmap\nax = axes[1,2]\ngpu_heatmap = np.random.beta(8, 2, (4, T))  # 4 GPUs over time\nim = ax.imshow(gpu_heatmap, aspect='auto', vmin=0, vmax=1, cmap='RdYlGn')\nplt.colorbar(im, ax=ax)\nax.set_title('GPU Utilization (4 workers)')\nax.set_xlabel('Time (min)')\nax.set_yticks(range(4))\nax.set_yticklabels([f'GPU {i}' for i in range(4)])\n\nplt.suptitle('Inference Service Grafana Dashboard', fontsize=14, fontweight='bold')\nplt.tight_layout()\nplt.savefig('grafana_dashboard.png', dpi=100, bbox_inches='tight')\nplt.show()\nprint('Saved grafana_dashboard.png')\n"],"c09"),
        cell_md(["## Experiments: Try These\n",
            "\n",
            "1. **Real Prometheus**: Add a Prometheus `/metrics` endpoint to a vLLM server. Configure Prometheus to scrape it and Grafana to visualize it.\n",
            "2. **OpenTelemetry SDK**: Instrument a FastAPI wrapper with OpenTelemetry traces. Export to Jaeger (run with Docker) and view the waterfall trace.\n",
            "3. **Alert rule**: Write a Prometheus alerting rule: fire PagerDuty if P95 TTFT > 500ms for 5 consecutive minutes.\n"],"c10"),
        cell_md(["## Key Takeaways\n",
            "\n",
            "- The three observability pillars for inference: metrics (aggregate trends), traces (per-request breakdown), and logs (event context).\n",
            "- Key Prometheus metrics: TTFT histogram, TPOT histogram, KV cache utilization, queue depth, GPU utilization, error rate.\n",
            "- Distributed tracing reveals hidden latency: queue wait time often dominates TTFT during traffic spikes, masking actual prefill time improvements.\n",
            "- KV cache utilization is the most actionable metric: if it's consistently >90%, you're OOMing and need more GPUs or shorter max sequences.\n",
            "\n## References\n- *Inference Engineering* Ch 7.4.3 — Philip Kiely, Baseten Books 2026\n- Prometheus documentation\n- OpenTelemetry specification\n- Grafana dashboard best practices\n"],"c11"),
    ]
    p = write_nb(26, "observability-metrics-tracing", cells)
    write_readme(26,"Observability: Metrics, Tracing & Dashboards","Tooling",
        "Built a Prometheus-style metrics registry for an inference server, implemented distributed tracing for a full request lifecycle, and designed a Grafana dashboard with alerting thresholds for all key inference metrics.",
        "Distributed traces reveal that queue wait time — not actual prefill compute — often dominates TTFT during traffic spikes. Without tracing, you'd optimize the wrong thing.",
        "observability-metrics-tracing","Grafana dashboard simulation + distributed trace waterfall visualization",
        "- *Inference Engineering* Ch 7.4.3 (Philip Kiely, Baseten Books 2026)\n- Prometheus documentation\n- OpenTelemetry specification")
    return validate(p)

def day27():
    cells = [
        cell_md(["# Day 27: Client Code — Streaming, Async & Protocol Support\n","> *Inference Engineering* — Chapter 7.5 | Philip Kiely, Baseten Books 2026\n","\n**Layer:** Tooling | **Prerequisite:** Day 26 (Observability)\n"],"c01"),
        cell_md(["## Concept Overview\n","\n",
            "Production inference clients require async I/O, streaming response handling, and robust error recovery. ",
            "Streaming uses Server-Sent Events (SSE) over HTTP — the server sends token chunks as they're generated, reducing perceived latency for users. ",
            "Async clients (asyncio + aiohttp) enable high concurrency from a single process without threading overhead. ",
            "The OpenAI API is the de facto standard: all major inference servers (vLLM, SGLang, TGI) expose compatible endpoints.\n"],"c02"),
        cell_code(["import asyncio\nimport time\nimport json\nimport numpy as np\nimport matplotlib.pyplot as plt\nfrom typing import AsyncIterator, Iterator\n\nprint('Client code patterns for inference')\n"],"c03"),
        cell_md(["## 1. Streaming with Server-Sent Events (SSE)\n","\n","SSE streams token deltas as they're generated. The client receives partial responses and can display them immediately.\n"],"c04"),
        cell_code(["def simulate_sse_stream(prompt: str, max_tokens: int = 100,\n                       ttft_ms: float = 50, tpot_ms: float = 20) -> Iterator[str]:\n    \"\"\"\n    Simulate an SSE stream from an OpenAI-compatible server.\n    Yields JSON chunks like the real API.\n    \"\"\"\n    tokens = [f'token_{i}' for i in range(max_tokens)]\n    request_id = 'chatcmpl-abc123'\n    model = 'llama-3-8b'\n\n    # First token has TTFT latency\n    time.sleep(ttft_ms / 1000)\n    yield json.dumps({\n        'id': request_id, 'model': model,\n        'choices': [{'delta': {'role': 'assistant', 'content': tokens[0]}, 'finish_reason': None}]\n    })\n\n    for token in tokens[1:]:\n        time.sleep(tpot_ms / 1000)\n        yield json.dumps({\n            'id': request_id, 'model': model,\n            'choices': [{'delta': {'content': ' ' + token}, 'finish_reason': None}]\n        })\n\n    yield json.dumps({\n        'id': request_id, 'model': model,\n        'choices': [{'delta': {}, 'finish_reason': 'stop'}]\n    })\n    yield '[DONE]'\n\n# Client that measures streaming metrics\ndef streaming_client(prompt: str, max_tokens: int = 20):\n    collected = []\n    first_token_time = None\n    start = time.perf_counter()\n    token_times = []\n\n    for chunk in simulate_sse_stream(prompt, max_tokens):\n        now = time.perf_counter()\n        if chunk == '[DONE]':\n            break\n        data = json.loads(chunk)\n        delta = data['choices'][0]['delta'].get('content', '')\n        if delta:\n            if first_token_time is None:\n                first_token_time = now\n            token_times.append(now)\n            collected.append(delta)\n\n    total_time = time.perf_counter() - start\n    ttft = (first_token_time - start) * 1000 if first_token_time else 0\n    tpot = np.mean(np.diff([t * 1000 for t in token_times])) if len(token_times) > 1 else 0\n\n    print(f'Streaming client metrics:')\n    print(f'  TTFT: {ttft:.1f} ms')\n    print(f'  TPOT: {tpot:.1f} ms')\n    print(f'  Total tokens: {len(collected)}')\n    print(f'  Total time: {total_time*1000:.0f} ms')\n    return collected\n\ntokens = streaming_client('Tell me about inference engineering', max_tokens=10)\n"],"c05"),
        cell_md(["## 2. Async Batch Client\n","\n","asyncio + aiohttp enables sending many concurrent requests from a single Python process — essential for load testing and batch processing.\n"],"c06"),
        cell_code(["import asyncio\n\nasync def simulate_async_request(request_id: int, prompt_len: int,\n                                  output_len: int, server_delay_ms: float = 50) -> dict:\n    \"\"\"Simulate an async HTTP request to an inference server.\"\"\"\n    start = time.perf_counter()\n    await asyncio.sleep(server_delay_ms / 1000)  # TTFT\n    await asyncio.sleep(output_len * 20 / 1000)  # decode\n    elapsed = (time.perf_counter() - start) * 1000\n    return {'id': request_id, 'latency_ms': elapsed, 'tokens': output_len}\n\nasync def batch_inference(requests: list, max_concurrency: int = 10):\n    \"\"\"Run batch inference with bounded concurrency.\"\"\"\n    semaphore = asyncio.Semaphore(max_concurrency)\n\n    async def bounded_request(req):\n        async with semaphore:\n            return await simulate_async_request(**req)\n\n    tasks = [bounded_request(req) for req in requests]\n    results = await asyncio.gather(*tasks)\n    return results\n\n# Run benchmark\nnp.random.seed(42)\nrequests = [\n    {'request_id': i, 'prompt_len': np.random.randint(50, 200),\n     'output_len': np.random.randint(20, 100), 'server_delay_ms': 30}\n    for i in range(50)\n]\n\nfor concurrency in [1, 5, 10, 20]:\n    start = time.perf_counter()\n    results = asyncio.run(batch_inference(requests, max_concurrency=concurrency))\n    total_time = (time.perf_counter() - start) * 1000\n    latencies = [r['latency_ms'] for r in results]\n    throughput = len(requests) / total_time * 1000\n    print(f'Concurrency={concurrency:>3}: {total_time:>7.0f}ms total, '\n          f'P50={np.percentile(latencies,50):>6.0f}ms, '\n          f'{throughput:>5.1f} req/s')\n"],"c07"),
        cell_md(["## 3. Retry and Error Handling\n","\n","Production clients need exponential backoff, timeout handling, and circuit breakers.\n"],"c08"),
        cell_code(["import random\n\nasync def robust_inference_client(prompt: str, max_retries: int = 3,\n                                   base_delay_s: float = 1.0,\n                                   timeout_s: float = 30.0):\n    \"\"\"Production inference client with retry and timeout.\"\"\"\n    for attempt in range(max_retries):\n        try:\n            # Simulate random failures (429 rate limit, 503 overload)\n            await asyncio.sleep(0.01)  # simulate network\n            if random.random() < 0.2:  # 20% failure rate for demo\n                error_code = random.choice([429, 503, 500])\n                raise ConnectionError(f'HTTP {error_code}')\n            return {'text': 'response', 'attempt': attempt + 1}\n\n        except ConnectionError as e:\n            if attempt == max_retries - 1:\n                raise\n            delay = base_delay_s * (2 ** attempt) + random.uniform(0, 1)\n            print(f'  Attempt {attempt+1} failed ({e}), retrying in {delay:.1f}s...')\n            await asyncio.sleep(min(delay, 10))\n\nasync def test_retry():\n    random.seed(42)\n    print('Retry logic demo (20% random failure rate):')\n    for i in range(5):\n        try:\n            result = await robust_inference_client(f'prompt {i}')\n            print(f'  Request {i}: SUCCESS on attempt {result[\"attempt\"]}')\n        except Exception as e:\n            print(f'  Request {i}: FAILED after all retries: {e}')\n\nasyncio.run(test_retry())\n"],"c09"),
        cell_md(["## 4. Protocol Comparison\n","\n","REST, gRPC, and WebSocket have different tradeoffs for inference.\n"],"c10"),
        cell_code(["protocols = {\n    'REST/HTTP SSE': {\n        'streaming': True,\n        'latency': 'Low (HTTP/2 + SSE)',\n        'throughput': 'Medium',\n        'client_complexity': 'Low',\n        'openai_compatible': True,\n        'best_for': 'Chat interfaces, standard API clients',\n    },\n    'gRPC': {\n        'streaming': True,\n        'latency': 'Very low (HTTP/2 + protobuf)',\n        'throughput': 'High',\n        'client_complexity': 'Medium (code gen)',\n        'openai_compatible': False,\n        'best_for': 'Internal microservices, high-throughput batch',\n    },\n    'WebSocket': {\n        'streaming': True,\n        'latency': 'Low',\n        'throughput': 'Medium',\n        'client_complexity': 'Medium',\n        'openai_compatible': False,\n        'best_for': 'Interactive applications, bidirectional',\n    },\n    'REST batch': {\n        'streaming': False,\n        'latency': 'High (wait for full response)',\n        'throughput': 'Medium',\n        'client_complexity': 'Very low',\n        'openai_compatible': True,\n        'best_for': 'Offline processing, simple integration',\n    },\n}\nfor name, props in protocols.items():\n    print(f'{name}:')\n    for k, v in props.items():\n        print(f'  {k:<25} {v}')\n    print()\n"],"c11"),
        cell_md(["## Experiments: Try These\n",
            "\n",
            "1. **Real SSE client**: Write a Python client using `httpx` with streaming that measures TTFT and TPOT from a live vLLM server.\n",
            "2. **Load test with async**: Use the async batch client to send 1000 concurrent requests to your inference server. Find the concurrency level where P99 latency doubles.\n",
            "3. **gRPC client**: Generate a gRPC client from the Triton Inference Server proto and compare latency to the REST endpoint.\n"],"c12"),
        cell_md(["## Key Takeaways\n",
            "\n",
            "- SSE streaming converts inference from a blocking call to a token-by-token stream — reduces perceived latency by showing output immediately, not just at end of generation.\n",
            "- Async clients (asyncio) enable high concurrency from a single Python process — essential for load testing and batch processing without threading overhead.\n",
            "- Production clients need: exponential backoff for retries, request timeout, and circuit breakers — naive clients will amplify server overload with retries.\n",
            "- OpenAI API compatibility (REST + SSE) is the de facto standard; any compliant client works with vLLM, SGLang, TGI, and NIMs.\n",
            "\n## References\n- *Inference Engineering* Ch 7.5 — Philip Kiely, Baseten Books 2026\n- OpenAI API documentation\n- httpx streaming documentation\n- asyncio documentation\n"],"c13"),
    ]
    p = write_nb(27, "client-streaming-async", cells)
    write_readme(27,"Client Code: Streaming, Async & Protocol Support","Tooling",
        "Built a complete inference client toolkit: SSE streaming client with TTFT/TPOT measurement, async batch client with bounded concurrency using asyncio.Semaphore, and retry logic with exponential backoff. Compared REST, gRPC, and WebSocket protocol tradeoffs.",
        "asyncio.Semaphore is the key primitive for production batch clients: it bounds concurrency to prevent overwhelming the server while maximizing throughput from the client side.",
        "client-streaming-async","Async concurrency sweep showing throughput vs latency tradeoff + SSE metrics",
        "- *Inference Engineering* Ch 7.5 (Philip Kiely, Baseten Books 2026)\n- OpenAI API documentation\n- asyncio documentation")
    return validate(p)

if __name__ == '__main__':
    results = {}
    for day_fn in [day06, day07, day08, day09, day10,
                   day11, day12, day13, day14, day15,
                   day16, day17, day18, day19, day20,
                   day21, day22, day23, day24, day25,
                   day26, day27]:
        day_num = int(day_fn.__name__[3:])
        print(f'Generating day {day_num:02d}...', end=' ')
        try:
            ok = day_fn()
            results[day_num] = 'OK' if ok else 'INVALID JSON'
            print('OK' if ok else 'INVALID')
        except Exception as e:
            results[day_num] = f'ERROR: {e}'
            print(f'ERROR: {e}')

    print('\nResults:')
    for day, status in results.items():
        print(f'  Day {day:02d}: {status}')
