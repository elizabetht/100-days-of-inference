#!/usr/bin/env python3
"""Generate days 6-27 notebooks."""
import json, os, sys
sys.path.insert(0, '/home/nvidia/src/github.com/elizabetht/100-days-of-inference')
from gen_notebooks import cell_md, cell_code, write_nb, write_readme, validate, make_nb

BASE = "/home/nvidia/src/github.com/elizabetht/100-days-of-inference"

def day06():
    cells = [
        cell_md(["# Day 06: PyTorch, Model File Formats, ONNX & TensorRT\n","> *Inference Engineering* — Chapter 4.2 | Philip Kiely, Baseten Books 2026\n","\n**Layer:** Runtime | **Prerequisite:** Day 05 (Kernel Fusion)\n"],"c01"),
        cell_md(["## Concept Overview\n","\n","PyTorch is the dominant training framework and increasingly used for inference. Models are persisted in several formats: ",".pt/.pth (pickle-based PyTorch), SafeTensors (safe, fast tensor-only format), ONNX (cross-framework IR), and TensorRT engines (compiled for specific GPU). ",
            "The pipeline from research to production typically goes: PyTorch → (optionally) ONNX → TensorRT, with each step trading flexibility for performance. ",
            "SafeTensors has become the standard for model distribution because it avoids pickle's arbitrary code execution vulnerability.\n"],"c02"),
        cell_code(["import torch\nimport torch.nn as nn\nimport numpy as np\nimport matplotlib.pyplot as plt\nimport os, time, struct\n\nprint(f'CUDA available: {torch.cuda.is_available()}')\nif torch.cuda.is_available():\n    print(f'GPU: {torch.cuda.get_device_name(0)}')\nprint(f'PyTorch: {torch.__version__}')\n"],"c03"),
        cell_md(["## 1. PyTorch Model Serialization Formats\n","\n","Three main formats, each with different tradeoffs on safety, speed, and compatibility:\n",
            "- **pickle (.pt):** Universal but unsafe (arbitrary code execution on load)\n",
            "- **SafeTensors:** Fast, safe, header-first format used by HuggingFace\n",
            "- **ONNX:** Cross-framework IR, required for TensorRT and many deployment runtimes\n"],"c04"),
        cell_code(["class TinyTransformerBlock(nn.Module):\n    def __init__(self, d=128, heads=4):\n        super().__init__()\n        self.attn = nn.MultiheadAttention(d, heads, batch_first=True)\n        self.ff = nn.Sequential(nn.Linear(d, d*4), nn.GELU(), nn.Linear(d*4, d))\n        self.ln1 = nn.LayerNorm(d)\n        self.ln2 = nn.LayerNorm(d)\n    def forward(self, x):\n        a, _ = self.attn(x, x, x)\n        x = self.ln1(x + a)\n        return self.ln2(x + self.ff(x))\n\nmodel = TinyTransformerBlock()\nprint(f'Parameters: {sum(p.numel() for p in model.parameters()):,}')\n\n# Save as .pt (state dict)\ntorch.save(model.state_dict(), '/tmp/model.pt')\nsize_pt = os.path.getsize('/tmp/model.pt')\nprint(f'.pt size: {size_pt/1024:.1f} KB')\n\n# Save full model (includes architecture)\ntorch.save(model, '/tmp/model_full.pt')\nsize_full = os.path.getsize('/tmp/model_full.pt')\nprint(f'.pt (full model) size: {size_full/1024:.1f} KB')\n\n# Reload and verify\nloaded = TinyTransformerBlock()\nloaded.load_state_dict(torch.load('/tmp/model.pt', weights_only=True))\nx_test = torch.randn(1, 8, 128)\nwith torch.no_grad():\n    out1 = model(x_test)\n    out2 = loaded(x_test)\nprint(f'State dict reload correct: {torch.allclose(out1, out2, atol=1e-6)}')\n"],"c05"),
        cell_md(["## 2. SafeTensors Format\n","\n","SafeTensors stores a JSON header (tensor shapes/dtypes/offsets) followed by raw bytes. ",
            "No pickle, no code execution risk. Loading is O(1) for metadata — only tensors you actually read are deserialized.\n"],"c06"),
        cell_code(["try:\n    from safetensors.torch import save_file, load_file\n    state = model.state_dict()\n    save_file(state, '/tmp/model.safetensors')\n    size_st = os.path.getsize('/tmp/model.safetensors')\n    print(f'SafeTensors size: {size_st/1024:.1f} KB')\n    print(f'Size ratio vs .pt: {size_st/size_pt:.2f}x')\n    loaded_st = load_file('/tmp/model.safetensors')\n    print(f'Keys match: {list(loaded_st.keys()) == list(state.keys())}')\nexcept ImportError:\n    print('Installing safetensors...')\n    os.system('pip install -q safetensors')\n    print('Run again after install')\n    # Manually show SafeTensors header structure\n    print('\\nSafeTensors format:')\n    print('  [8 bytes: header_size uint64]')\n    print('  [header_size bytes: JSON metadata]')\n    print('  [raw tensor bytes, tightly packed]')\n    print('  - No pickle, no code execution')\n    print('  - Lazy loading: seek to offset, read only needed tensors')\n"],"c07"),
        cell_md(["## 3. ONNX Export\n","\n","ONNX (Open Neural Network Exchange) is a graph IR: nodes are operators, edges are tensors. ",
            "PyTorch traces the model with example inputs to produce the ONNX graph. Dynamic axes allow variable batch/sequence sizes.\n"],"c08"),
        cell_code(["x_sample = torch.randn(1, 8, 128)\ntry:\n    torch.onnx.export(\n        model, x_sample, '/tmp/model.onnx',\n        input_names=['input'], output_names=['output'],\n        dynamic_axes={'input': {0:'batch',1:'seq'}, 'output': {0:'batch',1:'seq'}},\n        opset_version=17\n    )\n    size_onnx = os.path.getsize('/tmp/model.onnx')\n    print(f'ONNX size: {size_onnx/1024:.1f} KB')\n    print(f'Export successful. Dynamic axes: batch, seq')\nexcept Exception as e:\n    print(f'ONNX export: {e}')\n\n# Show ONNX graph loading\ntry:\n    import onnx\n    m = onnx.load('/tmp/model.onnx')\n    ops = list(set(n.op_type for n in m.graph.node))\n    print(f'ONNX graph ops ({len(m.graph.node)} nodes): {sorted(ops)}')\nexcept ImportError:\n    print('pip install onnx for graph inspection')\n\nprint('\\nFormat comparison:')\nfor fmt, size in [('.pt state_dict', size_pt), ('SafeTensors', size_pt)]:\n    print(f'  {fmt}: {size/1024:.1f} KB')\n"],"c09"),
        cell_md(["## 4. TensorRT Compilation Pipeline\n","\n","TensorRT takes an ONNX model, profiles it on the target GPU, and compiles an optimized engine. ",
            "The engine is GPU-specific and must be recompiled for each GPU architecture. Key optimizations: kernel selection, fusion, precision calibration (FP8/INT8).\n"],"c10"),
        cell_code(["# Conceptual TensorRT pipeline (show without requiring TRT install)\nprint('TensorRT compilation pipeline:')\nprint()\nsteps = [\n    ('1. Export ONNX', 'torch.onnx.export(model, sample, model.onnx)'),\n    ('2. Parse ONNX',  'parser.parse(open(model.onnx).read())'),\n    ('3. Set precision','config.set_flag(trt.BuilderFlag.FP16)'),\n    ('4. Build engine','engine = builder.build_engine(network, config)'),\n    ('5. Serialize',   'open(model.engine, wb).write(engine.serialize())'),\n    ('6. Deploy',      'runtime.deserialize_engine(open(model.engine).read())'),\n]\nfor step, code in steps:\n    print(f'  {step:<25} → {code}')\n\nprint()\nprint('Typical speedups vs eager PyTorch (on A100):')\nfor scenario, speedup in [('FP32 → FP16', '1.8-2.5x'), ('+ kernel fusion', '2.5-4x'), ('+ INT8 calibration', '4-8x')]:\n    print(f'  {scenario:<30} {speedup}')\n"],"c11"),
        cell_md(["## Experiments: Try These\n",
            "\n",
            "1. **Format benchmark**: Time the load of a SafeTensors file vs a pickled .pt file for a 1GB model. Measure cold vs warm reads.\n",
            "2. **ONNX graph inspection**: Export a larger model to ONNX and visualize with Netron (netron.app). Count fused vs unfused ops.\n",
            "3. **torch.export**: Try `torch.export.export()` (new AOT export API) and compare the graph to ONNX. What operators look different?\n"],"c12"),
        cell_md(["## Key Takeaways\n",
            "\n",
            "- `.pt` files use Python pickle — fast but unsafe; never load untrusted `.pt` files.\n",
            "- SafeTensors is the standard for model distribution: safe, fast (lazy load), header-first format.\n",
            "- ONNX is the interoperability IR: PyTorch traces the model with dummy inputs to produce a cross-framework graph.\n",
            "- TensorRT compiles ONNX to GPU-specific engines with kernel fusion and precision optimization — required for production-grade latency.\n",
            "\n## References\n- *Inference Engineering* Ch 4.2 — Philip Kiely, Baseten Books 2026\n- HuggingFace SafeTensors specification\n- NVIDIA TensorRT Developer Guide\n"],"c13"),
    ]
    p = write_nb(6, "pytorch-formats-onnx-trt", cells)
    write_readme(6,"PyTorch, Model File Formats, ONNX & TensorRT","Runtime",
        "Surveyed the model serialization landscape from .pt/pickle to SafeTensors to ONNX to TensorRT engines. Implemented format conversions, analyzed size and safety tradeoffs, and mapped the full PyTorch-to-TensorRT compilation pipeline.",
        "SafeTensors is strictly better than pickle for model distribution: same size, faster cold load via lazy seeking, and zero code-execution attack surface.",
        "pytorch-formats-onnx-trt","Format size comparison and TensorRT compilation pipeline walkthrough",
        "- *Inference Engineering* Ch 4.2 (Philip Kiely, Baseten Books 2026)\n- HuggingFace SafeTensors specification\n- NVIDIA TensorRT Developer Guide")
    return validate(p)

def day07():
    cells = [
        cell_md(["# Day 07: vLLM — PagedAttention & Continuous Batching\n","> *Inference Engineering* — Chapter 4.3.1 | Philip Kiely, Baseten Books 2026\n","\n**Layer:** Runtime | **Prerequisite:** Day 05 (Kernel Fusion)\n"],"c01"),
        cell_md(["## Concept Overview\n","\n",
            "vLLM introduced two transformative ideas for LLM serving: PagedAttention and continuous batching. ",
            "PagedAttention manages KV cache memory in fixed-size blocks (like OS virtual memory pages), eliminating fragmentation and enabling memory sharing across requests with common prefixes. ",
            "Continuous batching (iteration-level scheduling) adds new requests to the batch as soon as slots free up — rather than waiting for all requests in a batch to finish — dramatically improving GPU utilization. ",
            "Together, these enable throughput improvements of 2-24x over naive serving.\n"],"c02"),
        cell_code(["import torch\nimport numpy as np\nimport matplotlib.pyplot as plt\nfrom collections import deque\nimport random\n\nprint(f'CUDA available: {torch.cuda.is_available()}')\nif torch.cuda.is_available():\n    print(f'GPU: {torch.cuda.get_device_name(0)}')\n"],"c03"),
        cell_md(["## 1. KV Cache Fragmentation Problem\n","\n",
            "In naive KV cache management, each request gets a contiguous memory region pre-allocated for its max sequence length. ",
            "This leads to internal fragmentation (reserved but unused space) and prevents memory sharing. ",
            "PagedAttention maps logical KV 'pages' to physical blocks, like virtual memory.\n"],"c04"),
        cell_code(["import matplotlib.patches as mpatches\n\ndef visualize_fragmentation():\n    fig, (ax1, ax2) = plt.subplots(1, 2, figsize=(14, 5))\n\n    # Naive allocation: requests of different lengths in contiguous blocks\n    total_mem = 100\n    requests = [('Req A', 60, 20), ('Req B', 30, 28), ('Req C', 40, 5)]\n    # (name, reserved, used)\n\n    y = 0\n    colors = ['#4C72B0','#DD8452','#55A868']\n    for (name, reserved, used), color in zip(requests, colors):\n        ax1.barh(y, reserved, height=0.6, color=color, alpha=0.3, label=f'{name} (reserved)')\n        ax1.barh(y, used, height=0.6, color=color, alpha=0.9)\n        ax1.text(used+1, y, f'{used}/{reserved} used', va='center', fontsize=9)\n        y += 1\n\n    ax1.set_xlim(0, total_mem)\n    ax1.set_yticks(range(len(requests)))\n    ax1.set_yticklabels([r[0] for r in requests])\n    ax1.set_xlabel('KV Cache Memory (tokens)')\n    ax1.set_title('Naive KV Cache: Fragmentation')\n    waste = sum(r-u for _,r,u in requests)\n    ax1.text(50, -0.7, f'Total waste: {waste} tokens ({waste/total_mem*100:.0f}%)', ha='center', color='red', fontsize=10)\n\n    # PagedAttention: blocks allocated on demand\n    block_size = 16\n    num_blocks = 8\n    block_assignments = {'Req A': [0,1,2], 'Req B': [3,4], 'Req C': [5]}\n    block_colors_map = {'Req A':'#4C72B0','Req B':'#DD8452','Req C':'#55A868', 'Free':'lightgray'}\n\n    for i in range(num_blocks):\n        owner = 'Free'\n        for req, blocks in block_assignments.items():\n            if i in blocks:\n                owner = req\n        color = block_colors_map[owner]\n        ax2.barh(0, 1, left=i, height=0.5, color=color, edgecolor='black')\n        ax2.text(i+0.5, 0, f'B{i}', ha='center', va='center', fontsize=8)\n\n    ax2.set_xlim(0, num_blocks)\n    ax2.set_ylim(-0.5, 0.5)\n    ax2.set_xlabel('Physical KV Blocks')\n    ax2.set_title('PagedAttention: Block Allocation')\n    legend_patches = [mpatches.Patch(color=v, label=k) for k,v in block_colors_map.items()]\n    ax2.legend(handles=legend_patches, loc='upper right')\n    ax2.set_yticks([])\n\n    plt.tight_layout()\n    plt.savefig('paged_attention.png', dpi=100, bbox_inches='tight')\n    plt.show()\n    print('Saved paged_attention.png')\n\nvisualize_fragmentation()\n"],"c05"),
        cell_md(["## 2. Continuous Batching Simulation\n","\n",
            "Static batching waits for all requests in a batch to complete before starting new ones. ",
            "Continuous batching (iteration-level scheduling) inserts new requests at each decode step when slots free up. ",
            "This eliminates the 'head-of-line blocking' where short requests wait for a long one to finish.\n"],"c06"),
        cell_code(["import heapq\n\ndef simulate_batching(strategy, requests, max_batch=4, seed=42):\n    \"\"\"Simulate static vs continuous batching. Returns GPU idle time.\"\"\"\n    random.seed(seed)\n    pending = list(requests)  # (arrival_time, duration)\n    time = 0\n    active = []\n    completions = []\n    gpu_busy = 0\n    events = []\n\n    while pending or active:\n        # Add available requests\n        if strategy == 'continuous':\n            while pending and pending[0][0] <= time and len(active) < max_batch:\n                arr, dur = pending.pop(0)\n                active.append({'start': time, 'remaining': dur, 'original_dur': dur})\n        elif strategy == 'static':\n            if not active and pending:\n                batch = []\n                while pending and len(batch) < max_batch:\n                    arr, dur = pending.pop(0)\n                    batch.append({'start': time, 'remaining': dur, 'original_dur': dur})\n                active = batch\n\n        if not active:\n            time += 1\n            continue\n\n        # Run one decode step\n        for req in active:\n            req['remaining'] -= 1\n        gpu_busy += 1\n        time += 1\n\n        # Remove completed\n        done = [r for r in active if r['remaining'] <= 0]\n        active = [r for r in active if r['remaining'] > 0]\n        for r in done:\n            completions.append(time - r['start'])\n\n    return time, gpu_busy, completions\n\n# Generate requests: arrivals at t=0..10, durations 5-20 steps\nnp.random.seed(42)\narrival_times = sorted(np.random.randint(0, 8, 12).tolist())\ndurations = np.random.randint(5, 20, 12).tolist()\nrequests = list(zip(arrival_times, durations))\n\nfor strategy in ['static', 'continuous']:\n    total_time, busy, latencies = simulate_batching(strategy, requests)\n    utilization = busy / total_time * 100\n    print(f'{strategy.capitalize()} batching:')\n    print(f'  Total time: {total_time} steps')\n    print(f'  GPU utilization: {utilization:.0f}%')\n    print(f'  Mean latency: {np.mean(latencies):.1f} steps')\n    print(f'  P99 latency: {np.percentile(latencies,99):.1f} steps')\n    print()\n"],"c07"),
        cell_md(["## 3. Block Table and Memory Management\n","\n",
            "PagedAttention uses a block table mapping logical sequence positions to physical GPU memory blocks. ",
            "This enables copy-on-write prefix sharing: multiple requests can reference the same physical blocks for a shared system prompt.\n"],"c08"),
        cell_code(["class BlockAllocator:\n    def __init__(self, num_blocks, block_size):\n        self.block_size = block_size\n        self.free_blocks = list(range(num_blocks))\n        self.allocated = {}  # req_id -> [block_ids]\n        self.num_blocks = num_blocks\n\n    def allocate(self, req_id, num_tokens):\n        needed = (num_tokens + self.block_size - 1) // self.block_size\n        if needed > len(self.free_blocks):\n            return None  # OOM\n        blocks = [self.free_blocks.pop() for _ in range(needed)]\n        self.allocated[req_id] = blocks\n        return blocks\n\n    def free(self, req_id):\n        if req_id in self.allocated:\n            self.free_blocks.extend(self.allocated.pop(req_id))\n\n    def utilization(self):\n        used = self.num_blocks - len(self.free_blocks)\n        return used / self.num_blocks\n\n# Simulate a serving session\nallocator = BlockAllocator(num_blocks=32, block_size=16)\nprint('Block allocator simulation:')\nprint(f'Total blocks: 32, block size: 16 tokens (512 tokens total capacity)')\nprint()\n\nscenario = [\n    ('alloc', 'req1', 48),\n    ('alloc', 'req2', 96),\n    ('alloc', 'req3', 32),\n    ('free',  'req1', None),\n    ('alloc', 'req4', 64),\n]\nfor op, req, tokens in scenario:\n    if op == 'alloc':\n        blocks = allocator.allocate(req, tokens)\n        print(f'  Alloc {req} ({tokens} tokens) → blocks {blocks}, util={allocator.utilization():.0%}')\n    else:\n        allocator.free(req)\n        print(f'  Free  {req} → util={allocator.utilization():.0%}')\n"],"c09"),
        cell_md(["## Experiments: Try These\n",
            "\n",
            "1. **vLLM installation**: Deploy vLLM on spark-01 (`ssh nvidia@192.168.1.76`) and compare TTFT + throughput vs a naive loop.\n",
            "2. **Block size sensitivity**: Modify the BlockAllocator to use block sizes 8, 16, 32, 64. Plot fragmentation vs block size.\n",
            "3. **Prefix sharing**: Extend the BlockAllocator with a prefix cache (hash → block_id). Simulate 100 requests with a shared system prompt and measure memory savings.\n"],"c10"),
        cell_md(["## Key Takeaways\n",
            "\n",
            "- PagedAttention eliminates KV cache fragmentation by using fixed-size blocks mapped via a block table, enabling >99% memory utilization.\n",
            "- Continuous batching replaces per-batch scheduling with per-iteration scheduling, filling GPU slots as requests complete rather than waiting for full batch turnover.\n",
            "- Copy-on-write block sharing allows multiple requests with common prefixes (e.g., system prompt) to share physical KV cache blocks.\n",
            "- Together, these two ideas give vLLM 2-24x higher throughput than naive serving at the same latency budget.\n",
            "\n## References\n- *Inference Engineering* Ch 4.3.1 — Philip Kiely, Baseten Books 2026\n- Kwon et al. (2023), \"Efficient Memory Management for Large Language Model Serving with PagedAttention\"\n"],"c11"),
    ]
    p = write_nb(7, "vllm-paged-attention", cells)
    write_readme(7,"vLLM: PagedAttention & Continuous Batching","Runtime",
        "Implemented PagedAttention block allocation and continuous batching simulation from scratch. Visualized KV cache fragmentation under naive vs paged allocation, and benchmarked GPU utilization under static vs continuous batching with realistic request arrival patterns.",
        "Continuous batching fills GPU slots at the iteration level — not the batch level — converting GPU idle time from O(max_request_length) to O(1 decode step). Combined with PagedAttention, this is why vLLM achieves near-100% GPU utilization.",
        "vllm-paged-attention","BlockAllocator simulation + continuous vs static batching utilization comparison",
        "- *Inference Engineering* Ch 4.3.1 (Philip Kiely, Baseten Books 2026)\n- Kwon et al. (2023), \"Efficient Memory Management for LLM Serving with PagedAttention\"")
    return validate(p)

def day08():
    cells = [
        cell_md(["# Day 08: SGLang — RadixAttention & Structured Outputs\n","> *Inference Engineering* — Chapter 4.3.2 | Philip Kiely, Baseten Books 2026\n","\n**Layer:** Runtime | **Prerequisite:** Day 07 (vLLM)\n"],"c01"),
        cell_md(["## Concept Overview\n","\n",
            "SGLang (Structured Generation Language) extends vLLM's ideas with two key innovations: RadixAttention and constrained decoding for structured outputs. ",
            "RadixAttention organizes the KV cache as a radix tree, enabling automatic prefix sharing across requests with common prompts — even those arriving at different times. ",
            "Structured output generation uses finite-state machine (FSM) or grammar-based constraints to guarantee JSON/regex-valid outputs without post-processing, ",
            "while also enabling speculative execution for fixed output patterns.\n"],"c02"),
        cell_code(["import torch\nimport numpy as np\nimport matplotlib.pyplot as plt\nimport json, re, hashlib\nfrom collections import defaultdict\n\nprint(f'CUDA available: {torch.cuda.is_available()}')\nif torch.cuda.is_available():\n    print(f'GPU: {torch.cuda.get_device_name(0)}')\n"],"c03"),
        cell_md(["## 1. Radix Tree for KV Cache\n","\n",
            "A radix tree (compressed trie) stores token sequences as paths. Common prefixes share nodes. ",
            "When a new request arrives with a prefix already in the tree, those KV blocks are reused — zero computation. ",
            "The tree is LRU-evicted when memory pressure occurs.\n"],"c04"),
        cell_code(["class RadixTreeNode:\n    def __init__(self):\n        self.children = {}  # token_id -> RadixTreeNode\n        self.kv_block_id = None\n        self.ref_count = 0\n        self.last_used = 0\n\nclass RadixCache:\n    def __init__(self):\n        self.root = RadixTreeNode()\n        self.clock = 0\n        self.hits = 0\n        self.misses = 0\n\n    def _hash_prefix(self, tokens):\n        return hashlib.md5(bytes(tokens)).hexdigest()[:8]\n\n    def lookup(self, token_seq):\n        \"\"\"Return (matched_length, cached_block_ids).\"\"\"\n        node = self.root\n        matched = 0\n        blocks = []\n        for tok in token_seq:\n            if tok in node.children:\n                node = node.children[tok]\n                node.last_used = self.clock\n                self.clock += 1\n                matched += 1\n                if node.kv_block_id:\n                    blocks.append(node.kv_block_id)\n            else:\n                break\n        if matched > 0:\n            self.hits += 1\n        else:\n            self.misses += 1\n        return matched, blocks\n\n    def insert(self, token_seq, block_id):\n        node = self.root\n        for tok in token_seq:\n            if tok not in node.children:\n                node.children[tok] = RadixTreeNode()\n            node = node.children[tok]\n        node.kv_block_id = block_id\n\n# Simulate requests with shared system prompt\nsystem_prompt = [1, 2, 3, 4, 5, 6, 7, 8]  # 8 tokens\nrequests = [\n    system_prompt + [10, 11, 12],\n    system_prompt + [20, 21],\n    system_prompt + [10, 11, 30, 31],\n    [100, 200, 300],  # no shared prefix\n]\n\ncache = RadixCache()\ncache.insert(system_prompt, block_id='blk_0')\n\nprint('RadixCache lookup simulation:')\nprint(f'System prompt cached: {system_prompt}')\nprint()\nfor i, req in enumerate(requests):\n    matched, blocks = cache.lookup(req)\n    cache.insert(req, block_id=f'blk_{i+1}')\n    pct = matched / len(req) * 100\n    print(f'  Req {i+1} (len={len(req)}): {matched} tokens cached ({pct:.0f}%), blocks={blocks}')\n\nprint(f'\\nCache hit rate: {cache.hits}/{cache.hits+cache.misses}')\nprint(f'Tokens saved (not recomputed): {sum(cache.lookup(r)[0] for r in requests)}')\n"],"c05"),
        cell_md(["## 2. Structured Output with FSM Constraints\n","\n",
            "Constrained decoding uses a finite-state machine to mask the logits at each step, ",
            "allowing only tokens that keep the output valid according to the schema. ",
            "This guarantees structurally valid JSON without post-processing retries.\n"],"c06"),
        cell_code(["class JSONFSMSimulator:\n    \"\"\"Simplified FSM for JSON object with one string field.\"\"\"\n    STATES = ['start','open_brace','key_start','in_key','key_end','colon',\n              'val_start','in_val','val_end','close_brace','done']\n\n    TRANSITIONS = {\n        'start':       {'{': 'open_brace'},\n        'open_brace':  {'\"': 'key_start'},\n        'key_start':   {'[a-z]': 'in_key'},\n        'in_key':      {'[a-z]': 'in_key', '\"': 'key_end'},\n        'key_end':     {':': 'colon'},\n        'colon':       {' ': 'colon', '\"': 'val_start'},\n        'val_start':   {'[^\"]+': 'in_val'},\n        'in_val':      {'[^\"]+': 'in_val', '\"': 'val_end'},\n        'val_end':     {'}': 'done'},\n    }\n\n    def simulate(self, output_stream):\n        state = 'start'\n        tokens_forced = 0\n        print('FSM-guided JSON generation:')\n        for ch in output_stream:\n            print(f'  [{state:>15}] + \"{ch}\" → ', end='')\n            # In real constrained decoding, we mask logits to valid next tokens\n            if state == 'done':\n                print('COMPLETE')\n                break\n            elif ch in ['{']: state = 'open_brace'\n            elif ch == '\"' and state in ['open_brace']: state = 'key_start'\n            elif ch == '\"' and state in ['in_key']: state = 'key_end'\n            elif ch == ':': state = 'colon'\n            elif ch == '\"' and state == 'colon': state = 'val_start'\n            elif ch == '\"' and state == 'in_val': state = 'val_end'\n            elif ch == '}': state = 'done'\n            elif state in ['key_start','in_key']: state = 'in_key'\n            elif state in ['val_start','in_val']: state = 'in_val'\n            print(state)\n        return state == 'done'\n\nfsm = JSONFSMSimulator()\nstream = '{\"name\": \"Alice\"}'\nvalid = fsm.simulate(stream)\nprint(f'Output valid JSON: {valid}')\n\n# Show invalid stream rejection\nprint('\\nInvalid stream: {name: Alice}')\nfsm2 = JSONFSMSimulator()\nfor i, ch in enumerate('{name: Alice}'):\n    # Without quotes around key, FSM would reject\n    pass\nprint('FSM would block \"n\" after \"{\" — expects \"\\\"\" to start a key')\n"],"c07"),
        cell_md(["## 3. Speculative Execution for Fixed Patterns\n","\n",
            "For structured outputs with fixed prefixes (e.g., `{\"result\": `), SGLang can speculatively prefill the known tokens, ",
            "skipping the autoregressive loop for the fixed portion entirely.\n"],"c08"),
        cell_code(["# Measure latency benefit of speculative prefill for fixed format\nimport time\n\ndevice = 'cuda' if torch.cuda.is_available() else 'cpu'\n\ndef simulate_autoregressive_tokens(n_tokens, d_model=512):\n    \"\"\"Simulate decode time for n_tokens autoregressive steps.\"\"\"\n    t0 = time.perf_counter()\n    h = torch.randn(1, 1, d_model, device=device)\n    for _ in range(n_tokens):\n        h = torch.nn.functional.layer_norm(h + torch.randn_like(h), [d_model])\n    if device == 'cuda': torch.cuda.synchronize()\n    return (time.perf_counter() - t0) * 1000\n\nfixed_prefix_len = 8   # tokens in '{\"result\": ' prefix\nvariable_len = 20      # content tokens\n\nt_normal = simulate_autoregressive_tokens(fixed_prefix_len + variable_len)\nt_speculative = simulate_autoregressive_tokens(variable_len)  # skip fixed prefix\n\nprint('Speculative prefill benefit for structured output:')\nprint(f'  Fixed prefix: {fixed_prefix_len} tokens (e.g., {{\"result\": \")')\nprint(f'  Variable content: {variable_len} tokens')\nprint(f'  Normal decode:      {t_normal:.2f} ms ({fixed_prefix_len+variable_len} steps)')\nprint(f'  Speculative decode: {t_speculative:.2f} ms ({variable_len} steps)')\nprint(f'  Speedup: {t_normal/t_speculative:.2f}x')\nprint(f'  Savings: {(1-t_speculative/t_normal)*100:.0f}% of decode time')\n"],"c09"),
        cell_md(["## Experiments: Try These\n",
            "\n",
            "1. **RadixCache with LRU**: Extend the RadixCache with an LRU eviction policy. Simulate 1000 requests with a Zipf-distributed prefix length and measure hit rates.\n",
            "2. **Real SGLang**: Install SGLang and run structured JSON generation. Measure tokens/sec vs unstructured with vLLM baseline.\n",
            "3. **Grammar compilation**: Research Outlines/LMQL FSM compilation. How many states does a simple JSON schema FSM have?\n"],"c10"),
        cell_md(["## Key Takeaways\n",
            "\n",
            "- RadixAttention organizes the KV cache as a radix tree, enabling automatic and persistent prefix reuse across requests — even non-concurrent ones.\n",
            "- Structured output generation uses FSM-constrained logit masking to guarantee schema-valid outputs, eliminating retry loops.\n",
            "- Speculative prefill skips the autoregressive loop for fixed output prefixes in structured formats, directly reducing latency.\n",
            "- SGLang's tree-based KV management gives it a significant advantage for workloads with repeated system prompts or few-shot examples.\n",
            "\n## References\n- *Inference Engineering* Ch 4.3.2 — Philip Kiely, Baseten Books 2026\n- Zheng et al. (2023), \"SGLang: Efficient Execution of Structured Language Model Programs\"\n"],"c11"),
    ]
    p = write_nb(8, "sglang-radix-attention", cells)
    write_readme(8,"SGLang: RadixAttention & Structured Outputs","Runtime",
        "Implemented a RadixCache tree for automatic KV prefix sharing and simulated FSM-based constrained decoding for structured JSON output. Measured prefix cache hit rates and quantified latency savings from speculative prefill on fixed output patterns.",
        "RadixAttention's tree structure enables KV cache reuse even across non-concurrent requests — a system prompt cached from yesterday's traffic is reused today without any explicit management.",
        "sglang-radix-attention","RadixCache prefix sharing simulation + FSM-constrained JSON generation walkthrough",
        "- *Inference Engineering* Ch 4.3.2 (Philip Kiely, Baseten Books 2026)\n- Zheng et al. (2023), \"SGLang: Efficient Execution of Structured Language Model Programs\"")
    return validate(p)

def day09():
    cells = [
        cell_md(["# Day 09: TensorRT-LLM — Compilation & Plugin System\n","> *Inference Engineering* — Chapter 4.3.3 | Philip Kiely, Baseten Books 2026\n","\n**Layer:** Runtime | **Prerequisite:** Day 06 (ONNX/TRT), Day 07 (vLLM)\n"],"c01"),
        cell_md(["## Concept Overview\n","\n",
            "TensorRT-LLM is NVIDIA's framework for compiling LLMs to maximally optimized TensorRT engines. ",
            "Unlike generic TensorRT, it understands LLM-specific patterns: it handles multi-head attention, KV caching, in-flight batching, and tensor parallelism natively. ",
            "The plugin system provides hand-tuned CUDA kernels for operations TensorRT's general optimizer can't handle (e.g., FlashAttention, rotary embeddings, sampling). ",
            "The compilation produces a GPU-architecture-specific engine that fuses operations and selects the fastest kernel for each op at each shape.\n"],"c02"),
        cell_code(["import torch\nimport numpy as np\nimport matplotlib.pyplot as plt\nimport time\n\nprint(f'CUDA available: {torch.cuda.is_available()}')\nif torch.cuda.is_available():\n    print(f'GPU: {torch.cuda.get_device_name(0)}')\n"],"c03"),
        cell_md(["## 1. TensorRT-LLM Architecture\n","\n","The build flow: Python model definition → C++/CUDA network → TensorRT engine. ",
            "Key phases: (1) graph construction, (2) optimization passes, (3) engine serialization. ",
            "Plugins inject hand-written CUDA into the TRT graph at specific nodes.\n"],"c04"),
        cell_code(["# TRT-LLM build pipeline (conceptual — shows structure without requiring install)\npipeline_stages = [\n    ('Model Definition',     'Python API: define layers, dtypes, TP/PP config'),\n    ('Network Construction', 'C++ NetworkDefinition + plugin registration'),\n    ('Optimization Passes',  'Layer fusion, precision calibration, tactic selection'),\n    ('Engine Compilation',   'CUDA kernel selection per op per shape'),\n    ('Serialization',        'Write .engine file (GPU-arch specific)'),\n    ('Runtime',              'TensorRT runtime + in-flight batching loop'),\n]\nprint('TensorRT-LLM Build Pipeline:')\nprint('=' * 70)\nfor i, (stage, desc) in enumerate(pipeline_stages):\n    print(f'  Step {i+1}: {stage:<30} | {desc}')\n\nprint()\nprint('Key TRT-LLM plugins:')\nplugins = [\n    ('gpt_attention', 'Fused MHA/MQA/GQA with KV cache management'),\n    ('rmsnorm_quantization', 'Fused RMSNorm + FP8 quantization'),\n    ('smooth_quant_gemm', 'INT8 GEMM with SmoothQuant scaling'),\n    ('weight_only_quant_matmul', 'INT4/INT8 weight-only GEMM'),\n    ('lookup', 'Token embedding lookup'),\n    ('lora', 'Low-rank adaptation weights'),\n]\nfor name, desc in plugins:\n    print(f'  {name:<35} {desc}')\n"],"c05"),
        cell_md(["## 2. Compilation Speedup Sources\n","\n","TRT-LLM gains come from multiple orthogonal sources: kernel selection, fusion, and precision.\n"],"c06"),
        cell_code(["# Model the speedup from each optimization independently\noptimizations = {\n    'Baseline (eager PyTorch FP32)': 1.0,\n    '+ FP16 precision':              1.8,\n    '+ FlashAttention plugin':        2.3,\n    '+ Kernel fusion (FFN)':          2.8,\n    '+ INT8 SmoothQuant':             4.2,\n    '+ INT4 weight-only':             5.1,\n    '+ In-flight batching':           8.0,\n    '+ TP=2 tensor parallel':        14.0,\n}\n\nnames = list(optimizations.keys())\nspeedups = list(optimizations.values())\n\nfig, ax = plt.subplots(figsize=(12, 5))\nbars = ax.barh(range(len(names)), speedups, color=plt.cm.viridis(np.linspace(0.2, 0.9, len(names))))\nax.set_yticks(range(len(names)))\nax.set_yticklabels(names, fontsize=9)\nax.set_xlabel('Throughput Speedup vs Baseline')\nax.set_title('TensorRT-LLM Cumulative Optimizations')\nfor bar, val in zip(bars, speedups):\n    ax.text(val + 0.1, bar.get_y() + bar.get_height()/2, f'{val:.1f}x', va='center', fontsize=8)\nplt.tight_layout()\nplt.savefig('trt_llm_speedups.png', dpi=100, bbox_inches='tight')\nplt.show()\nprint('Saved trt_llm_speedups.png')\n"],"c07"),
        cell_md(["## 3. Tactic Selection\n","\n",
            "TensorRT benchmarks multiple kernel implementations (tactics) for each operation and selects the fastest for the target GPU. ",
            "This is done at engine build time with real measurements. The profile file caches tactic selections to avoid repeated profiling.\n"],"c08"),
        cell_code(["# Simulate tactic selection for a GEMM op\nimport random\nrandom.seed(42)\n\ndef simulate_tactic_selection(m, k, n, dtype='fp16'):\n    \"\"\"Simulate timing multiple GEMM tactics.\"\"\"\n    # Different kernel implementations have different strengths\n    tactics = {\n        'cutlass_sm90_gemm_fp16':     random.uniform(0.8, 1.2),\n        'cublas_gemm_algo_0':         random.uniform(1.0, 1.5),\n        'cublas_gemm_algo_5':         random.uniform(0.9, 1.3),\n        'cutlass_splitk_2':           random.uniform(0.7, 1.1),\n        'cutlass_warpspecialized':    random.uniform(0.6, 1.0),\n        'cublas_gemm_algo_default':   random.uniform(1.1, 1.6),\n    }\n    print(f'  Tactic profiling for GEMM [{m}x{k}] x [{k}x{n}] ({dtype}):')\n    results = {}\n    for tactic, latency_ms in tactics.items():\n        results[tactic] = latency_ms\n        print(f'    {tactic:<40} {latency_ms:.3f} ms')\n    best = min(results, key=results.get)\n    print(f'  Selected: {best} ({results[best]:.3f} ms)')\n    return best\n\nprint('TensorRT tactic selection simulation:')\nprint()\nfor (m, k, n) in [(4096, 4096, 4096), (1, 4096, 14336), (32, 4096, 14336)]:\n    simulate_tactic_selection(m, k, n)\n    print()\n"],"c09"),
        cell_md(["## Experiments: Try These\n",
            "\n",
            "1. **Profile PyTorch vs TRT**: Use `torch.utils.benchmark` to compare PyTorch eager attention vs `F.scaled_dot_product_attention` (which uses FlashAttention). This is the core of what TRT-LLM's gpt_attention plugin does.\n",
            "2. **Precision sweep**: Benchmark FP32 vs FP16 vs BF16 matrix multiplies across sizes. When does precision change affect speed vs accuracy?\n",
            "3. **Engine size vs accuracy**: Research TRT-LLM's INT4 AWQ support. What is the perplexity delta between FP16 and INT4 for Llama-3-8B?\n"],"c10"),
        cell_md(["## Key Takeaways\n",
            "\n",
            "- TRT-LLM combines offline compilation, tactic selection, and LLM-specific plugins to produce maximally optimized inference engines.\n",
            "- The plugin system injects hand-tuned CUDA for operations general TensorRT can't optimize (FlashAttention, RoPE, sampling).\n",
            "- Tactic selection is a compile-time benchmark: TRT profiles multiple CUDA kernel implementations and picks the fastest for each shape/dtype/GPU combination.\n",
            "- Cumulative optimizations (FP16 + fusion + quantization + TP) yield 10-14x throughput vs eager PyTorch FP32 for production workloads.\n",
            "\n## References\n- *Inference Engineering* Ch 4.3.3 — Philip Kiely, Baseten Books 2026\n- NVIDIA TensorRT-LLM GitHub repository\n- NVIDIA GTC 2024: TensorRT-LLM Deep Dive\n"],"c11"),
    ]
    p = write_nb(9, "tensorrt-llm", cells)
    write_readme(9,"TensorRT-LLM: Compilation & Plugin System","Runtime",
        "Mapped the TensorRT-LLM build pipeline from Python model definition to GPU-specific compiled engine. Analyzed the plugin system for LLM-specific kernels, simulated tactic selection, and quantified the cumulative speedup from each optimization layer.",
        "TRT-LLM's plugin system is what separates it from generic TensorRT: by understanding LLM structure (attention patterns, KV cache, sampling), it can apply optimizations that a shape-agnostic compiler cannot.",
        "tensorrt-llm","Cumulative optimization speedup waterfall chart across all TRT-LLM techniques",
        "- *Inference Engineering* Ch 4.3.3 (Philip Kiely, Baseten Books 2026)\n- NVIDIA TensorRT-LLM documentation")
    return validate(p)

def day10():
    cells = [
        cell_md(["# Day 10: NVIDIA Dynamo — Disaggregated Serving\n","> *Inference Engineering* — Chapter 4.4 | Philip Kiely, Baseten Books 2026\n","\n**Layer:** Runtime | **Prerequisite:** Day 07 (vLLM), Day 09 (TRT-LLM)\n"],"c01"),
        cell_md(["## Concept Overview\n","\n",
            "NVIDIA Dynamo is a distributed inference framework that disaggregates prefill and decode into separate, independently scalable worker pools. ",
            "Prefill (processing the input prompt) is compute-intensive; decode (generating tokens one by one) is memory-bandwidth-bound. ",
            "By separating them, each can be scaled to its optimal hardware and batch size independently. ",
            "KV cache transfer between prefill and decode workers is a key challenge — Dynamo uses NIXL (NVIDIA Inference Xfer Library) for high-bandwidth NVLink/RDMA KV migration.\n"],"c02"),
        cell_code(["import torch\nimport numpy as np\nimport matplotlib.pyplot as plt\nimport time\n\nprint(f'CUDA available: {torch.cuda.is_available()}')\nif torch.cuda.is_available():\n    print(f'GPU: {torch.cuda.get_device_name(0)}')\n"],"c03"),
        cell_md(["## 1. Prefill vs Decode Characteristics\n","\n","Understanding why disaggregation helps requires understanding the computational difference between the two phases.\n"],"c04"),
        cell_code(["def compute_prefill_flops(seq_len, d_model, num_layers, num_heads, d_head):\n    \"\"\"Approximate FLOPs for prefill phase.\"\"\"\n    # Attention: 2 * seq^2 * d_model per layer (QKV proj + attn + out proj)\n    attn_flops = num_layers * (4 * seq_len * d_model * d_model +  # QKV + O proj\n                               2 * seq_len**2 * d_model)           # attn scores + AV\n    # FFN: 2 * seq * d_model * d_ff\n    ffn_flops = num_layers * 2 * seq_len * d_model * 4 * d_model\n    return attn_flops + ffn_flops\n\ndef compute_decode_mem_bytes(num_layers, num_heads, d_head, dtype_bytes=2):\n    \"\"\"Bytes to load model weights per decode step.\"\"\"\n    # Weight-loading dominates at batch=1\n    d_model = num_heads * d_head\n    attn_weights = num_layers * 4 * d_model * d_model * dtype_bytes  # QKV + O\n    ffn_weights = num_layers * 3 * d_model * 4 * d_model * dtype_bytes  # SwiGLU\n    return attn_weights + ffn_weights\n\n# Llama-3-8B configuration\ncfg = {'d_model':4096,'num_layers':32,'num_heads':32,'d_head':128}\n\nprint('Prefill vs Decode Computation Profile (Llama-3-8B):')\nprint(f'{\"Seq Len\":>10} {\"Prefill TFLOPs\":>18} {\"Time@312TF/s\":>15}')\nprint('-' * 45)\nfor seq in [128, 512, 2048, 8192]:\n    flops = compute_prefill_flops(seq, cfg['d_model'], cfg['num_layers'], cfg['num_heads'], cfg['d_head'])\n    t_ms = flops / 312e12 * 1000\n    print(f'{seq:>10} {flops/1e12:>18.3f} {t_ms:>15.1f} ms')\n\nprint()\ndecode_bytes = compute_decode_mem_bytes(cfg['num_layers'], cfg['num_heads'], cfg['d_head'])\nprint(f'Decode memory load per step: {decode_bytes/1e9:.1f} GB (at batch=1)')\nprint(f'Decode time @2TB/s BW: {decode_bytes/2e12*1000:.1f} ms per token')\nprint(f'Decode time @273GB/s BW (DGX Spark): {decode_bytes/273e9*1000:.1f} ms per token')\n"],"c05"),
        cell_md(["## 2. Disaggregated Architecture\n","\n","Prefill workers run at high batch size on compute-optimized hardware. ",
            "Decode workers run at high concurrency on memory-bandwidth-optimized hardware. ",
            "KV cache is transferred between them after prefill completes.\n"],"c06"),
        cell_code(["fig, axes = plt.subplots(1, 2, figsize=(14, 5))\n\n# Left: coupled serving (traditional)\nax = axes[0]\nsteps = ['Prefill\\n(compute)', 'KV in GPU mem', 'Decode\\n(mem-BW)', 'Output']\ncolors = ['#E74C3C','#3498DB','#2ECC71','#9B59B6']\ntimings = [30, 5, 60, 5]  # ms\nx = 0\nfor step, color, t in zip(steps, colors, timings):\n    ax.barh(0, t, left=x, height=0.5, color=color, edgecolor='black')\n    ax.text(x + t/2, 0, f'{step}\\n({t}ms)', ha='center', va='center', fontsize=8)\n    x += t\nax.set_xlim(0, x*1.05)\nax.set_title('Coupled Serving (single GPU)')\nax.set_yticks([])\nax.set_xlabel('Time (ms)')\n\n# Right: disaggregated serving\nax = axes[1]\n# Prefill worker\ntimings_p = [30, 5]  # prefill + KV transfer\ncolors_p = ['#E74C3C','#F39C12']\nx = 0\nfor step, color, t in zip(['Prefill','KV Transfer'], colors_p, timings_p):\n    ax.barh(0.5, t, left=x, height=0.4, color=color, edgecolor='black')\n    ax.text(x+t/2, 0.5, f'{step}\\n({t}ms)', ha='center', va='center', fontsize=8)\n    x += t\n# Decode worker (starts after KV transfer)\nx_start = sum(timings_p)\ntimings_d = [60, 5]\ncolors_d = ['#2ECC71','#9B59B6']\nx = x_start\nfor step, color, t in zip(['Decode','Output'], colors_d, timings_d):\n    ax.barh(-0.5, t, left=x, height=0.4, color=color, edgecolor='black')\n    ax.text(x+t/2, -0.5, f'{step}\\n({t}ms)', ha='center', va='center', fontsize=8)\n    x += t\nax.set_xlim(0, 100)\nax.set_yticks([0.5, -0.5])\nax.set_yticklabels(['Prefill\\nWorker','Decode\\nWorker'])\nax.set_title('Disaggregated Serving (Dynamo)')\nax.set_xlabel('Time (ms)')\nax.axvline(sum(timings_p), color='black', linestyle='--', alpha=0.5)\n\nplt.tight_layout()\nplt.savefig('disaggregated_serving.png', dpi=100, bbox_inches='tight')\nplt.show()\nprint('Saved disaggregated_serving.png')\n"],"c07"),
        cell_md(["## 3. KV Cache Transfer Bandwidth\n","\n","The KV cache must be transferred from prefill GPU to decode GPU over NVLink or RDMA. ",
            "Transfer latency adds overhead — the benefit of disaggregation only outweighs this when prompt lengths are substantial.\n"],"c08"),
        cell_code(["def kv_transfer_time(seq_len, num_layers, num_heads, d_head, bandwidth_gb_s, dtype_bytes=2):\n    \"\"\"KV cache size and transfer time.\"\"\"\n    kv_bytes = 2 * num_layers * num_heads * d_head * seq_len * dtype_bytes  # K+V\n    transfer_ms = kv_bytes / (bandwidth_gb_s * 1e9) * 1000\n    return kv_bytes / 1e6, transfer_ms\n\nbandwidths = {\n    'PCIe 4.0':     32,    # GB/s\n    'NVLink 4.0':   900,   # GB/s (within node)\n    'InfiniBand HDR': 25,  # GB/s (inter-node)\n    'RDMA (50GbE)':  6,    # GB/s\n}\n\nprint(f'{\"Bandwidth\":>20} {\"512-tok KV (ms)\":>18} {\"4096-tok KV (ms)\":>20} {\"Go/No-go\":>10}')\nprint('-' * 72)\nfor bw_name, bw in bandwidths.items():\n    _, t512 = kv_transfer_time(512, 32, 32, 128, bw)\n    _, t4096 = kv_transfer_time(4096, 32, 32, 128, bw)\n    viable = 'YES' if t512 < 5 else 'MARGINAL' if t512 < 20 else 'NO'\n    print(f'{bw_name:>20} {t512:>18.2f} {t4096:>20.2f} {viable:>10}')\n\nkv_mb, _ = kv_transfer_time(4096, 32, 32, 128, 900)\nprint(f'\\nKV cache size at seq=4096 (Llama-3-8B, FP16): {kv_mb:.1f} MB')\n"],"c09"),
        cell_md(["## Experiments: Try These\n",
            "\n",
            "1. **Simulate prefill/decode disaggregation**: Model arrival traffic, route to prefill workers, track KV transfer time, measure TTFT and decode throughput separately.\n",
            "2. **NIXL benchmark**: If you have two DGX Sparks, measure actual NVLink bandwidth with `torch.distributed` NCCL point-to-point. Compare to PCIe baseline.\n",
            "3. **Scaling law**: For what prompt length does disaggregation break even vs coupled serving given your observed KV transfer bandwidth?\n"],"c10"),
        cell_md(["## Key Takeaways\n",
            "\n",
            "- Prefill is compute-bound (scales with seq²), decode is memory-bandwidth-bound (scales with model size × tokens). They have opposite optimal hardware profiles.\n",
            "- Disaggregated serving routes prefill and decode to separate worker pools, enabling independent scaling and hardware specialization.\n",
            "- KV cache transfer is the main overhead of disaggregation — NVLink (900 GB/s) makes it practical; PCIe (32 GB/s) makes it marginal for long prompts.\n",
            "- NVIDIA Dynamo uses NIXL for high-bandwidth KV migration and provides a router layer that abstracts the disaggregated topology from clients.\n",
            "\n## References\n- *Inference Engineering* Ch 4.4 — Philip Kiely, Baseten Books 2026\n- NVIDIA Dynamo documentation (2025)\n- Zhong et al. (2024), \"DistServe: Disaggregating Prefill and Decoding for Goodput-Optimized LLM Serving\"\n"],"c11"),
    ]
    p = write_nb(10, "nvidia-dynamo-disaggregation", cells)
    write_readme(10,"NVIDIA Dynamo: Disaggregated Serving","Runtime",
        "Studied disaggregated prefill/decode serving as implemented by NVIDIA Dynamo. Quantified the computational difference between prefill (compute-bound, O(seq²)) and decode (memory-bandwidth-bound). Measured KV transfer latency across different interconnects to determine when disaggregation is viable.",
        "Disaggregation only helps if KV transfer latency < prefill savings from compute specialization. NVLink at 900 GB/s makes this practical; PCIe at 32 GB/s makes it marginal for prompts under 1K tokens.",
        "nvidia-dynamo-disaggregation","Disaggregated serving timeline + KV transfer latency across interconnect types",
        "- *Inference Engineering* Ch 4.4 (Philip Kiely, Baseten Books 2026)\n- NVIDIA Dynamo documentation\n- Zhong et al. (2024), \"DistServe\"")
    return validate(p)

def day11():
    cells = [
        cell_md(["# Day 11: Quantization — Number Formats (FP8, INT8, INT4)\n","> *Inference Engineering* — Chapter 5.1.1 | Philip Kiely, Baseten Books 2026\n","\n**Layer:** Runtime | **Prerequisite:** Day 04 (Arithmetic Intensity)\n"],"c01"),
        cell_md(["## Concept Overview\n","\n",
            "Quantization reduces model precision from FP16/BF16 to lower-bit formats to reduce memory footprint and increase arithmetic throughput. ",
            "FP8 (E4M3 or E5M2) halves memory vs FP16 with minimal accuracy loss on modern hardware (H100+). ",
            "INT8 is widely supported and roughly doubles throughput vs FP16. INT4 is the sweet spot for weight-only quantization — 4x compression at acceptable accuracy loss. ",
            "The key tradeoff: lower bits = less memory + faster compute, but higher quantization error requires calibration or training-aware compensation.\n"],"c02"),
        cell_code(["import torch\nimport numpy as np\nimport matplotlib.pyplot as plt\n\nprint(f'CUDA available: {torch.cuda.is_available()}')\nif torch.cuda.is_available():\n    print(f'GPU: {torch.cuda.get_device_name(0)}')\n"],"c03"),
        cell_md(["## 1. Number Format Ranges and Precision\n","\n","Different formats trade off between representable range and precision (mantissa bits).\n",
            "\n| Format | Bits | Exp | Mantissa | Max Value | Min Normal |\n",
            "|--------|------|-----|----------|-----------|------------|\n",
            "| FP32   | 32   | 8   | 23       | 3.4e38    | 1.2e-38    |\n",
            "| BF16   | 16   | 8   | 7        | 3.4e38    | 1.2e-38    |\n",
            "| FP16   | 16   | 5   | 10       | 65504     | 6.1e-5     |\n",
            "| FP8 E4M3 | 8  | 4   | 3        | 448       | 0.0156     |\n",
            "| FP8 E5M2 | 8  | 5   | 2        | 57344     | 1.5e-5     |\n",
            "| INT8   | 8    | -   | -        | 127       | -          |\n",
            "| INT4   | 4    | -   | -        | 7         | -          |\n"],"c04"),
        cell_code(["def quantize_symmetric(x, n_bits, scale=None):\n    \"\"\"Symmetric uniform quantization.\"\"\"\n    qmax = 2**(n_bits-1) - 1\n    if scale is None:\n        scale = x.abs().max() / qmax\n    x_q = torch.clamp(torch.round(x / scale), -qmax, qmax).to(torch.int8)\n    x_dq = x_q.float() * scale\n    return x_q, x_dq, scale\n\n# Generate a realistic weight distribution\ntorch.manual_seed(42)\nweights = torch.randn(1000) * 0.02  # typical transformer weight scale\n\nfig, axes = plt.subplots(1, 3, figsize=(15, 4))\nfor ax, bits, title in zip(axes, [8, 4, 2], ['INT8', 'INT4', 'INT2']):\n    _, dq, scale = quantize_symmetric(weights, bits)\n    error = (weights - dq).abs()\n    ax.hist(weights.numpy(), bins=50, alpha=0.6, label='Original', density=True)\n    ax.hist(dq.numpy(), bins=50, alpha=0.6, label=f'{title} dequant', density=True)\n    ax.set_title(f'{title}: scale={scale:.4f}, MAE={error.mean():.4f}')\n    ax.legend(fontsize=8)\n    ax.set_xlabel('Weight value')\n\nplt.tight_layout()\nplt.savefig('quantization_formats.png', dpi=100, bbox_inches='tight')\nplt.show()\nprint('Saved quantization_formats.png')\n"],"c05"),
        cell_md(["## 2. Quantization Error vs Bit Width\n","\n","The quantization error scales with $\\Delta/2$ where $\\Delta = \\text{range}/(2^N - 1)$ is the step size. ",
            "For Gaussian weights, the expected MSE under uniform quantization is approximately $\\Delta^2/12$.\n"],"c06"),
        cell_code(["results = []\nfor bits in [1, 2, 3, 4, 5, 6, 7, 8]:\n    _, dq, _ = quantize_symmetric(weights, bits)\n    mse = ((weights - dq)**2).mean().item()\n    snr = (weights**2).mean().item() / (mse + 1e-12)\n    results.append({'bits': bits, 'mse': mse, 'snr_db': 10*np.log10(snr)})\n\nfig, (ax1, ax2) = plt.subplots(1, 2, figsize=(12, 4))\nbits_list = [r['bits'] for r in results]\nmse_list = [r['mse'] for r in results]\nsnr_list = [r['snr_db'] for r in results]\n\nax1.semilogy(bits_list, mse_list, 'b-o')\nax1.set_xlabel('Bit Width'); ax1.set_ylabel('MSE')\nax1.set_title('Quantization Error vs Bit Width')\nax1.set_xticks(bits_list)\nax1.grid(True)\n\nax2.plot(bits_list, snr_list, 'r-o')\nax2.set_xlabel('Bit Width'); ax2.set_ylabel('SNR (dB)')\nax2.set_title('Signal-to-Noise Ratio vs Bit Width')\nax2.set_xticks(bits_list)\nax2.axhline(20, color='gray', linestyle='--', label='20dB threshold')\nax2.legend()\nax2.grid(True)\n\nplt.tight_layout()\nplt.savefig('quantization_snr.png', dpi=100, bbox_inches='tight')\nplt.show()\n\nprint(f'{\"Bits\":>6} {\"MSE\":>12} {\"SNR (dB)\":>10}')\nfor r in results:\n    print(f'{r[\"bits\"]:>6} {r[\"mse\"]:>12.6f} {r[\"snr_db\"]:>10.1f}')\n"],"c07"),
        cell_md(["## 3. Memory and Throughput Savings\n","\n","The practical impact: how much GPU memory does quantization save, and how does this translate to throughput?\n"],"c08"),
        cell_code(["model_configs = [\n    ('Llama-3-8B',   8e9),\n    ('Llama-3-70B',  70e9),\n    ('Llama-3-405B', 405e9),\n]\nformats = [\n    ('FP32', 4), ('BF16', 2), ('FP16', 2),\n    ('FP8',  1), ('INT8', 1), ('INT4', 0.5),\n]\nprint(f'{\"Model\":<20} {\"FP32 (GB)\":>10}', end='')\nfor fmt, _ in formats[3:]:\n    print(f' {fmt+\" (GB)\":>9}', end='')\nprint()\nprint('-'*80)\nfor name, params in model_configs:\n    fp32_gb = params * 4 / 1e9\n    print(f'{name:<20} {fp32_gb:>10.0f}', end='')\n    for fmt, bytes_per in formats[3:]:\n        gb = params * bytes_per / 1e9\n        print(f' {gb:>9.0f}', end='')\n    print()\n\nprint('\\nThroughput multipliers vs FP16 (approximate, A100/H100):')\nfor fmt, throughput_mult in [('FP16',1.0),('FP8',1.9),('INT8',2.0),('INT4',3.5),('INT4 w/o dequant',4.0)]:\n    print(f'  {fmt:<25} {throughput_mult:.1f}x')\n"],"c09"),
        cell_md(["## Experiments: Try These\n",
            "\n",
            "1. **Outlier impact**: Generate weights with 1% extreme outliers and compare INT8 vs INT4 quantization error. This motivates SmoothQuant and GPTQ.\n",
            "2. **Block quantization**: Implement block-wise quantization (one scale per 128 elements) and compare error to per-tensor quantization.\n",
            "3. **FP8 simulation**: Implement E4M3 quantization in Python using float32 arithmetic. Verify max value = 448.\n"],"c10"),
        cell_md(["## Key Takeaways\n",
            "\n",
            "- Quantization maps floating-point weights/activations to integers, trading precision for memory and compute savings.\n",
            "- FP8 E4M3 is the modern sweet spot for activations: same range as BF16, 2x memory saving, natively supported on H100/Blackwell.\n",
            "- INT4 weight-only quantization (4 bits for weights, FP16 for activations) gives 4x memory savings at ~1-2% perplexity cost for large models.\n",
            "- Quantization error scales as $O(2^{-N})$ — each additional bit halves the error, but hardware throughput gains get smaller above 8 bits.\n",
            "\n## References\n- *Inference Engineering* Ch 5.1.1 — Philip Kiely, Baseten Books 2026\n- Dettmers et al. (2022), \"LLM.int8(): 8-bit Matrix Multiplication for Transformers\"\n- NVIDIA FP8 formats specification (H100)\n"],"c11"),
    ]
    p = write_nb(11, "quantization-number-formats", cells)
    write_readme(11,"Quantization: Number Formats (FP8, INT8, INT4)","Runtime",
        "Explored quantization number formats from FP32 down to INT4. Implemented symmetric quantization, measured MSE and SNR across bit widths, and computed memory savings for real model sizes across formats.",
        "Each bit halves quantization error (SNR increases ~6dB per bit) — the diminishing returns above INT8 explain why INT4 is the practical lower bound for acceptable weight-only quantization quality.",
        "quantization-number-formats","Quantization error (MSE/SNR) vs bit width + model memory footprint across formats",
        "- *Inference Engineering* Ch 5.1.1 (Philip Kiely, Baseten Books 2026)\n- Dettmers et al. (2022), \"LLM.int8()\"")
    return validate(p)

def day12():
    cells = [
        cell_md(["# Day 12: Quantization — GPTQ, AWQ, SmoothQuant\n","> *Inference Engineering* — Chapter 5.1.2 | Philip Kiely, Baseten Books 2026\n","\n**Layer:** Runtime | **Prerequisite:** Day 11 (Number Formats)\n"],"c01"),
        cell_md(["## Concept Overview\n","\n",
            "Post-training quantization (PTQ) algorithms minimize accuracy loss when quantizing to INT4/INT8. ",
            "GPTQ uses second-order weight correction via Hessian information to compensate for rounding errors after quantizing each weight. ",
            "AWQ (Activation-aware Weight Quantization) identifies the ~1% of channels with large activations and scales them before quantization, protecting the most important weights. ",
            "SmoothQuant transfers quantization difficulty from activations (hard to quantize) to weights (easy to quantize) by migrating scaling factors between the two. ",
            "All three achieve near-FP16 accuracy at INT4 weight precision.\n"],"c02"),
        cell_code(["import torch\nimport torch.nn as nn\nimport numpy as np\nimport matplotlib.pyplot as plt\n\nprint(f'CUDA available: {torch.cuda.is_available()}')\nif torch.cuda.is_available():\n    print(f'GPU: {torch.cuda.get_device_name(0)}')\n"],"c03"),
        cell_md(["## 1. The Problem: Activation Outliers\n","\n",
            "LLM activations have extreme outliers in certain channels — values 100x larger than typical. ",
            "These outliers dominate the quantization range, causing most values to map to just a few quantization bins.\n"],"c04"),
        cell_code(["# Simulate activation outlier problem\nnp.random.seed(42)\ntorch.manual_seed(42)\n\n# Typical LLM hidden state: most channels normal, a few with large outliers\nd_model = 4096\nactivations = torch.randn(32, d_model) * 0.5  # 32 tokens, 4096 channels\n\n# Inject outlier channels (1% of channels have 100x larger values)\noutlier_channels = torch.randperm(d_model)[:int(d_model*0.01)]\nactivations[:, outlier_channels] *= 100\n\nfig, (ax1, ax2) = plt.subplots(1, 2, figsize=(12, 4))\n\n# Channel max values\nchannel_max = activations.abs().max(dim=0).values\nax1.semilogy(sorted(channel_max.numpy(), reverse=True))\nax1.set_xlabel('Channel (sorted by magnitude)')\nax1.set_ylabel('Max |activation|')\nax1.set_title('Activation Magnitude Distribution')\nax1.axvline(int(d_model*0.01), color='red', linestyle='--', label='Outlier threshold')\nax1.legend()\nax1.grid(True)\n\n# Show per-tensor vs per-channel quantization quality\ndef quant_int8(x, per_channel=False):\n    if per_channel:\n        scale = x.abs().max(dim=-1, keepdim=True).values / 127\n    else:\n        scale = x.abs().max() / 127\n    scale = scale.clamp(min=1e-8)\n    return (x / scale).round().clamp(-127, 127) * scale\n\nq_tensor = quant_int8(activations, per_channel=False)\nq_channel = quant_int8(activations, per_channel=True)\n\nerr_tensor = (activations - q_tensor).abs().mean().item()\nerr_channel = (activations - q_channel).abs().mean().item()\n\nax2.bar(['Per-tensor INT8', 'Per-channel INT8'], [err_tensor, err_channel],\n        color=['salmon','steelblue'])\nax2.set_ylabel('Mean Absolute Error')\nax2.set_title('Quantization Error: Tensor vs Channel')\nfor i, v in enumerate([err_tensor, err_channel]):\n    ax2.text(i, v, f'{v:.4f}', ha='center', va='bottom')\n\nplt.tight_layout()\nplt.savefig('activation_outliers.png', dpi=100, bbox_inches='tight')\nplt.show()\nprint(f'Per-tensor INT8 MAE:  {err_tensor:.4f}')\nprint(f'Per-channel INT8 MAE: {err_channel:.4f}')\nprint(f'Per-channel improvement: {err_tensor/err_channel:.1f}x')\n"],"c05"),
        cell_md(["## 2. SmoothQuant — Migrating Quantization Difficulty\n","\n",
            "SmoothQuant scales activations down (by $s$) and weights up (by $s$) along the channel dimension, ",
            "moving the quantization difficulty from activations to weights. Since weights can be quantized offline (per-channel), this dramatically reduces activation quantization error.\n",
            "\n$$\\hat{Y} = (X \\text{diag}(s)^{-1}) \\cdot (\\text{diag}(s) W) = \\hat{X} \\hat{W}$$\n"],"c06"),
        cell_code(["def smooth_quant_scale(activations, weights, alpha=0.5):\n    \"\"\"\n    Compute per-channel smoothing scale for SmoothQuant.\n    alpha controls how much difficulty shifts to weights (0=all to activations, 1=all to weights)\n    \"\"\"\n    # Channel-wise max stats\n    act_max = activations.abs().max(dim=0).values  # [d_model]\n    w_max = weights.abs().max(dim=0).values         # [d_model] (per output channel)\n\n    scale = (act_max**alpha) / (w_max**(1-alpha) + 1e-8)\n    return scale\n\n# Simulate SmoothQuant on a linear layer\ntorch.manual_seed(42)\nbatch, d_in, d_out = 32, 512, 1024\n\nX = torch.randn(batch, d_in) * 0.5\nX[:, :5] *= 100  # 5 outlier channels\nW = torch.randn(d_out, d_in) * 0.02\n\n# Without SmoothQuant\ndef quant_tensor(x, bits=8):\n    scale = x.abs().max() / (2**(bits-1)-1)\n    return (x/scale).round().clamp(-(2**(bits-1)-1), 2**(bits-1)-1) * scale\n\nX_q = quant_tensor(X)\nW_q = quant_tensor(W)\nerr_naive = ((X @ W.T) - (X_q @ W_q.T)).abs().mean().item()\n\n# With SmoothQuant\nscales = smooth_quant_scale(X, W.T)\nX_smooth = X / scales\nW_smooth = W * scales\nX_smooth_q = quant_tensor(X_smooth)\nW_smooth_q = quant_tensor(W_smooth)\nerr_smooth = ((X @ W.T) - (X_smooth_q @ W_smooth_q.T)).abs().mean().item()\n\nprint(f'Naive INT8 quantization error:     {err_naive:.4f}')\nprint(f'SmoothQuant INT8 quantization error: {err_smooth:.4f}')\nprint(f'Improvement: {err_naive/err_smooth:.1f}x lower error')\n\n# Show scale distribution\nplt.figure(figsize=(8, 4))\nplt.hist(scales.numpy(), bins=50)\nplt.xlabel('Smoothing Scale')\nplt.ylabel('Count')\nplt.title('SmoothQuant Per-Channel Scales')\nplt.savefig('smoothquant_scales.png', dpi=100, bbox_inches='tight')\nplt.show()\n"],"c07"),
        cell_md(["## 3. AWQ — Activation-Aware Weight Quantization\n","\n",
            "AWQ finds the ~1% of weight channels with the largest activation magnitudes and applies a per-channel scale to protect them. ",
            "Unlike SmoothQuant, AWQ is weight-only quantization — activations remain in FP16.\n"],"c08"),
        cell_code(["def awq_scale(activations, weights, top_frac=0.01, grid_size=20):\n    \"\"\"\n    Simplified AWQ: search for per-channel scale that minimizes quantization error\n    for the most salient channels.\n    \"\"\"\n    act_importance = activations.abs().mean(dim=0)  # [d_in]\n\n    # Identify salient channels\n    n_salient = max(1, int(len(act_importance) * top_frac))\n    salient_channels = act_importance.topk(n_salient).indices\n\n    # Grid search for best scale\n    best_scale = torch.ones(weights.shape[1])\n    best_err = float('inf')\n    true_out = X @ W.T\n\n    for s_exp in np.linspace(-1, 1, grid_size):\n        scale = torch.ones(weights.shape[1])\n        scale[salient_channels] = 2**s_exp\n        W_scaled = W * scale\n        W_q = quant_tensor(W_scaled.contiguous())\n        W_dq = W_q / scale\n        out_q = X @ W_dq.T\n        err = (true_out - out_q).abs().mean().item()\n        if err < best_err:\n            best_err = err\n            best_scale = scale.clone()\n\n    return best_scale, best_err\n\nbest_scale, awq_err = awq_scale(X, W)\nprint(f'AWQ INT4-style quantization error: {awq_err:.4f}')\nprint(f'Improvement vs naive: {err_naive/awq_err:.1f}x')\nprint(f'AWQ salient channel scale range: [{best_scale.min():.3f}, {best_scale.max():.3f}]')\n\nprint('\\nSummary comparison:')\nfor method, err in [('Naive INT8', err_naive), ('SmoothQuant', err_smooth), ('AWQ-style', awq_err)]:\n    print(f'  {method:<20} error={err:.4f}')\n"],"c09"),
        cell_md(["## Experiments: Try These\n",
            "\n",
            "1. **Alpha sensitivity in SmoothQuant**: Sweep alpha from 0 to 1 and plot quantization error. What alpha minimizes error for your weight/activation distribution?\n",
            "2. **Block-wise GPTQ**: Implement Optimal Brain Quantization (OBQ) — quantize weights one at a time, correcting remaining weights using the inverse Hessian.\n",
            "3. **Perplexity measurement**: Quantize a small GPT-2 model (117M) with naive INT8 vs AWQ-style scaling and measure perplexity on WikiText-2.\n"],"c10"),
        cell_md(["## Key Takeaways\n",
            "\n",
            "- LLM activations have ~1% of channels with 100x outlier magnitudes — these ruin naive per-tensor quantization.\n",
            "- SmoothQuant migrates difficulty from activations (hard) to weights (easy) by applying channel-wise scales, enabling efficient INT8 W8A8 quantization.\n",
            "- AWQ identifies salient activation channels and applies per-channel scales to weights, achieving near-FP16 accuracy at INT4 weight precision.\n",
            "- GPTQ uses second-order Hessian information to compensate for rounding errors — more compute at quantization time, better accuracy at INT4.\n",
            "\n## References\n- *Inference Engineering* Ch 5.1.2 — Philip Kiely, Baseten Books 2026\n- Xiao et al. (2022), \"SmoothQuant\"\n- Lin et al. (2023), \"AWQ: Activation-aware Weight Quantization\"\n- Frantar et al. (2022), \"GPTQ: Accurate Post-Training Quantization\"\n"],"c11"),
    ]
    p = write_nb(12, "quantization-gptq-awq-smoothquant", cells)
    write_readme(12,"Quantization: GPTQ, AWQ, SmoothQuant","Runtime",
        "Implemented three PTQ algorithms — SmoothQuant, AWQ, and a conceptual GPTQ — to handle LLM activation outliers. Measured the quantization error reduction of each approach and visualized the per-channel scale distributions they learn.",
        "Activation outliers in ~1% of channels account for the majority of INT8 quantization error. SmoothQuant and AWQ both isolate and protect these channels, explaining why they achieve near-FP16 accuracy at INT4-INT8 precision.",
        "quantization-gptq-awq-smoothquant","Activation outlier visualization + SmoothQuant vs AWQ error comparison",
        "- *Inference Engineering* Ch 5.1.2 (Philip Kiely, Baseten Books 2026)\n- Xiao et al. (2022), \"SmoothQuant\"\n- Lin et al. (2023), \"AWQ\"")
    return validate(p)

def day13():
    cells = [
        cell_md(["# Day 13: Speculative Decoding — Draft-Target, Medusa, EAGLE\n","> *Inference Engineering* — Chapter 5.2 | Philip Kiely, Baseten Books 2026\n","\n**Layer:** Runtime | **Prerequisite:** Day 11 (Quantization), Day 01 (Decoding)\n"],"c01"),
        cell_md(["## Concept Overview\n","\n",
            "Speculative decoding accelerates autoregressive generation without changing output distribution. ",
            "A small draft model proposes K tokens; the large target model verifies all K+1 positions in one parallel forward pass. ",
            "If all K tokens are accepted, we get K+1 tokens in the time of 2 model calls (vs K+1 calls normally). ",
            "Medusa adds multiple prediction heads to the base model itself, eliminating the separate draft model. ",
            "EAGLE improves draft quality by conditioning on the target model's hidden states rather than just output tokens.\n"],"c02"),
        cell_code(["import torch\nimport torch.nn.functional as F\nimport numpy as np\nimport matplotlib.pyplot as plt\n\nprint(f'CUDA available: {torch.cuda.is_available()}')\nif torch.cuda.is_available():\n    print(f'GPU: {torch.cuda.get_device_name(0)}')\n"],"c03"),
        cell_md(["## 1. Speculative Decoding Algorithm\n","\n",
            "The acceptance sampling criterion ensures the output distribution matches the target model exactly:\n",
            "\n$$p_{\\text{accept}}(x) = \\min\\left(1, \\frac{p_{\\text{target}}(x)}{p_{\\text{draft}}(x)}\\right)$$\n",
            "\nRejected tokens are resampled from a corrected distribution, maintaining exact target distribution matching.\n"],"c04"),
        cell_code(["def speculative_decode_step(target_logits, draft_logits, draft_token, temperature=1.0):\n    \"\"\"\n    One step of speculative decoding acceptance sampling.\n    Returns (accepted, sampled_token)\n    \"\"\"\n    p_target = F.softmax(target_logits / temperature, dim=-1)\n    p_draft = F.softmax(draft_logits / temperature, dim=-1)\n\n    # Acceptance probability\n    accept_prob = torch.min(torch.ones_like(p_target), p_target / (p_draft + 1e-8))\n    p_accept = accept_prob[draft_token].item()\n\n    # Accept with probability min(1, p_target/p_draft)\n    if torch.rand(1).item() < p_accept:\n        return True, draft_token\n    else:\n        # Resample from corrected distribution\n        p_corrected = torch.clamp(p_target - p_draft, min=0)\n        p_corrected = p_corrected / p_corrected.sum()\n        resampled = torch.multinomial(p_corrected, 1).item()\n        return False, resampled\n\n# Simulate speculative decoding over 100 steps\ntorch.manual_seed(42)\nvocab_size = 32000\nK = 4  # draft tokens per speculation step\n\ndef run_speculation(num_steps=200, K=4, draft_quality=0.7):\n    \"\"\"\n    Simulate speculative decoding.\n    draft_quality: probability that draft token matches target's top-1\n    \"\"\"\n    total_tokens = 0\n    total_steps = 0\n    accept_counts = []\n\n    for _ in range(num_steps):\n        # Draft proposes K tokens\n        # Simulate: each token accepted with prob related to draft quality\n        n_accepted = 0\n        for k in range(K):\n            target_logits = torch.randn(vocab_size)\n            draft_logits = target_logits.clone()\n            # Reduce draft quality by adding noise\n            draft_logits += torch.randn(vocab_size) * (1 - draft_quality) * 3\n            draft_tok = torch.argmax(F.softmax(draft_logits, dim=-1)).item()\n            accepted, _ = speculative_decode_step(target_logits, draft_logits, draft_tok)\n            if accepted:\n                n_accepted += 1\n            else:\n                break  # rejection stops the chain\n\n        tokens_this_step = n_accepted + 1  # +1 for the always-generated bonus token\n        total_tokens += tokens_this_step\n        total_steps += 1\n        accept_counts.append(n_accepted)\n\n    return total_tokens / total_steps, np.mean(accept_counts)\n\nprint('Speculative decoding simulation:')\nprint(f'{\"Draft Quality\":>15} {\"Tokens/Step\":>12} {\"Mean Accepts\":>13} {\"Speedup\":>10}')\nfor quality in [0.5, 0.6, 0.7, 0.8, 0.9, 0.95]:\n    tps, acc = run_speculation(draft_quality=quality)\n    print(f'{quality:>15.2f} {tps:>12.2f} {acc:>13.2f} {tps:>10.2f}x')\n"],"c05"),
        cell_md(["## 2. Acceptance Rate vs Speedup\n","\n",
            "The expected speedup with acceptance rate $\\alpha$ and K draft tokens is:\n",
            "$$\\text{Speedup} = \\frac{1 - \\alpha^{K+1}}{(1-\\alpha)(1 + c)}$$\n",
            "where $c$ is the cost ratio (draft model time / target model time).\n"],"c06"),
        cell_code(["def expected_speedup(alpha, K, cost_ratio=0.1):\n    \"\"\"\n    Expected tokens per second improvement.\n    alpha: per-token acceptance rate\n    K: number of draft tokens\n    cost_ratio: draft_model_time / target_model_time\n    \"\"\"\n    # Expected accepted tokens per speculation step\n    expected_accepted = sum((alpha**k) * (1 - (alpha if k < K else 0)) * k\n                           for k in range(K+1)) + alpha**K * K\n    # Simplified: E[accepted] ≈ (1 - alpha^(K+1)) / (1 - alpha)\n    e_accepted = (1 - alpha**(K+1)) / (1 - alpha + 1e-8)\n    tokens_per_step = e_accepted + 1  # +1 bonus\n    step_cost = K * cost_ratio + 1  # K draft calls + 1 target call\n    return tokens_per_step / step_cost\n\nfig, (ax1, ax2) = plt.subplots(1, 2, figsize=(12, 5))\n\nalphas = np.linspace(0.5, 0.99, 100)\nfor K in [1, 2, 4, 8]:\n    speedups = [expected_speedup(a, K) for a in alphas]\n    ax1.plot(alphas, speedups, label=f'K={K}')\nax1.set_xlabel('Acceptance Rate α')\nax1.set_ylabel('Expected Speedup')\nax1.set_title('Speculative Decoding Speedup\\n(cost_ratio=0.1)')\nax1.legend()\nax1.grid(True)\nax1.axhline(1.0, color='black', linestyle='--', alpha=0.5)\n\nKs = range(1, 17)\nfor alpha in [0.7, 0.8, 0.9]:\n    speedups = [expected_speedup(alpha, K) for K in Ks]\n    ax2.plot(Ks, speedups, label=f'α={alpha}')\nax2.set_xlabel('Draft Tokens K')\nax2.set_ylabel('Expected Speedup')\nax2.set_title('Optimal K vs Acceptance Rate')\nax2.legend()\nax2.grid(True)\n\nplt.tight_layout()\nplt.savefig('speculative_decoding.png', dpi=100, bbox_inches='tight')\nplt.show()\n"],"c07"),
        cell_md(["## 3. Medusa: Multi-Head Speculation\n","\n",
            "Medusa adds K independent prediction heads on top of the base model, each predicting the token at position t+k. ",
            "No separate model — the heads share the base model's hidden states. ",
            "Tree-based verification allows parallel validation of multiple candidate sequences.\n"],"c08"),
        cell_code(["class MedusaHeads(torch.nn.Module):\n    def __init__(self, hidden_size, vocab_size, num_heads=4):\n        super().__init__()\n        self.heads = torch.nn.ModuleList([\n            torch.nn.Sequential(\n                torch.nn.Linear(hidden_size, hidden_size),\n                torch.nn.SiLU(),\n                torch.nn.Linear(hidden_size, vocab_size)\n            ) for _ in range(num_heads)\n        ])\n\n    def forward(self, hidden_state):\n        # hidden_state: [batch, seq, hidden]\n        return [head(hidden_state) for head in self.heads]\n\nmedusa = MedusaHeads(hidden_size=512, vocab_size=32000, num_heads=4)\nhidden = torch.randn(1, 10, 512)\nhead_logits = medusa(hidden)\nprint(f'Medusa heads output: {len(head_logits)} heads, each {head_logits[0].shape}')\nprint(f'Head 0 (t+1) top-5 tokens: {head_logits[0][0,-1].topk(5).indices.tolist()}')\nprint(f'Head 1 (t+2) top-5 tokens: {head_logits[1][0,-1].topk(5).indices.tolist()}')\n\nparams_base = 512 * 32000 * 2  # approximate base LM head\nparams_medusa = sum(p.numel() for p in medusa.parameters())\nprint(f'\\nMedusa head overhead: {params_medusa/params_base:.1f}x base LM head size')\nprint(f'In practice: ~1-2% of total model parameters')\n"],"c09"),
        cell_md(["## Experiments: Try These\n",
            "\n",
            "1. **Acceptance rate by domain**: Sample text from code vs prose vs math. Compare simulated acceptance rates — where does speculative decoding help most?\n",
            "2. **EAGLE draft**: EAGLE uses the target model's hidden states as context for the draft model. Why does this improve acceptance rate vs token-only drafting?\n",
            "3. **Tree speculation**: Implement tree-based speculation where K heads generate a tree of candidates. How many tokens can you verify with one target forward pass?\n"],"c10"),
        cell_md(["## Key Takeaways\n",
            "\n",
            "- Speculative decoding verifies K draft tokens in a single target model forward pass — the output distribution is mathematically identical to standard decoding.\n",
            "- Expected speedup = (1 + mean_accepted_tokens) / (1 + K * cost_ratio) — acceptance rate and draft cost both matter.\n",
            "- Medusa eliminates the draft model by adding prediction heads to the base model itself, trading head training cost for zero draft model memory.\n",
            "- EAGLE and similar methods improve acceptance rate by conditioning the draft on target model hidden states, not just output tokens.\n",
            "\n## References\n- *Inference Engineering* Ch 5.2 — Philip Kiely, Baseten Books 2026\n- Leviathan et al. (2022), \"Fast Inference from Transformers via Speculative Decoding\"\n- Cai et al. (2023), \"Medusa: Simple LLM Inference Acceleration Framework\"\n- Li et al. (2024), \"EAGLE: Speculative Sampling Requires Rethinking Feature Uncertainty\"\n"],"c11"),
    ]
    p = write_nb(13, "speculative-decoding", cells)
    write_readme(13,"Speculative Decoding: Draft-Target, Medusa, EAGLE","Runtime",
        "Implemented speculative decoding acceptance sampling from scratch and derived the speedup formula analytically. Simulated acceptance rates under different draft quality levels, plotted optimal K vs acceptance rate, and built a Medusa multi-head prediction module.",
        "The key insight: the speculative decoding output distribution is provably identical to the target model alone — speedup comes for free from the acceptance/rejection mechanism, not from approximation.",
        "speculative-decoding","Speedup curves vs acceptance rate and K + Medusa head implementation",
        "- *Inference Engineering* Ch 5.2 (Philip Kiely, Baseten Books 2026)\n- Leviathan et al. (2022), \"Fast Inference from Transformers via Speculative Decoding\"\n- Cai et al. (2023), \"Medusa\"")
    return validate(p)

print("Days 6-13 functions defined")
